// This file is auto-generated by @hey-api/openapi-ts

import type { Client, Options as Options2, TDataShape } from './client';
import { client } from './client.gen';
import type { AddTeamExternalGroupData, AddTeamExternalGroupErrors, AddTeamExternalGroupResponses, AddTeamMemberData, AddTeamMemberErrors, AddTeamMemberResponses, AnthropicListModelsWithAgentData, AnthropicListModelsWithAgentErrors, AnthropicListModelsWithAgentResponses, AnthropicListModelsWithDefaultAgentData, AnthropicListModelsWithDefaultAgentErrors, AnthropicListModelsWithDefaultAgentResponses, AnthropicMessagesWithAgentData, AnthropicMessagesWithAgentErrors, AnthropicMessagesWithAgentResponses, AnthropicMessagesWithDefaultAgentData, AnthropicMessagesWithDefaultAgentErrors, AnthropicMessagesWithDefaultAgentResponses, AppGalleryDeviceAuthPollData, AppGalleryDeviceAuthPollErrors, AppGalleryDeviceAuthPollResponses, AppGalleryDeviceAuthStartData, AppGalleryDeviceAuthStartErrors, AppGalleryDeviceAuthStartResponses, ApplyGithubPluginUpdateData, ApplyGithubPluginUpdateErrors, ApplyGithubPluginUpdateResponses, ApproveCatalogItemImageData, ApproveCatalogItemImageErrors, ApproveCatalogItemImageResponses, ArchestraChatCompletionsWithAgentData, ArchestraChatCompletionsWithAgentErrors, ArchestraChatCompletionsWithAgentResponses, ArchestraChatCompletionsWithDefaultAgentData, ArchestraChatCompletionsWithDefaultAgentErrors, ArchestraChatCompletionsWithDefaultAgentResponses, AssignConnectorToKnowledgeBasesData, AssignConnectorToKnowledgeBasesErrors, AssignConnectorToKnowledgeBasesResponses, AssignToolToAgentData, AssignToolToAgentErrors, AssignToolToAgentResponses, AssignToolToAppData, AssignToolToAppErrors, AssignToolToAppResponses, AutoConfigureAgentToolPoliciesData, AutoConfigureAgentToolPoliciesErrors, AutoConfigureAgentToolPoliciesResponses, AzureChatCompletionsWithAgentData, AzureChatCompletionsWithAgentErrors, AzureChatCompletionsWithAgentResponses, AzureChatCompletionsWithDefaultAgentData, AzureChatCompletionsWithDefaultAgentErrors, AzureChatCompletionsWithDefaultAgentResponses, AzureEmbeddingsWithAgentData, AzureEmbeddingsWithAgentErrors, AzureEmbeddingsWithAgentResponses, AzureEmbeddingsWithDefaultAgentData, AzureEmbeddingsWithDefaultAgentErrors, AzureEmbeddingsWithDefaultAgentResponses, AzureResponsesWithAgentData, AzureResponsesWithAgentErrors, AzureResponsesWithAgentResponses, AzureResponsesWithDefaultAgentData, AzureResponsesWithDefaultAgentErrors, AzureResponsesWithDefaultAgentResponses, BedrockConverseStreamWithAgentAndModelData, BedrockConverseStreamWithAgentAndModelResponses, BedrockConverseStreamWithAgentData, BedrockConverseStreamWithAgentResponses, BedrockConverseStreamWithDefaultAgentData, BedrockConverseStreamWithDefaultAgentResponses, BedrockConverseWithAgentAndModelData, BedrockConverseWithAgentAndModelErrors, BedrockConverseWithAgentAndModelResponses, BedrockConverseWithAgentData, BedrockConverseWithAgentErrors, BedrockConverseWithAgentResponses, BedrockConverseWithDefaultAgentData, BedrockConverseWithDefaultAgentErrors, BedrockConverseWithDefaultAgentResponses, BedrockInvokeStreamWithAgentAndModelData, BedrockInvokeStreamWithAgentAndModelResponses, BedrockInvokeStreamWithDefaultAgentAndModelData, BedrockInvokeStreamWithDefaultAgentAndModelResponses, BedrockInvokeWithAgentAndModelData, BedrockInvokeWithAgentAndModelErrors, BedrockInvokeWithAgentAndModelResponses, BedrockInvokeWithDefaultAgentAndModelData, BedrockInvokeWithDefaultAgentAndModelErrors, BedrockInvokeWithDefaultAgentAndModelResponses, BedrockOpenaiChatCompletionsWithAgentData, BedrockOpenaiChatCompletionsWithAgentResponses, BedrockOpenaiChatCompletionsWithDefaultAgentData, BedrockOpenaiChatCompletionsWithDefaultAgentResponses, BedrockOpenaiListModelsWithAgentData, BedrockOpenaiListModelsWithAgentErrors, BedrockOpenaiListModelsWithAgentResponses, BedrockOpenaiListModelsWithDefaultAgentData, BedrockOpenaiListModelsWithDefaultAgentErrors, BedrockOpenaiListModelsWithDefaultAgentResponses, BulkAssignToolsData, BulkAssignToolsErrors, BulkAssignToolsResponses, BulkDeleteAgentsData, BulkDeleteAgentsErrors, BulkDeleteAgentsResponses, BulkDeleteApiKeysData, BulkDeleteApiKeysErrors, BulkDeleteApiKeysResponses, BulkDeleteAppsData, BulkDeleteAppsErrors, BulkDeleteAppsResponses, BulkDeleteConnectorDocumentsData, BulkDeleteConnectorDocumentsErrors, BulkDeleteConnectorDocumentsResponses, BulkDeleteConnectorsData, BulkDeleteConnectorsErrors, BulkDeleteConnectorsResponses, BulkDeleteEnvironmentsData, BulkDeleteEnvironmentsErrors, BulkDeleteEnvironmentsResponses, BulkDeleteKnowledgeBasesData, BulkDeleteKnowledgeBasesErrors, BulkDeleteKnowledgeBasesResponses, BulkDeleteKnowledgeDirectoriesData, BulkDeleteKnowledgeDirectoriesErrors, BulkDeleteKnowledgeDirectoriesResponses, BulkDeleteKnowledgeFilesData, BulkDeleteKnowledgeFilesErrors, BulkDeleteKnowledgeFilesResponses, BulkDeleteLimitsData, BulkDeleteLimitsErrors, BulkDeleteLimitsResponses, BulkDeleteLlmOauthClientsData, BulkDeleteLlmOauthClientsErrors, BulkDeleteLlmOauthClientsResponses, BulkDeleteLlmProviderApiKeysData, BulkDeleteLlmProviderApiKeysErrors, BulkDeleteLlmProviderApiKeysResponses, BulkDeleteMcpServersData, BulkDeleteMcpServersErrors, BulkDeleteMcpServersResponses, BulkDeleteMembersData, BulkDeleteMembersErrors, BulkDeleteMembersResponses, BulkDeleteProjectsData, BulkDeleteProjectsErrors, BulkDeleteProjectsResponses, BulkDeleteRolesData, BulkDeleteRolesErrors, BulkDeleteRolesResponses, BulkDeleteServiceAccountsData, BulkDeleteServiceAccountsErrors, BulkDeleteServiceAccountsResponses, BulkDeleteSkillsData, BulkDeleteSkillsErrors, BulkDeleteSkillsResponses, BulkDeleteTeamsData, BulkDeleteTeamsErrors, BulkDeleteTeamsResponses, BulkDeleteVirtualApiKeysData, BulkDeleteVirtualApiKeysErrors, BulkDeleteVirtualApiKeysResponses, BulkRevokeSessionsData, BulkRevokeSessionsErrors, BulkRevokeSessionsResponses, BulkSetServiceAccountsDisabledData, BulkSetServiceAccountsDisabledErrors, BulkSetServiceAccountsDisabledResponses, BulkUpdateAgentsData, BulkUpdateAgentsErrors, BulkUpdateAgentsResponses, BulkUpdateAgentToolsData, BulkUpdateAgentToolsErrors, BulkUpdateAgentToolsResponses, BulkUpdateAppsData, BulkUpdateAppsErrors, BulkUpdateAppsResponses, BulkUpdateChatOpsBindingsData, BulkUpdateChatOpsBindingsErrors, BulkUpdateChatOpsBindingsResponses, BulkUpdateConnectorsData, BulkUpdateConnectorsErrors, BulkUpdateConnectorsResponses, BulkUpdateKnowledgeDirectoriesData, BulkUpdateKnowledgeDirectoriesErrors, BulkUpdateKnowledgeDirectoriesResponses, BulkUpdateKnowledgeFilesData, BulkUpdateKnowledgeFilesErrors, BulkUpdateKnowledgeFilesResponses, BulkUpdateModelsData, BulkUpdateModelsErrors, BulkUpdateModelsResponses, BulkUpdateProjectsData, BulkUpdateProjectsErrors, BulkUpdateProjectsResponses, BulkUpdateSkillsVisibilityData, BulkUpdateSkillsVisibilityErrors, BulkUpdateSkillsVisibilityResponses, BulkUpsertDefaultCallPolicyData, BulkUpsertDefaultCallPolicyErrors, BulkUpsertDefaultCallPolicyResponses, BulkUpsertDefaultResultPolicyData, BulkUpsertDefaultResultPolicyErrors, BulkUpsertDefaultResultPolicyResponses, CancelAppRecordingRenderData, CancelAppRecordingRenderErrors, CancelAppRecordingRenderResponses, CancelChatMcpTaskData, CancelChatMcpTaskErrors, CancelChatMcpTaskResponses, CancelConnectorRunData, CancelConnectorRunErrors, CancelConnectorRunResponses, CerebrasChatCompletionsWithAgentData, CerebrasChatCompletionsWithAgentErrors, CerebrasChatCompletionsWithAgentResponses, CerebrasChatCompletionsWithDefaultAgentData, CerebrasChatCompletionsWithDefaultAgentErrors, CerebrasChatCompletionsWithDefaultAgentResponses, CheckInvitationData, CheckInvitationErrors, CheckInvitationResponses, CheckSecretsConnectivityData, CheckSecretsConnectivityErrors, CheckSecretsConnectivityResponses, CheckTeamVaultFolderConnectivityData, CheckTeamVaultFolderConnectivityErrors, CheckTeamVaultFolderConnectivityResponses, ClearChatConversationErrorsData, ClearChatConversationErrorsErrors, ClearChatConversationErrorsResponses, CloneAgentData, CloneAgentErrors, CloneAgentResponses, CohereChatWithAgentData, CohereChatWithAgentErrors, CohereChatWithAgentResponses, CohereChatWithDefaultAgentData, CohereChatWithDefaultAgentErrors, CohereChatWithDefaultAgentResponses, CompactChatConversationData, CompactChatConversationErrors, CompactChatConversationResponses, CompleteGoogleDriveConnectorOAuthData, CompleteGoogleDriveConnectorOAuthResponses, CompleteOnboardingData, CompleteOnboardingErrors, CompleteOnboardingResponses, ConnectNgrokData, ConnectNgrokErrors, ConnectNgrokResponses, ConvertAgentToSkillData, ConvertAgentToSkillErrors, ConvertAgentToSkillResponses, CreateAgentData, CreateAgentErrors, CreateAgentResponses, CreateApiKeyData, CreateApiKeyErrors, CreateApiKeyResponses, CreateAppData, CreateAppErrors, CreateAppResponses, CreateChatConversationData, CreateChatConversationErrors, CreateChatConversationResponses, CreateChatOpsDmBindingData, CreateChatOpsDmBindingErrors, CreateChatOpsDmBindingResponses, CreateConnectionPassthroughKeyData, CreateConnectionPassthroughKeyErrors, CreateConnectionPassthroughKeyResponses, CreateConnectionSetupData, CreateConnectionSetupErrors, CreateConnectionSetupResponses, CreateConnectionVirtualKeyData, CreateConnectionVirtualKeyErrors, CreateConnectionVirtualKeyResponses, CreateConnectorData, CreateConnectorErrors, CreateConnectorResponses, CreateDefaultUserLimitData, CreateDefaultUserLimitErrors, CreateDefaultUserLimitResponses, CreateEnvironmentData, CreateEnvironmentErrors, CreateEnvironmentResponses, CreateGithubAppConfigData, CreateGithubAppConfigErrors, CreateGithubAppConfigResponses, CreateGithubPatData, CreateGithubPatErrors, CreateGithubPatResponses, CreateHookData, CreateHookErrors, CreateHookResponses, CreateIdentityProviderData, CreateIdentityProviderErrors, CreateIdentityProviderResponses, CreateInternalMcpCatalogItemData, CreateInternalMcpCatalogItemErrors, CreateInternalMcpCatalogItemResponses, CreateKnowledgeBaseData, CreateKnowledgeBaseErrors, CreateKnowledgeBaseResponses, CreateKnowledgeDirectoryData, CreateKnowledgeDirectoryErrors, CreateKnowledgeDirectoryResponses, CreateLimitData, CreateLimitErrors, CreateLimitResponses, CreateLlmOauthClientData, CreateLlmOauthClientErrors, CreateLlmOauthClientResponses, CreateLlmProviderApiKeyData, CreateLlmProviderApiKeyErrors, CreateLlmProviderApiKeyResponses, CreateMcpOauthClientData, CreateMcpOauthClientErrors, CreateMcpOauthClientResponses, CreatePluginData, CreatePluginErrors, CreatePluginResponses, CreateProjectData, CreateProjectErrors, CreateProjectFromConversationData, CreateProjectFromConversationErrors, CreateProjectFromConversationResponses, CreateProjectResponses, CreateRoleData, CreateRoleErrors, CreateRoleResponses, CreateScheduleTriggerData, CreateScheduleTriggerErrors, CreateScheduleTriggerResponses, CreateScheduleTriggerRunConversationData, CreateScheduleTriggerRunConversationErrors, CreateScheduleTriggerRunConversationResponses, CreateServiceAccountData, CreateServiceAccountErrors, CreateServiceAccountResponses, CreateServiceAccountTokenData, CreateServiceAccountTokenErrors, CreateServiceAccountTokenResponses, CreateSiteNotificationData, CreateSiteNotificationErrors, CreateSiteNotificationResponses, CreateSkillData, CreateSkillErrors, CreateSkillResponses, CreateSkillShareLinkData, CreateSkillShareLinkErrors, CreateSkillShareLinkResponses, CreateTeamData, CreateTeamErrors, CreateTeamResponses, CreateToolInvocationPolicyData, CreateToolInvocationPolicyErrors, CreateToolInvocationPolicyResponses, CreateTrustedDataPolicyData, CreateTrustedDataPolicyErrors, CreateTrustedDataPolicyResponses, CreateVirtualApiKeyData, CreateVirtualApiKeyErrors, CreateVirtualApiKeyResponses, DeepseekChatCompletionsWithAgentData, DeepseekChatCompletionsWithAgentErrors, DeepseekChatCompletionsWithAgentResponses, DeepseekChatCompletionsWithDefaultAgentData, DeepseekChatCompletionsWithDefaultAgentErrors, DeepseekChatCompletionsWithDefaultAgentResponses, DeleteAgentData, DeleteAgentDelegationData, DeleteAgentDelegationErrors, DeleteAgentDelegationResponses, DeleteAgentErrors, DeleteAgentResponses, DeleteApiKeyData, DeleteApiKeyErrors, DeleteApiKeyResponses, DeleteAppData, DeleteAppErrors, DeleteAppResponses, DeleteChatAttachmentData, DeleteChatAttachmentErrors, DeleteChatAttachmentResponses, DeleteChatConversationData, DeleteChatConversationErrors, DeleteChatConversationResponses, DeleteChatOpsBindingData, DeleteChatOpsBindingErrors, DeleteChatOpsBindingResponses, DeleteConnectorData, DeleteConnectorDocumentData, DeleteConnectorDocumentErrors, DeleteConnectorDocumentResponses, DeleteConnectorErrors, DeleteConnectorMemberOverrideData, DeleteConnectorMemberOverrideErrors, DeleteConnectorMemberOverrideResponses, DeleteConnectorResponses, DeleteConversationEnabledToolsData, DeleteConversationEnabledToolsErrors, DeleteConversationEnabledToolsResponses, DeleteDefaultUserLimitData, DeleteDefaultUserLimitErrors, DeleteDefaultUserLimitResponses, DeleteEnvironmentData, DeleteEnvironmentErrors, DeleteEnvironmentResponses, DeleteGithubAppConfigData, DeleteGithubAppConfigErrors, DeleteGithubAppConfigResponses, DeleteGithubPatData, DeleteGithubPatErrors, DeleteGithubPatResponses, DeleteHookData, DeleteHookErrors, DeleteHookResponses, DeleteIdentityProviderData, DeleteIdentityProviderErrors, DeleteIdentityProviderResponses, DeleteIncomingEmailSubscriptionData, DeleteIncomingEmailSubscriptionErrors, DeleteIncomingEmailSubscriptionResponses, DeleteInternalMcpCatalogItemByNameData, DeleteInternalMcpCatalogItemByNameErrors, DeleteInternalMcpCatalogItemByNameResponses, DeleteInternalMcpCatalogItemData, DeleteInternalMcpCatalogItemErrors, DeleteInternalMcpCatalogItemResponses, DeleteKnowledgeBaseData, DeleteKnowledgeBaseErrors, DeleteKnowledgeBaseResponses, DeleteKnowledgeDirectoryData, DeleteKnowledgeDirectoryErrors, DeleteKnowledgeDirectoryResponses, DeleteKnowledgeFileData, DeleteKnowledgeFileErrors, DeleteKnowledgeFileResponses, DeleteLimitData, DeleteLimitErrors, DeleteLimitResponses, DeleteLlmOauthClientData, DeleteLlmOauthClientErrors, DeleteLlmOauthClientResponses, DeleteLlmProviderApiKeyData, DeleteLlmProviderApiKeyErrors, DeleteLlmProviderApiKeyResponses, DeleteMcpOauthClientData, DeleteMcpOauthClientErrors, DeleteMcpOauthClientResponses, DeleteMcpServerData, DeleteMcpServerErrors, DeleteMcpServerResponses, DeletePendingSignupMemberData, DeletePendingSignupMemberErrors, DeletePendingSignupMemberResponses, DeletePluginData, DeletePluginErrors, DeletePluginResponses, DeleteProjectData, DeleteProjectErrors, DeleteProjectResponses, DeleteRoleData, DeleteRoleErrors, DeleteRoleResponses, DeleteScheduleTriggerData, DeleteScheduleTriggerErrors, DeleteScheduleTriggerResponses, DeleteServiceAccountData, DeleteServiceAccountErrors, DeleteServiceAccountResponses, DeleteServiceAccountTokenData, DeleteServiceAccountTokenErrors, DeleteServiceAccountTokenResponses, DeleteSiteNotificationData, DeleteSiteNotificationErrors, DeleteSiteNotificationResponses, DeleteSkillData, DeleteSkillErrors, DeleteSkillResponses, DeleteSkillSandboxArtifactData, DeleteSkillSandboxArtifactErrors, DeleteSkillSandboxArtifactResponses, DeleteTeamData, DeleteTeamErrors, DeleteTeamResponses, DeleteTeamVaultFolderData, DeleteTeamVaultFolderErrors, DeleteTeamVaultFolderResponses, DeleteToolData, DeleteToolErrors, DeleteToolInvocationPolicyData, DeleteToolInvocationPolicyErrors, DeleteToolInvocationPolicyResponses, DeleteToolResponses, DeleteTrustedDataPolicyData, DeleteTrustedDataPolicyErrors, DeleteTrustedDataPolicyResponses, DeleteVirtualApiKeyData, DeleteVirtualApiKeyErrors, DeleteVirtualApiKeyResponses, DisableAppData, DisableAppErrors, DisableAppResponses, DisableScheduleTriggerData, DisableScheduleTriggerErrors, DisableScheduleTriggerResponses, DisconnectNgrokData, DisconnectNgrokErrors, DisconnectNgrokResponses, DiscoverGithubPluginMarketplaceData, DiscoverGithubPluginMarketplaceErrors, DiscoverGithubPluginMarketplaceResponses, DiscoverGithubSkillsData, DiscoverGithubSkillsErrors, DiscoverGithubSkillsResponses, DownloadAppRecordingVideoData, DownloadAppRecordingVideoResponses, DropEmbeddingConfigData, DropEmbeddingConfigErrors, DropEmbeddingConfigResponses, EnableAppData, EnableAppErrors, EnableAppResponses, EnableScheduleTriggerData, EnableScheduleTriggerErrors, EnableScheduleTriggerResponses, EnableSkillToolDefaultsData, EnableSkillToolDefaultsErrors, EnableSkillToolDefaultsResponses, EnhanceAppRecordingData, EnhanceAppRecordingErrors, EnhanceAppRecordingResponses, ExportAgentData, ExportAgentErrors, ExportAgentResponses, ForceResyncConnectorData, ForceResyncConnectorErrors, ForceResyncConnectorResponses, ForkChatConversationData, ForkChatConversationErrors, ForkChatConversationResponses, ForkSharedConversationData, ForkSharedConversationErrors, ForkSharedConversationResponses, GeminiEmbeddingsWithAgentData, GeminiEmbeddingsWithAgentErrors, GeminiEmbeddingsWithAgentResponses, GeminiEmbeddingsWithDefaultAgentData, GeminiEmbeddingsWithDefaultAgentErrors, GeminiEmbeddingsWithDefaultAgentResponses, GenerateChatConversationTitleData, GenerateChatConversationTitleErrors, GenerateChatConversationTitleResponses, GenerateTelegramLinkCodeData, GenerateTelegramLinkCodeErrors, GenerateTelegramLinkCodeResponses, GetActiveChatRunData, GetActiveChatRunErrors, GetActiveChatRunResponses, GetAgentCredentialReadinessData, GetAgentCredentialReadinessErrors, GetAgentCredentialReadinessResponses, GetAgentData, GetAgentDelegationsData, GetAgentDelegationsErrors, GetAgentDelegationsResponses, GetAgentEmailAddressData, GetAgentEmailAddressErrors, GetAgentEmailAddressResponses, GetAgentErrors, GetAgentKnowledgeSourceExclusionsData, GetAgentKnowledgeSourceExclusionsErrors, GetAgentKnowledgeSourceExclusionsResponses, GetAgentResponses, GetAgentsData, GetAgentsErrors, GetAgentSkillExclusionsData, GetAgentSkillExclusionsErrors, GetAgentSkillExclusionsResponses, GetAgentSkillsData, GetAgentSkillsErrors, GetAgentSkillsResponses, GetAgentsResponses, GetAgentStatisticsData, GetAgentStatisticsErrors, GetAgentStatisticsResponses, GetAgentSubagentExclusionsData, GetAgentSubagentExclusionsErrors, GetAgentSubagentExclusionsResponses, GetAgentToolExclusionsData, GetAgentToolExclusionsErrors, GetAgentToolExclusionsResponses, GetAgentToolsData, GetAgentToolsErrors, GetAgentToolsResponses, GetAgentVersionData, GetAgentVersionErrors, GetAgentVersionResponses, GetAgentVersionsData, GetAgentVersionsErrors, GetAgentVersionsResponses, GetAllAgentsData, GetAllAgentsErrors, GetAllAgentsResponses, GetAllAgentToolsData, GetAllAgentToolsErrors, GetAllAgentToolsResponses, GetAllDelegationConnectionsData, GetAllDelegationConnectionsErrors, GetAllDelegationConnectionsResponses, GetAllVirtualApiKeysData, GetAllVirtualApiKeysErrors, GetAllVirtualApiKeysResponses, GetApiAuthBy__Data, GetApiAuthBy__Responses, GetApiAuthOauth2AuthorizeData, GetApiAuthOauth2AuthorizeResponses, GetApiKeyData, GetApiKeyErrors, GetApiKeyResponses, GetApiKeysData, GetApiKeysErrors, GetApiKeysResponses, GetAppData, GetAppearanceSettingsData, GetAppearanceSettingsErrors, GetAppearanceSettingsResponses, GetAppErrors, GetAppLabelKeysData, GetAppLabelKeysErrors, GetAppLabelKeysResponses, GetAppLabelValuesData, GetAppLabelValuesErrors, GetAppLabelValuesResponses, GetAppRecordingRenderStatusData, GetAppRecordingRenderStatusErrors, GetAppRecordingRenderStatusResponses, GetAppResponses, GetAppsData, GetAppsErrors, GetAppsResponses, GetAppStatisticsData, GetAppStatisticsErrors, GetAppStatisticsResponses, GetAppTemplatesData, GetAppTemplatesErrors, GetAppTemplatesResponses, GetAppToolsData, GetAppToolsErrors, GetAppToolsResponses, GetAppVersionData, GetAppVersionErrors, GetAppVersionResponses, GetAppVersionsData, GetAppVersionsErrors, GetAppVersionsResponses, GetAuditLogData, GetAuditLogErrors, GetAuditLogResponses, GetAuditLogsData, GetAuditLogsErrors, GetAuditLogsResponses, GetAuthStateData, GetAuthStateResponses, GetAvailableLlmProviderApiKeysData, GetAvailableLlmProviderApiKeysErrors, GetAvailableLlmProviderApiKeysResponses, GetChatAgentMcpToolsData, GetChatAgentMcpToolsErrors, GetChatAgentMcpToolsResponses, GetChatAttachmentContentData, GetChatAttachmentContentErrors, GetChatConversationData, GetChatConversationErrors, GetChatConversationFilesData, GetChatConversationFilesErrors, GetChatConversationFilesResponses, GetChatConversationResponses, GetChatConversationsData, GetChatConversationsErrors, GetChatConversationsResponses, GetChatOpsStatusData, GetChatOpsStatusErrors, GetChatOpsStatusResponses, GetConfigData, GetConfigResponses, GetConnectionHealthData, GetConnectionHealthErrors, GetConnectionHealthResponses, GetConnectionSetupScriptData, GetConnectionSetupScriptResponses, GetConnectorData, GetConnectorDocumentData, GetConnectorDocumentErrors, GetConnectorDocumentResponses, GetConnectorDocumentsData, GetConnectorDocumentsErrors, GetConnectorDocumentsResponses, GetConnectorErrors, GetConnectorKnowledgeBasesData, GetConnectorKnowledgeBasesErrors, GetConnectorKnowledgeBasesResponses, GetConnectorResponses, GetConnectorRunData, GetConnectorRunErrors, GetConnectorRunResponses, GetConnectorRunsData, GetConnectorRunsErrors, GetConnectorRunsResponses, GetConnectorsData, GetConnectorsErrors, GetConnectorsResponses, GetConnectorUserGroupsData, GetConnectorUserGroupsErrors, GetConnectorUserGroupsResponses, GetConversationEnabledToolsData, GetConversationEnabledToolsErrors, GetConversationEnabledToolsResponses, GetConversationShareData, GetConversationShareErrors, GetConversationShareResponses, GetCostSavingsStatisticsData, GetCostSavingsStatisticsErrors, GetCostSavingsStatisticsResponses, GetDefaultCredentialsStatusData, GetDefaultCredentialsStatusErrors, GetDefaultCredentialsStatusResponses, GetDefaultMcpGatewayData, GetDefaultMcpGatewayErrors, GetDefaultMcpGatewayResponses, GetDeletedChatConversationsData, GetDeletedChatConversationsErrors, GetDeletedChatConversationsResponses, GetDeploymentYamlPreviewData, GetDeploymentYamlPreviewErrors, GetDeploymentYamlPreviewResponses, GetExternalAppData, GetExternalAppErrors, GetExternalAppResponses, GetExternalMcpSkillData, GetExternalMcpSkillErrors, GetExternalMcpSkillResponses, GetExternalMcpSkillsData, GetExternalMcpSkillsErrors, GetExternalMcpSkillsResponses, GetExternalMcpSkillUsageStatisticsData, GetExternalMcpSkillUsageStatisticsErrors, GetExternalMcpSkillUsageStatisticsResponses, GetFeedbackPopupActivationData, GetFeedbackPopupActivationErrors, GetFeedbackPopupActivationResponses, GetGithubAppConfigData, GetGithubAppConfigErrors, GetGithubAppConfigResponses, GetHealthData, GetHealthResponses, GetHooksData, GetHooksErrors, GetHooksResponses, GetIdentityProviderData, GetIdentityProviderErrors, GetIdentityProviderIdpLogoutUrlData, GetIdentityProviderIdpLogoutUrlErrors, GetIdentityProviderIdpLogoutUrlResponses, GetIdentityProviderLatestIdTokenClaimsData, GetIdentityProviderLatestIdTokenClaimsErrors, GetIdentityProviderLatestIdTokenClaimsResponses, GetIdentityProviderLinkStatusData, GetIdentityProviderLinkStatusErrors, GetIdentityProviderLinkStatusResponses, GetIdentityProviderResponses, GetIdentityProvidersData, GetIdentityProvidersErrors, GetIdentityProvidersResponses, GetIdentityProviderTeamSyncOptionsData, GetIdentityProviderTeamSyncOptionsErrors, GetIdentityProviderTeamSyncOptionsResponses, GetImpersonableUsersData, GetImpersonableUsersErrors, GetImpersonableUsersResponses, GetIncomingEmailStatusData, GetIncomingEmailStatusErrors, GetIncomingEmailStatusResponses, GetInteractionData, GetInteractionErrors, GetInteractionResponses, GetInteractionsData, GetInteractionsErrors, GetInteractionSessionsData, GetInteractionSessionsErrors, GetInteractionSessionsResponses, GetInteractionsResponses, GetInternalMcpCatalogData, GetInternalMcpCatalogErrors, GetInternalMcpCatalogItemData, GetInternalMcpCatalogItemErrors, GetInternalMcpCatalogItemResponses, GetInternalMcpCatalogLabelKeysData, GetInternalMcpCatalogLabelKeysErrors, GetInternalMcpCatalogLabelKeysResponses, GetInternalMcpCatalogLabelValuesData, GetInternalMcpCatalogLabelValuesErrors, GetInternalMcpCatalogLabelValuesResponses, GetInternalMcpCatalogResponses, GetInternalMcpCatalogToolsBatchData, GetInternalMcpCatalogToolsBatchErrors, GetInternalMcpCatalogToolsBatchResponses, GetInternalMcpCatalogToolsData, GetInternalMcpCatalogToolsErrors, GetInternalMcpCatalogToolsResponses, GetK8sCapabilitiesData, GetK8sCapabilitiesErrors, GetK8sCapabilitiesResponses, GetK8sImagePullSecretsData, GetK8sImagePullSecretsErrors, GetK8sImagePullSecretsResponses, GetKeywordRankingStatusData, GetKeywordRankingStatusErrors, GetKeywordRankingStatusResponses, GetKnowledgeBaseData, GetKnowledgeBaseErrors, GetKnowledgeBaseHealthData, GetKnowledgeBaseHealthErrors, GetKnowledgeBaseHealthResponses, GetKnowledgeBaseResponses, GetKnowledgeBasesData, GetKnowledgeBasesErrors, GetKnowledgeBasesResponses, GetKnowledgeDirectoriesData, GetKnowledgeDirectoriesErrors, GetKnowledgeDirectoriesResponses, GetKnowledgeFileContentData, GetKnowledgeFileContentResponses, GetKnowledgeFilesData, GetKnowledgeFilesErrors, GetKnowledgeFilesResponses, GetLabelKeysData, GetLabelKeysErrors, GetLabelKeysResponses, GetLabelValuesData, GetLabelValuesErrors, GetLabelValuesResponses, GetLimitData, GetLimitErrors, GetLimitResponses, GetLimitsData, GetLimitsErrors, GetLimitsResponses, GetLlmModelsData, GetLlmModelsErrors, GetLlmModelsResponses, GetLlmOauthClientsData, GetLlmOauthClientsErrors, GetLlmOauthClientsResponses, GetLlmProviderApiKeyData, GetLlmProviderApiKeyErrors, GetLlmProviderApiKeyResponses, GetLlmProviderApiKeysData, GetLlmProviderApiKeysErrors, GetLlmProviderApiKeysResponses, GetLlmProxyData, GetLlmProxyErrors, GetLlmProxyResponses, GetMcpOauthClientsData, GetMcpOauthClientsErrors, GetMcpOauthClientsResponses, GetMcpServerAutoModeAgentsData, GetMcpServerAutoModeAgentsErrors, GetMcpServerAutoModeAgentsResponses, GetMcpServerData, GetMcpServerErrors, GetMcpServerInstallationStatusData, GetMcpServerInstallationStatusErrors, GetMcpServerInstallationStatusResponses, GetMcpServerResponses, GetMcpServersData, GetMcpServersErrors, GetMcpServersResponses, GetMcpServerToolsData, GetMcpServerToolsErrors, GetMcpServerToolsResponses, GetMcpToolCallData, GetMcpToolCallErrors, GetMcpToolCallResponses, GetMcpToolCallsData, GetMcpToolCallsErrors, GetMcpToolCallsResponses, GetMemberDefaultAgentData, GetMemberDefaultAgentErrors, GetMemberDefaultAgentResponses, GetMemberDefaultModelData, GetMemberDefaultModelErrors, GetMemberDefaultModelResponses, GetMembersData, GetMembersErrors, GetMemberSignupStatusData, GetMemberSignupStatusErrors, GetMemberSignupStatusResponses, GetMembersResponses, GetMfilesVafAddOnDistributionData, GetMfilesVafAddOnDistributionErrors, GetMfilesVafAddOnDistributionResponses, GetMfilesVafAddOnPackageData, GetMfilesVafAddOnPackageResponses, GetMfilesVafAddOnScriptData, GetMfilesVafAddOnScriptResponses, GetModelStatisticsData, GetModelStatisticsErrors, GetModelStatisticsResponses, GetModelsWithApiKeysData, GetModelsWithApiKeysErrors, GetModelsWithApiKeysResponses, GetMyStatisticsData, GetMyStatisticsErrors, GetMyStatisticsResponses, GetMyUsageBreakdownData, GetMyUsageBreakdownErrors, GetMyUsageBreakdownResponses, GetNgrokConfigData, GetNgrokConfigErrors, GetNgrokConfigResponses, GetOAuthClientInfoData, GetOAuthClientInfoResponses, GetOnboardingSeenNavItemsData, GetOnboardingSeenNavItemsErrors, GetOnboardingSeenNavItemsResponses, GetOnboardingStatusData, GetOnboardingStatusErrors, GetOnboardingStatusResponses, GetOnboardingSurveyEligibilityData, GetOnboardingSurveyEligibilityErrors, GetOnboardingSurveyEligibilityResponses, GetOperatorsData, GetOperatorsErrors, GetOperatorsResponses, GetOrganizationData, GetOrganizationErrors, GetOrganizationMemberData, GetOrganizationMemberErrors, GetOrganizationMemberResponses, GetOrganizationMembersData, GetOrganizationMembersErrors, GetOrganizationMembersResponses, GetOrganizationResponses, GetOverviewStatisticsData, GetOverviewStatisticsErrors, GetOverviewStatisticsResponses, GetPermissionSyncCoverageData, GetPermissionSyncCoverageErrors, GetPermissionSyncCoverageResponses, GetPluginData, GetPluginErrors, GetPluginResponses, GetPluginsData, GetPluginsErrors, GetPluginSkillData, GetPluginSkillErrors, GetPluginSkillResponses, GetPluginSkillsData, GetPluginSkillsErrors, GetPluginSkillsResponses, GetPluginSkillUsageStatisticsData, GetPluginSkillUsageStatisticsErrors, GetPluginSkillUsageStatisticsResponses, GetPluginsResponses, GetProjectConversationsData, GetProjectConversationsErrors, GetProjectConversationsResponses, GetProjectData, GetProjectErrors, GetProjectFilesData, GetProjectFilesErrors, GetProjectFilesResponses, GetProjectInstructionsData, GetProjectInstructionsErrors, GetProjectInstructionsResponses, GetProjectResponses, GetProjectsData, GetProjectsErrors, GetProjectsResponses, GetPublicConfigData, GetPublicConfigResponses, GetPublicIdentityProvidersData, GetPublicIdentityProvidersErrors, GetPublicIdentityProvidersResponses, GetReadyData, GetReadyErrors, GetReadyResponses, GetRoleData, GetRoleErrors, GetRoleResponses, GetRolesData, GetRolesErrors, GetRolesResponses, GetScheduleTriggerData, GetScheduleTriggerErrors, GetScheduleTriggerResponses, GetScheduleTriggerRunData, GetScheduleTriggerRunErrors, GetScheduleTriggerRunResponses, GetScheduleTriggerRunsData, GetScheduleTriggerRunsErrors, GetScheduleTriggerRunsResponses, GetScheduleTriggersData, GetScheduleTriggersErrors, GetScheduleTriggersResponses, GetSecretData, GetSecretErrors, GetSecretResponses, GetSecretsTypeData, GetSecretsTypeErrors, GetSecretsTypeResponses, GetServiceAccountData, GetServiceAccountErrors, GetServiceAccountResponses, GetServiceAccountsData, GetServiceAccountsErrors, GetServiceAccountsResponses, GetSharedConversationData, GetSharedConversationErrors, GetSharedConversationResponses, GetSiteNotificationData, GetSiteNotificationErrors, GetSiteNotificationResponses, GetSiteNotificationSettingsData, GetSiteNotificationSettingsErrors, GetSiteNotificationSettingsResponses, GetSkillData, GetSkillErrors, GetSkillMarketplaceData, GetSkillMarketplaceErrors, GetSkillMarketplaceResponses, GetSkillResponses, GetSkillSandboxArtifactData, GetSkillSandboxArtifactResponses, GetSkillSandboxConversationArtifactsData, GetSkillSandboxConversationArtifactsErrors, GetSkillSandboxConversationArtifactsResponses, GetSkillsData, GetSkillsErrors, GetSkillShareLinksData, GetSkillShareLinksErrors, GetSkillShareLinksResponses, GetSkillSourceReposData, GetSkillSourceReposErrors, GetSkillSourceReposResponses, GetSkillsResponses, GetSkillStatisticsData, GetSkillStatisticsErrors, GetSkillStatisticsResponses, GetSkillUsageStatisticsData, GetSkillUsageStatisticsErrors, GetSkillUsageStatisticsResponses, GetSkillVersionData, GetSkillVersionErrors, GetSkillVersionResponses, GetSkillVersionsData, GetSkillVersionsErrors, GetSkillVersionsResponses, GetTeamData, GetTeamErrors, GetTeamExternalGroupsData, GetTeamExternalGroupsErrors, GetTeamExternalGroupsResponses, GetTeamLabelKeysData, GetTeamLabelKeysErrors, GetTeamLabelKeysResponses, GetTeamLabelValuesData, GetTeamLabelValuesErrors, GetTeamLabelValuesResponses, GetTeamMembersData, GetTeamMembersErrors, GetTeamMembersResponses, GetTeamResponses, GetTeamsData, GetTeamsErrors, GetTeamsResponses, GetTeamStatisticsData, GetTeamStatisticsErrors, GetTeamStatisticsResponses, GetTeamVaultFolderData, GetTeamVaultFolderErrors, GetTeamVaultFolderResponses, GetTeamVaultSecretKeysData, GetTeamVaultSecretKeysErrors, GetTeamVaultSecretKeysResponses, GetTokensData, GetTokensErrors, GetTokensResponses, GetTokenValueData, GetTokenValueErrors, GetTokenValueResponses, GetToolData, GetToolErrors, GetToolInvocationPoliciesData, GetToolInvocationPoliciesErrors, GetToolInvocationPoliciesResponses, GetToolInvocationPolicyData, GetToolInvocationPolicyErrors, GetToolInvocationPolicyResponses, GetToolObserversData, GetToolObserversErrors, GetToolObserversResponses, GetToolResponses, GetToolsData, GetToolsErrors, GetToolsResponses, GetToolsWithAssignmentsData, GetToolsWithAssignmentsErrors, GetToolsWithAssignmentsResponses, GetTrustedDataPoliciesData, GetTrustedDataPoliciesErrors, GetTrustedDataPoliciesResponses, GetTrustedDataPolicyData, GetTrustedDataPolicyErrors, GetTrustedDataPolicyResponses, GetUniqueExternalAgentIdsData, GetUniqueExternalAgentIdsErrors, GetUniqueExternalAgentIdsResponses, GetUniqueUserIdsData, GetUniqueUserIdsErrors, GetUniqueUserIdsResponses, GetUserPermissionsData, GetUserPermissionsErrors, GetUserPermissionsResponses, GetUserStatisticsData, GetUserStatisticsErrors, GetUserStatisticsResponses, GetUserTokenData, GetUserTokenErrors, GetUserTokenResponses, GetUserTokenValueData, GetUserTokenValueErrors, GetUserTokenValueResponses, GetV1A2aByAgentIdWellKnownAgentJsonData, GetV1A2aByAgentIdWellKnownAgentJsonResponses, GetV2A2aAgentsData, GetV2A2aAgentsResponses, GetV2A2aByAgentIdWellKnownAgentCardJsonData, GetV2A2aByAgentIdWellKnownAgentCardJsonResponses, GetVirtualApiKeyData, GetVirtualApiKeyErrors, GetVirtualApiKeyResponses, GetVirtualApiKeyValueData, GetVirtualApiKeyValueErrors, GetVirtualApiKeyValueResponses, GetWellKnownOauthAuthorizationServerData, GetWellKnownOauthAuthorizationServerResponses, GetWellKnownOauthProtectedResourceBy__Data, GetWellKnownOauthProtectedResourceBy__Responses, GithubCopilotChatCompletionsWithAgentData, GithubCopilotChatCompletionsWithAgentErrors, GithubCopilotChatCompletionsWithAgentResponses, GithubCopilotChatCompletionsWithDefaultAgentData, GithubCopilotChatCompletionsWithDefaultAgentErrors, GithubCopilotChatCompletionsWithDefaultAgentResponses, GithubCopilotDeviceAuthPollData, GithubCopilotDeviceAuthPollErrors, GithubCopilotDeviceAuthPollResponses, GithubCopilotDeviceAuthStartData, GithubCopilotDeviceAuthStartErrors, GithubCopilotDeviceAuthStartResponses, GithubCopilotListModelsWithAgentData, GithubCopilotListModelsWithAgentErrors, GithubCopilotListModelsWithAgentResponses, GithubCopilotListModelsWithDefaultAgentData, GithubCopilotListModelsWithDefaultAgentErrors, GithubCopilotListModelsWithDefaultAgentResponses, GithubCopilotResponsesWithAgentData, GithubCopilotResponsesWithAgentErrors, GithubCopilotResponsesWithAgentResponses, GithubCopilotResponsesWithDefaultAgentData, GithubCopilotResponsesWithDefaultAgentErrors, GithubCopilotResponsesWithDefaultAgentResponses, GroqChatCompletionsWithAgentData, GroqChatCompletionsWithAgentErrors, GroqChatCompletionsWithAgentResponses, GroqChatCompletionsWithDefaultAgentData, GroqChatCompletionsWithDefaultAgentErrors, GroqChatCompletionsWithDefaultAgentResponses, HandleOAuthCallbackData, HandleOAuthCallbackErrors, HandleOAuthCallbackResponses, HardResetMcpServerData, HardResetMcpServerErrors, HardResetMcpServerResponses, ImportAgentData, ImportAgentErrors, ImportAgentResponses, ImportGithubPluginData, ImportGithubPluginErrors, ImportGithubPluginMarketplaceData, ImportGithubPluginMarketplaceErrors, ImportGithubPluginMarketplaceResponses, ImportGithubPluginResponses, ImportGithubSkillsData, ImportGithubSkillsErrors, ImportGithubSkillsResponses, IndexKnowledgeFilesData, IndexKnowledgeFilesErrors, IndexKnowledgeFilesResponses, IngestRumEventsData, IngestRumEventsErrors, IngestRumEventsResponses, InitiateOAuthData, InitiateOAuthErrors, InitiateOAuthResponses, InspectMcpServerData, InspectMcpServerErrors, InspectMcpServerResponses, InstallMcpServerData, InstallMcpServerErrors, InstallMcpServerResponses, KimiChatCompletionsWithAgentData, KimiChatCompletionsWithAgentErrors, KimiChatCompletionsWithAgentResponses, KimiChatCompletionsWithDefaultAgentData, KimiChatCompletionsWithDefaultAgentErrors, KimiChatCompletionsWithDefaultAgentResponses, LinkTelegramChatOpsAccountData, LinkTelegramChatOpsAccountErrors, LinkTelegramChatOpsAccountResponses, ListChatOpsBindingsData, ListChatOpsBindingsErrors, ListChatOpsBindingsResponses, ListDefaultUserLimitsData, ListDefaultUserLimitsErrors, ListDefaultUserLimitsResponses, ListEnvironmentsData, ListEnvironmentsErrors, ListEnvironmentsResponses, ListGithubAppConfigsData, ListGithubAppConfigsErrors, ListGithubAppConfigsResponses, ListGithubPatsData, ListGithubPatsErrors, ListGithubPatsResponses, ListPendingImageApprovalCatalogItemsData, ListPendingImageApprovalCatalogItemsErrors, ListPendingImageApprovalCatalogItemsResponses, ListTeamVaultFolderSecretsData, ListTeamVaultFolderSecretsErrors, ListTeamVaultFolderSecretsResponses, LockAppData, LockAppErrors, LockAppResponses, MarkChatConversationReadData, MarkChatConversationReadErrors, MarkChatConversationReadResponses, MarkOnboardingNavItemsSeenData, MarkOnboardingNavItemsSeenErrors, MarkOnboardingNavItemsSeenResponses, McpAppProxyPostData, McpAppProxyPostResponses, McpGatewayGetData, McpGatewayGetErrors, McpGatewayGetResponses, McpGatewayPostData, McpGatewayPostResponses, McpProxyPostData, McpProxyPostResponses, McpServerProxyPostData, McpServerProxyPostResponses, Microsoft365CopilotChatCompletionsWithAgentData, Microsoft365CopilotChatCompletionsWithAgentErrors, Microsoft365CopilotChatCompletionsWithAgentResponses, Microsoft365CopilotChatCompletionsWithDefaultAgentData, Microsoft365CopilotChatCompletionsWithDefaultAgentErrors, Microsoft365CopilotChatCompletionsWithDefaultAgentResponses, Microsoft365CopilotDeviceAuthPollData, Microsoft365CopilotDeviceAuthPollErrors, Microsoft365CopilotDeviceAuthPollResponses, Microsoft365CopilotDeviceAuthStartData, Microsoft365CopilotDeviceAuthStartErrors, Microsoft365CopilotDeviceAuthStartResponses, Microsoft365CopilotListModelsWithAgentData, Microsoft365CopilotListModelsWithAgentErrors, Microsoft365CopilotListModelsWithAgentResponses, Microsoft365CopilotListModelsWithDefaultAgentData, Microsoft365CopilotListModelsWithDefaultAgentErrors, Microsoft365CopilotListModelsWithDefaultAgentResponses, MinimaxChatCompletionsWithAgentData, MinimaxChatCompletionsWithAgentErrors, MinimaxChatCompletionsWithAgentResponses, MinimaxChatCompletionsWithDefaultAgentData, MinimaxChatCompletionsWithDefaultAgentErrors, MinimaxChatCompletionsWithDefaultAgentResponses, MistralChatCompletionsWithAgentData, MistralChatCompletionsWithAgentErrors, MistralChatCompletionsWithAgentResponses, MistralChatCompletionsWithDefaultAgentData, MistralChatCompletionsWithDefaultAgentErrors, MistralChatCompletionsWithDefaultAgentResponses, MistralEmbeddingsWithAgentData, MistralEmbeddingsWithAgentErrors, MistralEmbeddingsWithAgentResponses, MistralEmbeddingsWithDefaultAgentData, MistralEmbeddingsWithDefaultAgentErrors, MistralEmbeddingsWithDefaultAgentResponses, ModelRouterChatCompletionsWithAgentData, ModelRouterChatCompletionsWithAgentErrors, ModelRouterChatCompletionsWithAgentResponses, ModelRouterChatCompletionsWithDefaultAgentData, ModelRouterChatCompletionsWithDefaultAgentErrors, ModelRouterChatCompletionsWithDefaultAgentResponses, ModelRouterEmbeddingsWithAgentData, ModelRouterEmbeddingsWithAgentErrors, ModelRouterEmbeddingsWithAgentResponses, ModelRouterEmbeddingsWithDefaultAgentData, ModelRouterEmbeddingsWithDefaultAgentErrors, ModelRouterEmbeddingsWithDefaultAgentResponses, ModelRouterListModelsWithAgentData, ModelRouterListModelsWithAgentErrors, ModelRouterListModelsWithAgentResponses, ModelRouterListModelsWithDefaultAgentData, ModelRouterListModelsWithDefaultAgentErrors, ModelRouterListModelsWithDefaultAgentResponses, ModelRouterResponsesWithAgentData, ModelRouterResponsesWithAgentErrors, ModelRouterResponsesWithAgentResponses, ModelRouterResponsesWithDefaultAgentData, ModelRouterResponsesWithDefaultAgentErrors, ModelRouterResponsesWithDefaultAgentResponses, MuteMcpCatalogAlertData, MuteMcpCatalogAlertErrors, MuteMcpCatalogAlertResponses, MuteMcpServerAlertData, MuteMcpServerAlertErrors, MuteMcpServerAlertResponses, OllamaChatCompletionsWithAgentData, OllamaChatCompletionsWithAgentErrors, OllamaChatCompletionsWithAgentResponses, OllamaChatCompletionsWithDefaultAgentData, OllamaChatCompletionsWithDefaultAgentErrors, OllamaChatCompletionsWithDefaultAgentResponses, OllamaEmbeddingsWithAgentData, OllamaEmbeddingsWithAgentErrors, OllamaEmbeddingsWithAgentResponses, OllamaEmbeddingsWithDefaultAgentData, OllamaEmbeddingsWithDefaultAgentErrors, OllamaEmbeddingsWithDefaultAgentResponses, OllamaNativeChatWithAgentData, OllamaNativeChatWithAgentErrors, OllamaNativeChatWithAgentResponses, OllamaNativeChatWithDefaultAgentData, OllamaNativeChatWithDefaultAgentErrors, OllamaNativeChatWithDefaultAgentResponses, OpenAiChatCompletionsWithAgentData, OpenAiChatCompletionsWithAgentErrors, OpenAiChatCompletionsWithAgentResponses, OpenAiChatCompletionsWithDefaultAgentData, OpenAiChatCompletionsWithDefaultAgentErrors, OpenAiChatCompletionsWithDefaultAgentResponses, OpenaiCodexDeviceAuthPollData, OpenaiCodexDeviceAuthPollErrors, OpenaiCodexDeviceAuthPollResponses, OpenaiCodexDeviceAuthStartData, OpenaiCodexDeviceAuthStartErrors, OpenaiCodexDeviceAuthStartResponses, OpenAiEmbeddingsWithAgentData, OpenAiEmbeddingsWithAgentErrors, OpenAiEmbeddingsWithAgentResponses, OpenAiEmbeddingsWithDefaultAgentData, OpenAiEmbeddingsWithDefaultAgentErrors, OpenAiEmbeddingsWithDefaultAgentResponses, OpenAiListModelsWithAgentData, OpenAiListModelsWithAgentErrors, OpenAiListModelsWithAgentResponses, OpenAiListModelsWithDefaultAgentData, OpenAiListModelsWithDefaultAgentErrors, OpenAiListModelsWithDefaultAgentResponses, OpenAiResponsesWithAgentData, OpenAiResponsesWithAgentErrors, OpenAiResponsesWithAgentResponses, OpenAiResponsesWithDefaultAgentData, OpenAiResponsesWithDefaultAgentErrors, OpenAiResponsesWithDefaultAgentResponses, OpenAppInChatData, OpenAppInChatErrors, OpenAppInChatResponses, OpenExternalAppInChatData, OpenExternalAppInChatErrors, OpenExternalAppInChatResponses, OpenrouterChatCompletionsWithAgentData, OpenrouterChatCompletionsWithAgentErrors, OpenrouterChatCompletionsWithAgentResponses, OpenrouterChatCompletionsWithDefaultAgentData, OpenrouterChatCompletionsWithDefaultAgentErrors, OpenrouterChatCompletionsWithDefaultAgentResponses, PermanentlyDeleteAgentData, PermanentlyDeleteAgentErrors, PermanentlyDeleteAgentResponses, PermanentlyDeleteConnectorData, PermanentlyDeleteConnectorErrors, PermanentlyDeleteConnectorResponses, PermanentlyDeleteKnowledgeBaseData, PermanentlyDeleteKnowledgeBaseErrors, PermanentlyDeleteKnowledgeBaseResponses, PermanentlyDeleteProjectData, PermanentlyDeleteProjectErrors, PermanentlyDeleteProjectResponses, PermanentlyDeleteSkillData, PermanentlyDeleteSkillErrors, PermanentlyDeleteSkillResponses, PerplexityChatCompletionsWithAgentData, PerplexityChatCompletionsWithAgentErrors, PerplexityChatCompletionsWithAgentResponses, PerplexityChatCompletionsWithDefaultAgentData, PerplexityChatCompletionsWithDefaultAgentErrors, PerplexityChatCompletionsWithDefaultAgentResponses, PerplexityResponsesWithAgentData, PerplexityResponsesWithAgentErrors, PerplexityResponsesWithAgentResponses, PerplexityResponsesWithDefaultAgentData, PerplexityResponsesWithDefaultAgentErrors, PerplexityResponsesWithDefaultAgentResponses, PinAppData, PinAppErrors, PinAppResponses, PinExternalAppData, PinExternalAppErrors, PinExternalAppResponses, PinProjectData, PinProjectErrors, PinProjectResponses, PostApiAuthBy__Data, PostApiAuthBy__Responses, PostApiAuthOauth2RegisterData, PostApiAuthOauth2RegisterResponses, PostApiAuthOauth2RevokeData, PostApiAuthOauth2RevokeResponses, PostApiAuthOauth2TokenData, PostApiAuthOauth2TokenResponses, PostApiAuthOrganizationRemoveMemberData, PostApiAuthOrganizationRemoveMemberResponses, PostApiAuthSignInSsoData, PostApiAuthSignInSsoResponses, PostApiWebhooksChatopsMsTeamsData, PostApiWebhooksChatopsMsTeamsErrors, PostApiWebhooksChatopsMsTeamsResponses, PostApiWebhooksChatopsSlackData, PostApiWebhooksChatopsSlackErrors, PostApiWebhooksChatopsSlackInteractiveData, PostApiWebhooksChatopsSlackInteractiveErrors, PostApiWebhooksChatopsSlackInteractiveResponses, PostApiWebhooksChatopsSlackResponses, PostApiWebhooksChatopsSlackSlashCommandData, PostApiWebhooksChatopsSlackSlashCommandErrors, PostApiWebhooksChatopsSlackSlashCommandResponses, PostApiWebhooksIncomingEmailData, PostApiWebhooksIncomingEmailErrors, PostApiWebhooksIncomingEmailResponses, PostAppRenderDiagnosticsData, PostAppRenderDiagnosticsErrors, PostAppRenderDiagnosticsResponses, PostAppRenderScreenshotData, PostAppRenderScreenshotErrors, PostAppRenderScreenshotResponses, PostV1A2aByAgentIdData, PostV1A2aByAgentIdResponses, PostV1GeminiByAgentIdV1BetaModelsByModelGenerateContentData, PostV1GeminiByAgentIdV1BetaModelsByModelGenerateContentErrors, PostV1GeminiByAgentIdV1BetaModelsByModelGenerateContentResponses, PostV1GeminiByAgentIdV1BetaModelsByModelStreamGenerateContentData, PostV1GeminiByAgentIdV1BetaModelsByModelStreamGenerateContentErrors, PostV1GeminiV1BetaModelsByModelGenerateContentData, PostV1GeminiV1BetaModelsByModelGenerateContentErrors, PostV1GeminiV1BetaModelsByModelGenerateContentResponses, PostV1GeminiV1BetaModelsByModelStreamGenerateContentData, PostV1GeminiV1BetaModelsByModelStreamGenerateContentErrors, PostV2A2aByAgentIdData, PostV2A2aByAgentIdResponses, PreviewGithubPluginData, PreviewGithubPluginErrors, PreviewGithubPluginResponses, PreviewGithubPluginUpdateData, PreviewGithubPluginUpdateErrors, PreviewGithubPluginUpdateResponses, PreviewGithubSkillData, PreviewGithubSkillErrors, PreviewGithubSkillResponses, PromoteAttachmentToKnowledgeFileData, PromoteAttachmentToKnowledgeFileErrors, PromoteAttachmentToKnowledgeFileResponses, ReauthenticateMcpServerData, ReauthenticateMcpServerErrors, ReauthenticateMcpServerResponses, ReconnectLlmProviderApiKeyData, ReconnectLlmProviderApiKeyErrors, ReconnectLlmProviderApiKeyResponses, RefreshChatOpsChannelDiscoveryData, RefreshChatOpsChannelDiscoveryErrors, RefreshChatOpsChannelDiscoveryResponses, RefreshInternalMcpCatalogImageData, RefreshInternalMcpCatalogImageErrors, RefreshInternalMcpCatalogImageResponses, ReinstallInternalMcpCatalogItemData, ReinstallInternalMcpCatalogItemErrors, ReinstallInternalMcpCatalogItemResponses, ReinstallMcpServerData, ReinstallMcpServerErrors, ReinstallMcpServerResponses, ReloadMcpServerToolsData, ReloadMcpServerToolsErrors, ReloadMcpServerToolsResponses, RemoveTeamExternalGroupData, RemoveTeamExternalGroupErrors, RemoveTeamExternalGroupResponses, RemoveTeamMemberData, RemoveTeamMemberErrors, RemoveTeamMemberResponses, RenderAppRecordingVideoData, RenderAppRecordingVideoErrors, RenderAppRecordingVideoResponses, RenewIncomingEmailSubscriptionData, RenewIncomingEmailSubscriptionErrors, RenewIncomingEmailSubscriptionResponses, ResetDeploymentYamlData, ResetDeploymentYamlErrors, ResetDeploymentYamlResponses, ResetSkillData, ResetSkillErrors, ResetSkillResponses, ResolveChatMcpElicitationData, ResolveChatMcpElicitationErrors, ResolveChatMcpElicitationResponses, RestoreAgentData, RestoreAgentErrors, RestoreAgentResponses, RestoreAgentVersionData, RestoreAgentVersionErrors, RestoreAgentVersionResponses, RestoreChatConversationData, RestoreChatConversationErrors, RestoreChatConversationResponses, RestoreConnectorData, RestoreConnectorErrors, RestoreConnectorResponses, RestoreInternalMcpCatalogItemData, RestoreInternalMcpCatalogItemErrors, RestoreInternalMcpCatalogItemResponses, RestoreKnowledgeBaseData, RestoreKnowledgeBaseErrors, RestoreKnowledgeBaseResponses, RestoreMcpServerData, RestoreMcpServerErrors, RestoreMcpServerResponses, RestoreProjectData, RestoreProjectErrors, RestoreProjectResponses, RestoreSkillData, RestoreSkillErrors, RestoreSkillResponses, ReviewAppRecordingData, ReviewAppRecordingErrors, ReviewAppRecordingResponses, RevokeSkillShareLinkData, RevokeSkillShareLinkErrors, RevokeSkillShareLinkResponses, RotateLlmOauthClientSecretData, RotateLlmOauthClientSecretErrors, RotateLlmOauthClientSecretResponses, RotateMcpOauthClientSecretData, RotateMcpOauthClientSecretErrors, RotateMcpOauthClientSecretResponses, RotateSkillShareLinkData, RotateSkillShareLinkErrors, RotateSkillShareLinkResponses, RotateTokenData, RotateTokenErrors, RotateTokenResponses, RotateUserTokenData, RotateUserTokenErrors, RotateUserTokenResponses, RunScheduleTriggerNowData, RunScheduleTriggerNowErrors, RunScheduleTriggerNowResponses, SearchSkillCatalogData, SearchSkillCatalogErrors, SearchSkillCatalogResponses, SetChatMessageFeedbackData, SetChatMessageFeedbackErrors, SetChatMessageFeedbackResponses, SetConversationHooksDebugData, SetConversationHooksDebugErrors, SetConversationHooksDebugResponses, SetProjectInstructionsData, SetProjectInstructionsErrors, SetProjectInstructionsResponses, SetProjectShareData, SetProjectShareErrors, SetProjectShareResponses, SetTeamVaultFolderData, SetTeamVaultFolderErrors, SetTeamVaultFolderResponses, SetupIncomingEmailWebhookData, SetupIncomingEmailWebhookErrors, SetupIncomingEmailWebhookResponses, ShareConversationData, ShareConversationErrors, ShareConversationResponses, StartGoogleDriveConnectorOAuthData, StartGoogleDriveConnectorOAuthErrors, StartGoogleDriveConnectorOAuthResponses, StopChatStreamData, StopChatStreamErrors, StopChatStreamResponses, StreamChatData, StreamChatErrors, SubmitOAuthConsentData, SubmitOAuthConsentErrors, SubmitOAuthConsentResponses, SubmitOnboardingSurveyData, SubmitOnboardingSurveyErrors, SubmitOnboardingSurveyResponses, SuggestSkillDescriptionData, SuggestSkillDescriptionErrors, SuggestSkillDescriptionResponses, SyncAgentDelegationsData, SyncAgentDelegationsErrors, SyncAgentDelegationsResponses, SyncConnectorData, SyncConnectorErrors, SyncConnectorResponses, SyncLlmModelsData, SyncLlmModelsErrors, SyncLlmModelsResponses, TestConnectorConnectionData, TestConnectorConnectionErrors, TestConnectorConnectionResponses, TestEmbeddingConnectionData, TestEmbeddingConnectionErrors, TestEmbeddingConnectionResponses, TestOcrConnectionData, TestOcrConnectionErrors, TestOcrConnectionResponses, TestRerankerConnectionData, TestRerankerConnectionErrors, TestRerankerConnectionResponses, TriggerPermissionSyncData, TriggerPermissionSyncErrors, TriggerPermissionSyncResponses, TriggerPluginGithubSyncData, TriggerPluginGithubSyncErrors, TriggerPluginGithubSyncResponses, UnassignConnectorFromKnowledgeBaseData, UnassignConnectorFromKnowledgeBaseErrors, UnassignConnectorFromKnowledgeBaseResponses, UnassignToolFromAgentData, UnassignToolFromAgentErrors, UnassignToolFromAgentResponses, UnassignToolFromAppData, UnassignToolFromAppErrors, UnassignToolFromAppResponses, UnlockAppData, UnlockAppErrors, UnlockAppResponses, UnmuteMcpCatalogAlertData, UnmuteMcpCatalogAlertErrors, UnmuteMcpCatalogAlertResponses, UnmuteMcpServerAlertData, UnmuteMcpServerAlertErrors, UnmuteMcpServerAlertResponses, UnpinAppData, UnpinAppErrors, UnpinAppResponses, UnpinExternalAppData, UnpinExternalAppErrors, UnpinExternalAppResponses, UnpinProjectData, UnpinProjectErrors, UnpinProjectResponses, UnshareConversationData, UnshareConversationErrors, UnshareConversationResponses, UpdateAgentData, UpdateAgentErrors, UpdateAgentKnowledgeSourceExclusionsData, UpdateAgentKnowledgeSourceExclusionsErrors, UpdateAgentKnowledgeSourceExclusionsResponses, UpdateAgentResponses, UpdateAgentSettingsData, UpdateAgentSettingsErrors, UpdateAgentSettingsResponses, UpdateAgentSkillExclusionsData, UpdateAgentSkillExclusionsErrors, UpdateAgentSkillExclusionsResponses, UpdateAgentSkillsData, UpdateAgentSkillsErrors, UpdateAgentSkillsResponses, UpdateAgentSubagentExclusionsData, UpdateAgentSubagentExclusionsErrors, UpdateAgentSubagentExclusionsResponses, UpdateAgentToolData, UpdateAgentToolErrors, UpdateAgentToolExclusionsData, UpdateAgentToolExclusionsErrors, UpdateAgentToolExclusionsResponses, UpdateAgentToolResponses, UpdateAppData, UpdateAppearanceSettingsData, UpdateAppearanceSettingsErrors, UpdateAppearanceSettingsResponses, UpdateAppErrors, UpdateAppResponses, UpdateAuthSettingsData, UpdateAuthSettingsErrors, UpdateAuthSettingsResponses, UpdateChatConversationData, UpdateChatConversationErrors, UpdateChatConversationResponses, UpdateChatMessageData, UpdateChatMessageErrors, UpdateChatMessageResponses, UpdateChatOpsBindingData, UpdateChatOpsBindingErrors, UpdateChatOpsBindingResponses, UpdateChatOpsConfigInQuickstartData, UpdateChatOpsConfigInQuickstartErrors, UpdateChatOpsConfigInQuickstartResponses, UpdateConnectionSettingsData, UpdateConnectionSettingsErrors, UpdateConnectionSettingsResponses, UpdateConnectorData, UpdateConnectorErrors, UpdateConnectorResponses, UpdateConversationEnabledToolsData, UpdateConversationEnabledToolsErrors, UpdateConversationEnabledToolsResponses, UpdateDefaultEnvironmentData, UpdateDefaultEnvironmentErrors, UpdateDefaultEnvironmentResponses, UpdateDefaultUserLimitData, UpdateDefaultUserLimitErrors, UpdateDefaultUserLimitResponses, UpdateEnvironmentData, UpdateEnvironmentErrors, UpdateEnvironmentResourceDefaultsData, UpdateEnvironmentResourceDefaultsErrors, UpdateEnvironmentResourceDefaultsResponses, UpdateEnvironmentResponses, UpdateGithubAppConfigData, UpdateGithubAppConfigErrors, UpdateGithubAppConfigResponses, UpdateGithubPatData, UpdateGithubPatErrors, UpdateGithubPatResponses, UpdateHookData, UpdateHookErrors, UpdateHookResponses, UpdateIdentityProviderData, UpdateIdentityProviderErrors, UpdateIdentityProviderResponses, UpdateIntegrationSettingsData, UpdateIntegrationSettingsErrors, UpdateIntegrationSettingsResponses, UpdateInternalMcpCatalogItemData, UpdateInternalMcpCatalogItemErrors, UpdateInternalMcpCatalogItemResponses, UpdateKnowledgeBaseData, UpdateKnowledgeBaseErrors, UpdateKnowledgeBaseResponses, UpdateKnowledgeDirectoryData, UpdateKnowledgeDirectoryErrors, UpdateKnowledgeDirectoryResponses, UpdateKnowledgeFileData, UpdateKnowledgeFileErrors, UpdateKnowledgeFileResponses, UpdateKnowledgeSettingsData, UpdateKnowledgeSettingsErrors, UpdateKnowledgeSettingsResponses, UpdateLimitData, UpdateLimitErrors, UpdateLimitResponses, UpdateLlmOauthClientData, UpdateLlmOauthClientErrors, UpdateLlmOauthClientResponses, UpdateLlmProviderApiKeyData, UpdateLlmProviderApiKeyErrors, UpdateLlmProviderApiKeyResponses, UpdateLlmProxyData, UpdateLlmProxyErrors, UpdateLlmProxyResponses, UpdateLlmSettingsData, UpdateLlmSettingsErrors, UpdateLlmSettingsResponses, UpdateMcpOauthClientData, UpdateMcpOauthClientErrors, UpdateMcpOauthClientResponses, UpdateMcpSettingsData, UpdateMcpSettingsErrors, UpdateMcpSettingsResponses, UpdateMemberDefaultAgentData, UpdateMemberDefaultAgentErrors, UpdateMemberDefaultAgentResponses, UpdateMemberDefaultModelData, UpdateMemberDefaultModelErrors, UpdateMemberDefaultModelResponses, UpdateModelData, UpdateModelErrors, UpdateModelResponses, UpdatePluginData, UpdatePluginErrors, UpdatePluginGithubSyncData, UpdatePluginGithubSyncErrors, UpdatePluginGithubSyncResponses, UpdatePluginResponses, UpdateProjectData, UpdateProjectErrors, UpdateProjectResponses, UpdateRoleData, UpdateRoleErrors, UpdateRoleResponses, UpdateScheduleTriggerData, UpdateScheduleTriggerErrors, UpdateScheduleTriggerResponses, UpdateSecuritySettingsData, UpdateSecuritySettingsErrors, UpdateSecuritySettingsResponses, UpdateServiceAccountData, UpdateServiceAccountErrors, UpdateServiceAccountResponses, UpdateServiceAccountTokenData, UpdateServiceAccountTokenErrors, UpdateServiceAccountTokenResponses, UpdateSiteNotificationData, UpdateSiteNotificationErrors, UpdateSiteNotificationResponses, UpdateSkillData, UpdateSkillErrors, UpdateSkillGithubSyncData, UpdateSkillGithubSyncErrors, UpdateSkillGithubSyncResponses, UpdateSkillResponses, UpdateSkillSandboxArtifactContentData, UpdateSkillSandboxArtifactContentErrors, UpdateSkillSandboxArtifactContentResponses, UpdateSkillsSettingsData, UpdateSkillsSettingsErrors, UpdateSkillsSettingsResponses, UpdateSlackChatOpsConfigData, UpdateSlackChatOpsConfigErrors, UpdateSlackChatOpsConfigResponses, UpdateTeamData, UpdateTeamErrors, UpdateTeamMemberData, UpdateTeamMemberErrors, UpdateTeamMemberResponses, UpdateTeamResponses, UpdateTelegramChatOpsConfigData, UpdateTelegramChatOpsConfigErrors, UpdateTelegramChatOpsConfigResponses, UpdateToolInvocationPolicyData, UpdateToolInvocationPolicyErrors, UpdateToolInvocationPolicyResponses, UpdateTrustedDataPolicyData, UpdateTrustedDataPolicyErrors, UpdateTrustedDataPolicyResponses, UpdateVirtualApiKeyData, UpdateVirtualApiKeyErrors, UpdateVirtualApiKeyResponses, UploadKnowledgeFileData, UploadKnowledgeFileErrors, UploadKnowledgeFileResponses, UploadProjectFilesData, UploadProjectFilesErrors, UploadProjectFilesResponses, UpsertConnectorMemberOverrideData, UpsertConnectorMemberOverrideErrors, UpsertConnectorMemberOverrideResponses, ValidateDeploymentYamlData, ValidateDeploymentYamlErrors, ValidateDeploymentYamlResponses, VllmChatCompletionsWithAgentData, VllmChatCompletionsWithAgentErrors, VllmChatCompletionsWithAgentResponses, VllmChatCompletionsWithDefaultAgentData, VllmChatCompletionsWithDefaultAgentErrors, VllmChatCompletionsWithDefaultAgentResponses, VllmEmbeddingsWithAgentData, VllmEmbeddingsWithAgentErrors, VllmEmbeddingsWithAgentResponses, VllmEmbeddingsWithDefaultAgentData, VllmEmbeddingsWithDefaultAgentErrors, VllmEmbeddingsWithDefaultAgentResponses, XaiChatCompletionsWithAgentData, XaiChatCompletionsWithAgentErrors, XaiChatCompletionsWithAgentResponses, XaiChatCompletionsWithDefaultAgentData, XaiChatCompletionsWithDefaultAgentErrors, XaiChatCompletionsWithDefaultAgentResponses, XaiSubscriptionDeviceAuthPollData, XaiSubscriptionDeviceAuthPollErrors, XaiSubscriptionDeviceAuthPollResponses, XaiSubscriptionDeviceAuthStartData, XaiSubscriptionDeviceAuthStartErrors, XaiSubscriptionDeviceAuthStartResponses, ZhipuaiChatCompletionsWithAgentData, ZhipuaiChatCompletionsWithAgentErrors, ZhipuaiChatCompletionsWithAgentResponses, ZhipuaiChatCompletionsWithDefaultAgentData, ZhipuaiChatCompletionsWithDefaultAgentErrors, ZhipuaiChatCompletionsWithDefaultAgentResponses, ZhipuaiEmbeddingsWithAgentData, ZhipuaiEmbeddingsWithAgentErrors, ZhipuaiEmbeddingsWithAgentResponses, ZhipuaiEmbeddingsWithDefaultAgentData, ZhipuaiEmbeddingsWithDefaultAgentErrors, ZhipuaiEmbeddingsWithDefaultAgentResponses } from './types.gen';

export type Options<TData extends TDataShape = TDataShape, ThrowOnError extends boolean = boolean, TResponse = unknown> = Options2<TData, ThrowOnError, TResponse> & {
    /**
     * You can provide a client instance returned by `createClient()` instead of
     * individual options. This might be also useful if you want to implement a
     * custom client.
     */
    client?: Client;
    /**
     * You can pass arbitrary values through the `meta` object. This can be
     * used to access values that aren't defined as part of the SDK function.
     */
    meta?: Record<string, unknown>;
};

/**
 * Get A2A AgentCard for an internal agent (must be agentType='agent')
 */
export const getV1A2aByAgentIdWellKnownAgentJson = <ThrowOnError extends boolean = false>(options: Options<GetV1A2aByAgentIdWellKnownAgentJsonData, ThrowOnError>) => (options.client ?? client).get<GetV1A2aByAgentIdWellKnownAgentJsonResponses, unknown, ThrowOnError>({ url: '/v1/a2a/{agentId}/.well-known/agent.json', ...options });

/**
 * Execute A2A message on an internal agent (must be agentType='agent'). Accepts a JSON-RPC envelope or any JSON payload — non-JSON-RPC payloads are stringified and passed through to the agent as the user message.
 */
export const postV1A2aByAgentId = <ThrowOnError extends boolean = false>(options: Options<PostV1A2aByAgentIdData, ThrowOnError>) => (options.client ?? client).post<PostV1A2aByAgentIdResponses, unknown, ThrowOnError>({
    url: '/v1/a2a/{agentId}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List the A2A AgentCards the presented credential is allowed to reach
 */
export const getV2A2aAgents = <ThrowOnError extends boolean = false>(options?: Options<GetV2A2aAgentsData, ThrowOnError>) => (options?.client ?? client).get<GetV2A2aAgentsResponses, unknown, ThrowOnError>({ url: '/v2/a2a/agents', ...options });

/**
 * Get A2A AgentCard for an internal agent (must be agentType='agent')
 */
export const getV2A2aByAgentIdWellKnownAgentCardJson = <ThrowOnError extends boolean = false>(options: Options<GetV2A2aByAgentIdWellKnownAgentCardJsonData, ThrowOnError>) => (options.client ?? client).get<GetV2A2aByAgentIdWellKnownAgentCardJsonResponses, unknown, ThrowOnError>({ url: '/v2/a2a/{agentId}/.well-known/agent-card.json', ...options });

/**
 * Main A2A JSON-RPC endpoint
 */
export const postV2A2aByAgentId = <ThrowOnError extends boolean = false>(options: Options<PostV2A2aByAgentIdData, ThrowOnError>) => (options.client ?? client).post<PostV2A2aByAgentIdResponses, unknown, ThrowOnError>({
    url: '/v2/a2a/{agentId}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get all agents with pagination, sorting, and filtering
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * Checked dynamically based on agent type. `profile` and `agent` require `agent:read`; `mcp_gateway` requires `mcpGateway:read`. If no type filter is provided, the user must have read access to at least one agent type.
 */
export const getAgents = <ThrowOnError extends boolean = false>(options?: Options<GetAgentsData, ThrowOnError>) => (options?.client ?? client).get<GetAgentsResponses, GetAgentsErrors, ThrowOnError>({ url: '/api/agents', ...options });

/**
 * Create a new agent
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * Checked dynamically based on the agent type being created. `profile` and `agent` require `agent:create`; `mcp_gateway` requires `mcpGateway:create`. Additional scope and team-admin checks may apply.
 */
export const createAgent = <ThrowOnError extends boolean = false>(options: Options<CreateAgentData, ThrowOnError>) => (options.client ?? client).post<CreateAgentResponses, CreateAgentErrors, ThrowOnError>({
    url: '/api/agents',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get all agents without pagination
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * Checked dynamically based on agent type. `profile` and `agent` require `agent:read`; `mcp_gateway` requires `mcpGateway:read`. If no type filter is provided, the user must have read access to at least one agent type.
 */
export const getAllAgents = <ThrowOnError extends boolean = false>(options?: Options<GetAllAgentsData, ThrowOnError>) => (options?.client ?? client).get<GetAllAgentsResponses, GetAllAgentsErrors, ThrowOnError>({ url: '/api/agents/all', ...options });

/**
 * For each internal agent that enforces a missing-credential behavior, the MCP servers the calling user has no usable connection to
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * None (no additional RBAC permission required)
 */
export const getAgentCredentialReadiness = <ThrowOnError extends boolean = false>(options?: Options<GetAgentCredentialReadinessData, ThrowOnError>) => (options?.client ?? client).get<GetAgentCredentialReadinessResponses, GetAgentCredentialReadinessErrors, ThrowOnError>({ url: '/api/agents/credential-readiness', ...options });

/**
 * Get default MCP Gateway
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `mcpGateway:read`: View and list MCP gateways
 */
export const getDefaultMcpGateway = <ThrowOnError extends boolean = false>(options?: Options<GetDefaultMcpGatewayData, ThrowOnError>) => (options?.client ?? client).get<GetDefaultMcpGatewayResponses, GetDefaultMcpGatewayErrors, ThrowOnError>({ url: '/api/mcp-gateways/default', ...options });

/**
 * Import an agent from a portable JSON payload. Creates a new agent with all resolvable associations and returns soft warnings for any references that could not be found.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * None (no additional RBAC permission required)
 */
export const importAgent = <ThrowOnError extends boolean = false>(options: Options<ImportAgentData, ThrowOnError>) => (options.client ?? client).post<ImportAgentResponses, ImportAgentErrors, ThrowOnError>({
    url: '/api/agents/import',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete an agent
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * Checked dynamically based on the target agent's type. `profile` and `agent` require `agent:delete`; `mcp_gateway` requires `mcpGateway:delete`. Additional scope checks may apply.
 */
export const deleteAgent = <ThrowOnError extends boolean = false>(options: Options<DeleteAgentData, ThrowOnError>) => (options.client ?? client).delete<DeleteAgentResponses, DeleteAgentErrors, ThrowOnError>({ url: '/api/agents/{id}', ...options });

/**
 * Get agent by ID
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * Checked dynamically based on the target agent's type. `profile` and `agent` require `agent:read`; `mcp_gateway` requires `mcpGateway:read`.
 */
export const getAgent = <ThrowOnError extends boolean = false>(options: Options<GetAgentData, ThrowOnError>) => (options.client ?? client).get<GetAgentResponses, GetAgentErrors, ThrowOnError>({ url: '/api/agents/{id}', ...options });

/**
 * Update an agent
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * Checked dynamically based on the target agent's type. `profile` and `agent` require `agent:update`; `mcp_gateway` requires `mcpGateway:update`. Additional scope and team-admin checks may apply.
 */
export const updateAgent = <ThrowOnError extends boolean = false>(options: Options<UpdateAgentData, ThrowOnError>) => (options.client ?? client).put<UpdateAgentResponses, UpdateAgentErrors, ThrowOnError>({
    url: '/api/agents/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List an agent's config version history, newest first, as metadata only (no snapshot). Retention keeps the last 100 versions, so the oldest listed version may be greater than 1.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * None (no additional RBAC permission required)
 */
export const getAgentVersions = <ThrowOnError extends boolean = false>(options: Options<GetAgentVersionsData, ThrowOnError>) => (options.client ?? client).get<GetAgentVersionsResponses, GetAgentVersionsErrors, ThrowOnError>({ url: '/api/agents/{id}/versions', ...options });

/**
 * Get one immutable agent config version (full snapshot; key material is never captured). Versions dropped by retention are 404.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * None (no additional RBAC permission required)
 */
export const getAgentVersion = <ThrowOnError extends boolean = false>(options: Options<GetAgentVersionData, ThrowOnError>) => (options.client ?? client).get<GetAgentVersionResponses, GetAgentVersionErrors, ThrowOnError>({ url: '/api/agents/{id}/versions/{version}', ...options });

/**
 * Restore an agent's config to an earlier version by replaying its snapshot forward as a new head version — history is never rewritten. All-or-nothing: the restore is validated in full before anything is written, and a version referencing something that no longer exists or is out of the caller's reach (a deleted tool, key or knowledge source) is rejected with 400 rather than partially applied. Only differences from the agent's live config are written, so restoring the current configuration is a no-op. Retrying is safe: the source version is immutable, and the pre-restore config is forked as a version before anything is written.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * None (no additional RBAC permission required)
 */
export const restoreAgentVersion = <ThrowOnError extends boolean = false>(options: Options<RestoreAgentVersionData, ThrowOnError>) => (options.client ?? client).post<RestoreAgentVersionResponses, RestoreAgentVersionErrors, ThrowOnError>({
    url: '/api/agents/{id}/versions/{version}/restore',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Clone an agent and all its associations. Optionally override the clone's visibility (scope/teams); by default the source's visibility is copied.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * None (no additional RBAC permission required)
 */
export const cloneAgent = <ThrowOnError extends boolean = false>(options: Options<CloneAgentData, ThrowOnError>) => (options.client ?? client).post<CloneAgentResponses, CloneAgentErrors, ThrowOnError>({
    url: '/api/agents/{id}/clone',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Export an agent configuration as a portable JSON payload for cross-instance transfer
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * None (no additional RBAC permission required)
 */
export const exportAgent = <ThrowOnError extends boolean = false>(options: Options<ExportAgentData, ThrowOnError>) => (options.client ?? client).get<ExportAgentResponses, ExportAgentErrors, ThrowOnError>({ url: '/api/agents/{id}/export', ...options });

/**
 * Get the agent's Auto-tool-mode exclusions: MCP catalogs and individual tools removed from its tool surface while 'access all tools' is on
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * None (no additional RBAC permission required)
 */
export const getAgentToolExclusions = <ThrowOnError extends boolean = false>(options: Options<GetAgentToolExclusionsData, ThrowOnError>) => (options.client ?? client).get<GetAgentToolExclusionsResponses, GetAgentToolExclusionsErrors, ThrowOnError>({ url: '/api/agents/{id}/tool-exclusions', ...options });

/**
 * Replace the agent's Auto-tool-mode exclusions (full replace of the excluded tool set)
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * None (no additional RBAC permission required)
 */
export const updateAgentToolExclusions = <ThrowOnError extends boolean = false>(options: Options<UpdateAgentToolExclusionsData, ThrowOnError>) => (options.client ?? client).put<UpdateAgentToolExclusionsResponses, UpdateAgentToolExclusionsErrors, ThrowOnError>({
    url: '/api/agents/{id}/tool-exclusions',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get the agent's Auto-subagent-mode exclusions: delegation target agents removed from its Auto delegation surface while 'access all subagents' is on
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * None (no additional RBAC permission required)
 */
export const getAgentSubagentExclusions = <ThrowOnError extends boolean = false>(options: Options<GetAgentSubagentExclusionsData, ThrowOnError>) => (options.client ?? client).get<GetAgentSubagentExclusionsResponses, GetAgentSubagentExclusionsErrors, ThrowOnError>({ url: '/api/agents/{id}/subagent-exclusions', ...options });

/**
 * Replace the agent's Auto-subagent-mode exclusions (full replace of the excluded delegation-target set)
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * None (no additional RBAC permission required)
 */
export const updateAgentSubagentExclusions = <ThrowOnError extends boolean = false>(options: Options<UpdateAgentSubagentExclusionsData, ThrowOnError>) => (options.client ?? client).put<UpdateAgentSubagentExclusionsResponses, UpdateAgentSubagentExclusionsErrors, ThrowOnError>({
    url: '/api/agents/{id}/subagent-exclusions',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get the agent's Auto-mode knowledge-source exclusions: knowledge connectors removed from the surface its knowledge queries span while 'access all tools' is on
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `knowledgeSource:read`: View Knowledge Bases and Connectors
 */
export const getAgentKnowledgeSourceExclusions = <ThrowOnError extends boolean = false>(options: Options<GetAgentKnowledgeSourceExclusionsData, ThrowOnError>) => (options.client ?? client).get<GetAgentKnowledgeSourceExclusionsResponses, GetAgentKnowledgeSourceExclusionsErrors, ThrowOnError>({ url: '/api/agents/{id}/knowledge-source-exclusions', ...options });

/**
 * Replace the agent's Auto-mode knowledge-source exclusions (full replace of the excluded knowledge-connector set)
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `knowledgeSource:read`: View Knowledge Bases and Connectors
 */
export const updateAgentKnowledgeSourceExclusions = <ThrowOnError extends boolean = false>(options: Options<UpdateAgentKnowledgeSourceExclusionsData, ThrowOnError>) => (options.client ?? client).put<UpdateAgentKnowledgeSourceExclusionsResponses, UpdateAgentKnowledgeSourceExclusionsErrors, ThrowOnError>({
    url: '/api/agents/{id}/knowledge-source-exclusions',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get the skills this gateway publishes over MCP: the explicitly assigned set, plus whether Auto mode ('access all skills') is on
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `skill:read`: View and use agent skills within your scope (org, your teams, your own)
 */
export const getAgentSkills = <ThrowOnError extends boolean = false>(options: Options<GetAgentSkillsData, ThrowOnError>) => (options.client ?? client).get<GetAgentSkillsResponses, GetAgentSkillsErrors, ThrowOnError>({ url: '/api/agents/{id}/skills', ...options });

/**
 * Replace the skills this gateway publishes over MCP (full replace of the assigned set) and set Auto mode
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `skill:read`: View and use agent skills within your scope (org, your teams, your own)
 */
export const updateAgentSkills = <ThrowOnError extends boolean = false>(options: Options<UpdateAgentSkillsData, ThrowOnError>) => (options.client ?? client).put<UpdateAgentSkillsResponses, UpdateAgentSkillsErrors, ThrowOnError>({
    url: '/api/agents/{id}/skills',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get the agent's Auto-skill-mode exclusions: skills removed from its published skill surface while 'access all skills' is on
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `skill:read`: View and use agent skills within your scope (org, your teams, your own)
 */
export const getAgentSkillExclusions = <ThrowOnError extends boolean = false>(options: Options<GetAgentSkillExclusionsData, ThrowOnError>) => (options.client ?? client).get<GetAgentSkillExclusionsResponses, GetAgentSkillExclusionsErrors, ThrowOnError>({ url: '/api/agents/{id}/skill-exclusions', ...options });

/**
 * Replace the agent's Auto-skill-mode exclusions (full replace of the excluded skill set)
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `skill:read`: View and use agent skills within your scope (org, your teams, your own)
 */
export const updateAgentSkillExclusions = <ThrowOnError extends boolean = false>(options: Options<UpdateAgentSkillExclusionsData, ThrowOnError>) => (options.client ?? client).put<UpdateAgentSkillExclusionsResponses, UpdateAgentSkillExclusionsErrors, ThrowOnError>({
    url: '/api/agents/{id}/skill-exclusions',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Soft-delete several agents in one request. Each id is authorized exactly as the single-agent delete authorizes its own, so an id the caller cannot see or modify — and a built-in agent or personal MCP gateway, which are never deletable — is reported in `failed` while the rest of the batch still applies. Deleted agents can be restored from the trash. Members who chose one as their personal default, and projects pinning it, are unpinned, as they are on the single-agent delete.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * None (no additional RBAC permission required)
 */
export const bulkDeleteAgents = <ThrowOnError extends boolean = false>(options: Options<BulkDeleteAgentsData, ThrowOnError>) => (options.client ?? client).delete<BulkDeleteAgentsResponses, BulkDeleteAgentsErrors, ThrowOnError>({
    url: '/api/agents/bulk',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Update several agents in one request. Today the only editable surface is visibility — `scope` with the `teams` it belongs to or the `users` it is shared with — and every agent in the batch is moved to the same one. The target is validated once for the whole request (a 400 or 403 changes nothing); per-agent problems, such as an id the caller cannot see or modify, are reported in `failed` and leave the rest of the batch applied. An agent already in the requested state is reported as succeeded without being rewritten.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * None (no additional RBAC permission required)
 */
export const bulkUpdateAgents = <ThrowOnError extends boolean = false>(options: Options<BulkUpdateAgentsData, ThrowOnError>) => (options.client ?? client).patch<BulkUpdateAgentsResponses, BulkUpdateAgentsErrors, ThrowOnError>({
    url: '/api/agents/bulk',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Restore a soft-deleted agent
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * Checked dynamically based on the target agent's type. `profile` and `agent` require `agent:delete`; `mcp_gateway` requires `mcpGateway:delete`. Additional scope checks may apply.
 */
export const restoreAgent = <ThrowOnError extends boolean = false>(options: Options<RestoreAgentData, ThrowOnError>) => (options.client ?? client).post<RestoreAgentResponses, RestoreAgentErrors, ThrowOnError>({ url: '/api/agents/{id}/restore', ...options });

/**
 * Permanently destroy a soft-deleted agent. Global admins only — an `agent:delete` or `agent:admin` grant reaches the trash, not past it. Irreversible, with no grace period: the agent's configuration and scheduled runs are destroyed, and it is cleared from the organization, /connection, and member defaults. Its history survives, detached — conversations and LLM usage rows are kept and simply stop pointing at it. 404 if there is no soft-deleted agent with that id in the org, which is also the answer when the agent is still live or the caller is not a global admin. Restore wins a race.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * Restricted to the built-in Admin and Platform Admin roles. No permission grants it: `agent:delete` and even `agent:admin` reach the trash, not past it, and a service account never qualifies. A caller without it gets 404, not 403, so the endpoint never confirms the agent exists.
 */
export const permanentlyDeleteAgent = <ThrowOnError extends boolean = false>(options: Options<PermanentlyDeleteAgentData, ThrowOnError>) => (options.client ?? client).delete<PermanentlyDeleteAgentResponses, PermanentlyDeleteAgentErrors, ThrowOnError>({ url: '/api/agents/{id}/permanent', ...options });

/**
 * Get all available label keys
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * None (no additional RBAC permission required)
 */
export const getLabelKeys = <ThrowOnError extends boolean = false>(options?: Options<GetLabelKeysData, ThrowOnError>) => (options?.client ?? client).get<GetLabelKeysResponses, GetLabelKeysErrors, ThrowOnError>({ url: '/api/agents/labels/keys', ...options });

/**
 * Get all available label values
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * None (no additional RBAC permission required)
 */
export const getLabelValues = <ThrowOnError extends boolean = false>(options?: Options<GetLabelValuesData, ThrowOnError>) => (options?.client ?? client).get<GetLabelValuesResponses, GetLabelValuesErrors, ThrowOnError>({ url: '/api/agents/labels/values', ...options });

/**
 * Get the current user's default agent ID
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * None (no additional RBAC permission required)
 */
export const getMemberDefaultAgent = <ThrowOnError extends boolean = false>(options?: Options<GetMemberDefaultAgentData, ThrowOnError>) => (options?.client ?? client).get<GetMemberDefaultAgentResponses, GetMemberDefaultAgentErrors, ThrowOnError>({ url: '/api/members/default-agent', ...options });

/**
 * Set or clear the current user's default agent. Any chat agent the caller can see may be pinned — their own, a team's, or an organization-wide one — and it is preselected for their new chats ahead of the organization default. Null clears it, so the organization default applies. Nothing else writes this: a member who never pinned one has no personal default, and the organization default reaches them.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * None (no additional RBAC permission required)
 */
export const updateMemberDefaultAgent = <ThrowOnError extends boolean = false>(options: Options<UpdateMemberDefaultAgentData, ThrowOnError>) => (options.client ?? client).put<UpdateMemberDefaultAgentResponses, UpdateMemberDefaultAgentErrors, ThrowOnError>({
    url: '/api/members/default-agent',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get the current user's default model and API key
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * None (no additional RBAC permission required)
 */
export const getMemberDefaultModel = <ThrowOnError extends boolean = false>(options?: Options<GetMemberDefaultModelData, ThrowOnError>) => (options?.client ?? client).get<GetMemberDefaultModelResponses, GetMemberDefaultModelErrors, ThrowOnError>({ url: '/api/members/default-model', ...options });

/**
 * Set the current user's default model and API key
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * None (no additional RBAC permission required)
 */
export const updateMemberDefaultModel = <ThrowOnError extends boolean = false>(options: Options<UpdateMemberDefaultModelData, ThrowOnError>) => (options.client ?? client).put<UpdateMemberDefaultModelResponses, UpdateMemberDefaultModelErrors, ThrowOnError>({
    url: '/api/members/default-model',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get all agent-tool relationships with pagination, sorting, and filtering
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `toolPolicy:read`: View tools, tool invocation policies, and trusted data policies
 */
export const getAllAgentTools = <ThrowOnError extends boolean = false>(options?: Options<GetAllAgentToolsData, ThrowOnError>) => (options?.client ?? client).get<GetAllAgentToolsResponses, GetAllAgentToolsErrors, ThrowOnError>({ url: '/api/agent-tools', ...options });

/**
 * Unassign a tool from an agent
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * None (no additional RBAC permission required)
 */
export const unassignToolFromAgent = <ThrowOnError extends boolean = false>(options: Options<UnassignToolFromAgentData, ThrowOnError>) => (options.client ?? client).delete<UnassignToolFromAgentResponses, UnassignToolFromAgentErrors, ThrowOnError>({ url: '/api/agents/{agentId}/tools/{toolId}', ...options });

/**
 * Assign a tool to an agent
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * None (no additional RBAC permission required)
 */
export const assignToolToAgent = <ThrowOnError extends boolean = false>(options: Options<AssignToolToAgentData, ThrowOnError>) => (options.client ?? client).post<AssignToolToAgentResponses, AssignToolToAgentErrors, ThrowOnError>({
    url: '/api/agents/{agentId}/tools/{toolId}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Assign multiple tools to multiple agents in bulk
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * None (no additional RBAC permission required)
 */
export const bulkAssignTools = <ThrowOnError extends boolean = false>(options: Options<BulkAssignToolsData, ThrowOnError>) => (options.client ?? client).post<BulkAssignToolsResponses, BulkAssignToolsErrors, ThrowOnError>({
    url: '/api/agents/tools/bulk-assign',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Apply a batch of tool assignments and removals across agents in one request. Intended for editors that save a whole tool selection at once: the batch produces a single config version per affected agent instead of one per tool. If the same agent/tool pair appears in both lists, the assignment wins and the removal is ignored. A pair repeated within a list is written once but reported once per occurrence. Agents the caller cannot modify are reported two different ways: an agent that belongs to another organization or no longer exists is reported per entry in `failed`, and the rest of the batch still applies; an agent in the caller's own organization whose agent type the caller lacks `update` on rejects the entire request with 403 and applies nothing.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * None (no additional RBAC permission required)
 */
export const bulkUpdateAgentTools = <ThrowOnError extends boolean = false>(options: Options<BulkUpdateAgentToolsData, ThrowOnError>) => (options.client ?? client).post<BulkUpdateAgentToolsResponses, BulkUpdateAgentToolsErrors, ThrowOnError>({
    url: '/api/agents/tools/bulk-update',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Automatically configure security policies for tools using LLM analysis
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `toolPolicy:update`: Modify tools, tool configuration, and security policies
 */
export const autoConfigureAgentToolPolicies = <ThrowOnError extends boolean = false>(options: Options<AutoConfigureAgentToolPoliciesData, ThrowOnError>) => (options.client ?? client).post<AutoConfigureAgentToolPoliciesResponses, AutoConfigureAgentToolPoliciesErrors, ThrowOnError>({
    url: '/api/agent-tools/auto-configure-policies',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get all tools for an agent (both proxy-sniffed and MCP tools)
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * None (no additional RBAC permission required)
 */
export const getAgentTools = <ThrowOnError extends boolean = false>(options: Options<GetAgentToolsData, ThrowOnError>) => (options.client ?? client).get<GetAgentToolsResponses, GetAgentToolsErrors, ThrowOnError>({ url: '/api/agents/{agentId}/tools', ...options });

/**
 * Update an agent-tool relationship
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `toolPolicy:update`: Modify tools, tool configuration, and security policies
 */
export const updateAgentTool = <ThrowOnError extends boolean = false>(options: Options<UpdateAgentToolData, ThrowOnError>) => (options.client ?? client).patch<UpdateAgentToolResponses, UpdateAgentToolErrors, ThrowOnError>({
    url: '/api/agent-tools/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get all delegation targets for an agent. Not applicable to LLM proxies.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * None (no additional RBAC permission required)
 */
export const getAgentDelegations = <ThrowOnError extends boolean = false>(options: Options<GetAgentDelegationsData, ThrowOnError>) => (options.client ?? client).get<GetAgentDelegationsResponses, GetAgentDelegationsErrors, ThrowOnError>({ url: '/api/agents/{agentId}/delegations', ...options });

/**
 * Sync delegation targets for an agent. Replaces all existing delegations with the new list. Not applicable to LLM proxies.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * None (no additional RBAC permission required)
 */
export const syncAgentDelegations = <ThrowOnError extends boolean = false>(options: Options<SyncAgentDelegationsData, ThrowOnError>) => (options.client ?? client).post<SyncAgentDelegationsResponses, SyncAgentDelegationsErrors, ThrowOnError>({
    url: '/api/agents/{agentId}/delegations',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Remove a specific delegation from an agent. Not applicable to LLM proxies.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * None (no additional RBAC permission required)
 */
export const deleteAgentDelegation = <ThrowOnError extends boolean = false>(options: Options<DeleteAgentDelegationData, ThrowOnError>) => (options.client ?? client).delete<DeleteAgentDelegationResponses, DeleteAgentDelegationErrors, ThrowOnError>({ url: '/api/agents/{agentId}/delegations/{targetAgentId}', ...options });

/**
 * Get all agent delegation connections for canvas visualization.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * None (no additional RBAC permission required)
 */
export const getAllDelegationConnections = <ThrowOnError extends boolean = false>(options?: Options<GetAllDelegationConnectionsData, ThrowOnError>) => (options?.client ?? client).get<GetAllDelegationConnectionsResponses, GetAllDelegationConnectionsErrors, ThrowOnError>({ url: '/api/agent-delegations', ...options });

/**
 * Send a message to Anthropic using the default agent
 *
 * Authentication:
 *
 * This route accepts either an LLM provider API key or a Virtual API Key. See [LLM Proxy Authentication](/docs/platform-llm-proxy-authentication).
 */
export const anthropicMessagesWithDefaultAgent = <ThrowOnError extends boolean = false>(options: Options<AnthropicMessagesWithDefaultAgentData, ThrowOnError>) => (options.client ?? client).post<AnthropicMessagesWithDefaultAgentResponses, AnthropicMessagesWithDefaultAgentErrors, ThrowOnError>({
    url: '/v1/anthropic/v1/messages',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Send a message to Anthropic using a specific agent (n8n URL format)
 *
 * Authentication:
 *
 * This route accepts either an LLM provider API key or a Virtual API Key. See [LLM Proxy Authentication](/docs/platform-llm-proxy-authentication).
 */
export const anthropicMessagesWithAgent = <ThrowOnError extends boolean = false>(options: Options<AnthropicMessagesWithAgentData, ThrowOnError>) => (options.client ?? client).post<AnthropicMessagesWithAgentResponses, AnthropicMessagesWithAgentErrors, ThrowOnError>({
    url: '/v1/anthropic/{agentId}/v1/messages',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List Anthropic models (default agent)
 *
 * Authentication:
 *
 * This route accepts either an LLM provider API key or a Virtual API Key. See [LLM Proxy Authentication](/docs/platform-llm-proxy-authentication).
 */
export const anthropicListModelsWithDefaultAgent = <ThrowOnError extends boolean = false>(options?: Options<AnthropicListModelsWithDefaultAgentData, ThrowOnError>) => (options?.client ?? client).get<AnthropicListModelsWithDefaultAgentResponses, AnthropicListModelsWithDefaultAgentErrors, ThrowOnError>({ url: '/v1/anthropic/v1/models', ...options });

/**
 * List Anthropic models (specific agent)
 *
 * Authentication:
 *
 * This route accepts either an LLM provider API key or a Virtual API Key. See [LLM Proxy Authentication](/docs/platform-llm-proxy-authentication).
 */
export const anthropicListModelsWithAgent = <ThrowOnError extends boolean = false>(options: Options<AnthropicListModelsWithAgentData, ThrowOnError>) => (options.client ?? client).get<AnthropicListModelsWithAgentResponses, AnthropicListModelsWithAgentErrors, ThrowOnError>({ url: '/v1/anthropic/{agentId}/v1/models', ...options });

/**
 * List the authenticated user's Archestra API keys
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `apiKey:read`: View API keys
 */
export const getApiKeys = <ThrowOnError extends boolean = false>(options?: Options<GetApiKeysData, ThrowOnError>) => (options?.client ?? client).get<GetApiKeysResponses, GetApiKeysErrors, ThrowOnError>({ url: '/api/api-keys', ...options });

/**
 * Create an Archestra API key for the authenticated user
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `apiKey:create`: Create API keys
 */
export const createApiKey = <ThrowOnError extends boolean = false>(options: Options<CreateApiKeyData, ThrowOnError>) => (options.client ?? client).post<CreateApiKeyResponses, CreateApiKeyErrors, ThrowOnError>({
    url: '/api/api-keys',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete an Archestra API key for the authenticated user
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `apiKey:delete`: Delete API keys
 */
export const deleteApiKey = <ThrowOnError extends boolean = false>(options: Options<DeleteApiKeyData, ThrowOnError>) => (options.client ?? client).delete<DeleteApiKeyResponses, DeleteApiKeyErrors, ThrowOnError>({ url: '/api/api-keys/{id}', ...options });

/**
 * Get one authenticated user's Archestra API key
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `apiKey:read`: View API keys
 */
export const getApiKey = <ThrowOnError extends boolean = false>(options: Options<GetApiKeyData, ThrowOnError>) => (options.client ?? client).get<GetApiKeyResponses, GetApiKeyErrors, ThrowOnError>({ url: '/api/api-keys/{id}', ...options });

/**
 * Delete several of the authenticated user's Archestra API keys in one request. Keys are personal, so an id belonging to anyone else is reported in `failed` as not found and the rest of the batch still applies.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `apiKey:delete`: Delete API keys
 */
export const bulkDeleteApiKeys = <ThrowOnError extends boolean = false>(options: Options<BulkDeleteApiKeysData, ThrowOnError>) => (options.client ?? client).delete<BulkDeleteApiKeysResponses, BulkDeleteApiKeysErrors, ThrowOnError>({
    url: '/api/api-keys/bulk',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Start the GitHub device flow used to share a recorded app session to the public App Gallery
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `chat:update`: Edit chat messages and conversation settings
 */
export const appGalleryDeviceAuthStart = <ThrowOnError extends boolean = false>(options?: Options<AppGalleryDeviceAuthStartData, ThrowOnError>) => (options?.client ?? client).post<AppGalleryDeviceAuthStartResponses, AppGalleryDeviceAuthStartErrors, ThrowOnError>({ url: '/api/app-gallery/device/start', ...options });

/**
 * Poll the GitHub device flow once; returns the GitHub OAuth token when the participant has authorized
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `chat:update`: Edit chat messages and conversation settings
 */
export const appGalleryDeviceAuthPoll = <ThrowOnError extends boolean = false>(options: Options<AppGalleryDeviceAuthPollData, ThrowOnError>) => (options.client ?? client).post<AppGalleryDeviceAuthPollResponses, AppGalleryDeviceAuthPollErrors, ThrowOnError>({
    url: '/api/app-gallery/device/poll',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Draft the AI enhancement for a recorded app-building session: a one-sentence app description, one consolidated build prompt, one closing agent response, and a gallery category, all generated from the full chat. All are drafts the builder edits before applying; nulls mean generation was unavailable and the client falls back, with `reason` naming why the build prompt is missing so the client can say so instead of degrading silently.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `chat:update`: Edit chat messages and conversation settings
 */
export const enhanceAppRecording = <ThrowOnError extends boolean = false>(options: Options<EnhanceAppRecordingData, ThrowOnError>) => (options.client ?? client).post<EnhanceAppRecordingResponses, EnhanceAppRecordingErrors, ThrowOnError>({
    url: '/api/app-recordings/enhance',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Start rendering a recorded app session to an MP4 and return the job's id. Rendering runs in the background — poll the status endpoint and collect the file when it reports done. The recording is rendered from the bundle sent with the request; nothing is read from or written to server storage.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `chat:update`: Edit chat messages and conversation settings
 */
export const renderAppRecordingVideo = <ThrowOnError extends boolean = false>(options: Options<RenderAppRecordingVideoData, ThrowOnError>) => (options.client ?? client).post<RenderAppRecordingVideoResponses, RenderAppRecordingVideoErrors, ThrowOnError>({
    url: '/api/app-recordings/render',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Stop a running render and discard it, freeing the browser it was driving.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `chat:update`: Edit chat messages and conversation settings
 */
export const cancelAppRecordingRender = <ThrowOnError extends boolean = false>(options: Options<CancelAppRecordingRenderData, ThrowOnError>) => (options.client ?? client).delete<CancelAppRecordingRenderResponses, CancelAppRecordingRenderErrors, ThrowOnError>({ url: '/api/app-recordings/render/{jobId}', ...options });

/**
 * How a video render is going. `done` means the file is ready to collect from the download endpoint.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `chat:update`: Edit chat messages and conversation settings
 */
export const getAppRecordingRenderStatus = <ThrowOnError extends boolean = false>(options: Options<GetAppRecordingRenderStatusData, ThrowOnError>) => (options.client ?? client).get<GetAppRecordingRenderStatusResponses, GetAppRecordingRenderStatusErrors, ThrowOnError>({ url: '/api/app-recordings/render/{jobId}', ...options });

/**
 * Collect a finished render. The job ends here — the file is handed over once and the server keeps no copy.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `chat:update`: Edit chat messages and conversation settings
 */
export const downloadAppRecordingVideo = <ThrowOnError extends boolean = false>(options: Options<DownloadAppRecordingVideoData, ThrowOnError>) => (options.client ?? client).get<DownloadAppRecordingVideoResponses, unknown, ThrowOnError>({ url: '/api/app-recordings/render/{jobId}/video', ...options });

/**
 * Fetch a hackathon submission's recording bundle from GitHub (raw.githubusercontent.com only), validate it against the recording contract, and return it for the on-platform, read-only review player. The fetch runs server-side so the frontend's CSP never has to reach GitHub; the bundle is public data and nothing is stored.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * None (no additional RBAC permission required)
 */
export const reviewAppRecording = <ThrowOnError extends boolean = false>(options: Options<ReviewAppRecordingData, ThrowOnError>) => (options.client ?? client).get<ReviewAppRecordingResponses, ReviewAppRecordingErrors, ThrowOnError>({ url: '/api/app-recording/review', ...options });

/**
 * List apps visible to the caller (paginated).
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `app:read`: View and run MCP Apps within your scope (org, your teams, your own)
 */
export const getApps = <ThrowOnError extends boolean = false>(options?: Options<GetAppsData, ThrowOnError>) => (options?.client ?? client).get<GetAppsResponses, GetAppsErrors, ThrowOnError>({ url: '/api/apps', ...options });

/**
 * Create a new MCP App.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `app:create`: Create new MCP Apps
 */
export const createApp = <ThrowOnError extends boolean = false>(options: Options<CreateAppData, ThrowOnError>) => (options.client ?? client).post<CreateAppResponses, CreateAppErrors, ThrowOnError>({
    url: '/api/apps',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Resolve an external UI-providing app by catalog id: its UI resource and the caller's accessible installs (for the standalone run page's install selector).
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `app:read`: View and run MCP Apps within your scope (org, your teams, your own)
 */
export const getExternalApp = <ThrowOnError extends boolean = false>(options: Options<GetExternalAppData, ThrowOnError>) => (options.client ?? client).get<GetExternalAppResponses, GetExternalAppErrors, ThrowOnError>({ url: '/api/apps/external/{catalogId}', ...options });

/**
 * Get all label keys used by apps
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `app:read`: View and run MCP Apps within your scope (org, your teams, your own)
 */
export const getAppLabelKeys = <ThrowOnError extends boolean = false>(options?: Options<GetAppLabelKeysData, ThrowOnError>) => (options?.client ?? client).get<GetAppLabelKeysResponses, GetAppLabelKeysErrors, ThrowOnError>({ url: '/api/apps/labels/keys', ...options });

/**
 * Get all label values used by apps
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `app:read`: View and run MCP Apps within your scope (org, your teams, your own)
 */
export const getAppLabelValues = <ThrowOnError extends boolean = false>(options?: Options<GetAppLabelValuesData, ThrowOnError>) => (options?.client ?? client).get<GetAppLabelValuesResponses, GetAppLabelValuesErrors, ThrowOnError>({ url: '/api/apps/labels/values', ...options });

/**
 * List the curated starter templates a new app can use.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `app:read`: View and run MCP Apps within your scope (org, your teams, your own)
 */
export const getAppTemplates = <ThrowOnError extends boolean = false>(options?: Options<GetAppTemplatesData, ThrowOnError>) => (options?.client ?? client).get<GetAppTemplatesResponses, GetAppTemplatesErrors, ThrowOnError>({ url: '/api/app-templates', ...options });

/**
 * Open an existing app in chat: create a conversation with the app already rendered (no model turn) and return its id to navigate to.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `app:read`: View and run MCP Apps within your scope (org, your teams, your own)
 * `chat:create`: Start new chat conversations
 */
export const openAppInChat = <ThrowOnError extends boolean = false>(options: Options<OpenAppInChatData, ThrowOnError>) => (options.client ?? client).post<OpenAppInChatResponses, OpenAppInChatErrors, ThrowOnError>({ url: '/api/apps/{appId}/open-in-chat', ...options });

/**
 * Open an external (MCP-server) UI app in chat: create a conversation and return its id to navigate to. When the tool needs no inputs the app is seeded already rendered (no model turn); when it has required inputs the conversation is created empty and the response carries an opening prompt for the client to send.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `app:read`: View and run MCP Apps within your scope (org, your teams, your own)
 * `chat:create`: Start new chat conversations
 */
export const openExternalAppInChat = <ThrowOnError extends boolean = false>(options: Options<OpenExternalAppInChatData, ThrowOnError>) => (options.client ?? client).post<OpenExternalAppInChatResponses, OpenExternalAppInChatErrors, ThrowOnError>({
    url: '/api/apps/external/{mcpServerId}/open-in-chat',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Remove the current user's pin on an app. Idempotent; intentionally no visibility check, so a stale pin on an app that was since re-scoped away (or deleted) can still be cleared.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `app:read`: View and run MCP Apps within your scope (org, your teams, your own)
 */
export const unpinApp = <ThrowOnError extends boolean = false>(options: Options<UnpinAppData, ThrowOnError>) => (options.client ?? client).delete<UnpinAppResponses, UnpinAppErrors, ThrowOnError>({ url: '/api/apps/{appId}/pin', ...options });

/**
 * Pin an app for the current user (mirrors project pins). Personal — does not affect other members. Any user who can view the app may pin it.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `app:read`: View and run MCP Apps within your scope (org, your teams, your own)
 */
export const pinApp = <ThrowOnError extends boolean = false>(options: Options<PinAppData, ThrowOnError>) => (options.client ?? client).put<PinAppResponses, PinAppErrors, ThrowOnError>({ url: '/api/apps/{appId}/pin', ...options });

/**
 * Remove the current user's pin on an external app. Idempotent; intentionally no access check, so a stale pin on an install the user lost access to can still be cleared. `resourceUri` and `toolName` ride the query string (DELETE carries no body).
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `app:read`: View and run MCP Apps within your scope (org, your teams, your own)
 */
export const unpinExternalApp = <ThrowOnError extends boolean = false>(options: Options<UnpinExternalAppData, ThrowOnError>) => (options.client ?? client).delete<UnpinExternalAppResponses, UnpinExternalAppErrors, ThrowOnError>({ url: '/api/apps/external/{mcpServerId}/pin', ...options });

/**
 * Pin an external (MCP-server) UI app for the current user, identified by install + resource + tool (several tools of one server can share a UI resource, so the tool name pins one tile, not the group). Personal — does not affect other members.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `app:read`: View and run MCP Apps within your scope (org, your teams, your own)
 */
export const pinExternalApp = <ThrowOnError extends boolean = false>(options: Options<PinExternalAppData, ThrowOnError>) => (options.client ?? client).put<PinExternalAppResponses, PinExternalAppErrors, ThrowOnError>({
    url: '/api/apps/external/{mcpServerId}/pin',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Soft-delete an app the caller owns or administers.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `app:delete`: Delete MCP Apps
 */
export const deleteApp = <ThrowOnError extends boolean = false>(options: Options<DeleteAppData, ThrowOnError>) => (options.client ?? client).delete<DeleteAppResponses, DeleteAppErrors, ThrowOnError>({ url: '/api/apps/{appId}', ...options });

/**
 * Get a single app by id or slug, if the caller may view it.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `app:read`: View and run MCP Apps within your scope (org, your teams, your own)
 */
export const getApp = <ThrowOnError extends boolean = false>(options: Options<GetAppData, ThrowOnError>) => (options.client ?? client).get<GetAppResponses, GetAppErrors, ThrowOnError>({ url: '/api/apps/{appId}', ...options });

/**
 * Update an app's metadata and/or html (forks a new version).
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `app:update`: Modify MCP Apps, their tools, and their team assignments
 */
export const updateApp = <ThrowOnError extends boolean = false>(options: Options<UpdateAppData, ThrowOnError>) => (options.client ?? client).patch<UpdateAppResponses, UpdateAppErrors, ThrowOnError>({
    url: '/api/apps/{appId}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Soft-delete several apps in one request. Each id is authorized exactly as the single-app delete authorizes its own, so an app the caller cannot see or administer — and a locked app, which is never deletable until unlocked — is reported in `failed` while the rest of the batch still applies.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `app:delete`: Delete MCP Apps
 */
export const bulkDeleteApps = <ThrowOnError extends boolean = false>(options: Options<BulkDeleteAppsData, ThrowOnError>) => (options.client ?? client).delete<BulkDeleteAppsResponses, BulkDeleteAppsErrors, ThrowOnError>({
    url: '/api/apps/bulk',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Update several apps in one request. Today the only bulk-editable surface is visibility — `scope` with the `teamIds` or `userIds` it reaches — and every app in the batch is moved to the same one. Content is deliberately not editable here: replacing html forks a version and is authorized more strictly than re-scoping. Per-app problems are reported in `failed` and leave the rest applied.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `app:update`: Modify MCP Apps, their tools, and their team assignments
 */
export const bulkUpdateApps = <ThrowOnError extends boolean = false>(options: Options<BulkUpdateAppsData, ThrowOnError>) => (options.client ?? client).patch<BulkUpdateAppsResponses, BulkUpdateAppsErrors, ThrowOnError>({
    url: '/api/apps/bulk',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Enable a disabled app, making it live at its scope.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `app:update`: Modify MCP Apps, their tools, and their team assignments
 */
export const enableApp = <ThrowOnError extends boolean = false>(options: Options<EnableAppData, ThrowOnError>) => (options.client ?? client).post<EnableAppResponses, EnableAppErrors, ThrowOnError>({ url: '/api/apps/{appId}/enable', ...options });

/**
 * Disable an app, pulling it back to an author-only state.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `app:update`: Modify MCP Apps, their tools, and their team assignments
 */
export const disableApp = <ThrowOnError extends boolean = false>(options: Options<DisableAppData, ThrowOnError>) => (options.client ?? client).post<DisableAppResponses, DisableAppErrors, ThrowOnError>({ url: '/api/apps/{appId}/disable', ...options });

/**
 * Lock an app, refusing all chat-driven modification (and REST html replacement/deletion) until unlocked.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `app:update`: Modify MCP Apps, their tools, and their team assignments
 */
export const lockApp = <ThrowOnError extends boolean = false>(options: Options<LockAppData, ThrowOnError>) => (options.client ?? client).post<LockAppResponses, LockAppErrors, ThrowOnError>({ url: '/api/apps/{appId}/lock', ...options });

/**
 * Unlock a locked app, making it editable again.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `app:update`: Modify MCP Apps, their tools, and their team assignments
 */
export const unlockApp = <ThrowOnError extends boolean = false>(options: Options<UnlockAppData, ThrowOnError>) => (options.client ?? client).post<UnlockAppResponses, UnlockAppErrors, ThrowOnError>({ url: '/api/apps/{appId}/unlock', ...options });

/**
 * List an app's versions, newest first.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `app:read`: View and run MCP Apps within your scope (org, your teams, your own)
 */
export const getAppVersions = <ThrowOnError extends boolean = false>(options: Options<GetAppVersionsData, ThrowOnError>) => (options.client ?? client).get<GetAppVersionsResponses, GetAppVersionsErrors, ThrowOnError>({ url: '/api/apps/{appId}/versions', ...options });

/**
 * Get a specific app version.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `app:read`: View and run MCP Apps within your scope (org, your teams, your own)
 */
export const getAppVersion = <ThrowOnError extends boolean = false>(options: Options<GetAppVersionData, ThrowOnError>) => (options.client ?? client).get<GetAppVersionResponses, GetAppVersionErrors, ThrowOnError>({ url: '/api/apps/{appId}/versions/{version}', ...options });

/**
 * List the tools assigned to an app.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `app:read`: View and run MCP Apps within your scope (org, your teams, your own)
 */
export const getAppTools = <ThrowOnError extends boolean = false>(options: Options<GetAppToolsData, ThrowOnError>) => (options.client ?? client).get<GetAppToolsResponses, GetAppToolsErrors, ThrowOnError>({ url: '/api/apps/{appId}/tools', ...options });

/**
 * Record the calling user's latest render diagnostics for an app. An empty entries array means the render was clean.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `app:read`: View and run MCP Apps within your scope (org, your teams, your own)
 */
export const postAppRenderDiagnostics = <ThrowOnError extends boolean = false>(options: Options<PostAppRenderDiagnosticsData, ThrowOnError>) => (options.client ?? client).post<PostAppRenderDiagnosticsResponses, PostAppRenderDiagnosticsErrors, ThrowOnError>({
    url: '/api/apps/{appId}/diagnostics',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Record the calling user's latest render screenshot for an app (a base64 image data URL the app self-captured).
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `app:read`: View and run MCP Apps within your scope (org, your teams, your own)
 */
export const postAppRenderScreenshot = <ThrowOnError extends boolean = false>(options: Options<PostAppRenderScreenshotData, ThrowOnError>) => (options.client ?? client).post<PostAppRenderScreenshotResponses, PostAppRenderScreenshotErrors, ThrowOnError>({
    url: '/api/apps/{appId}/screenshot',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Unassign a tool from an app.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `app:update`: Modify MCP Apps, their tools, and their team assignments
 */
export const unassignToolFromApp = <ThrowOnError extends boolean = false>(options: Options<UnassignToolFromAppData, ThrowOnError>) => (options.client ?? client).delete<UnassignToolFromAppResponses, UnassignToolFromAppErrors, ThrowOnError>({ url: '/api/apps/{appId}/tools/{toolId}', ...options });

/**
 * Assign an upstream tool to an app.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `app:update`: Modify MCP Apps, their tools, and their team assignments
 */
export const assignToolToApp = <ThrowOnError extends boolean = false>(options: Options<AssignToolToAppData, ThrowOnError>) => (options.client ?? client).post<AssignToolToAppResponses, AssignToolToAppErrors, ThrowOnError>({
    url: '/api/apps/{appId}/tools/{toolId}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Create a chat completion with Archestra (uses default agent)
 *
 * Authentication:
 *
 * This route accepts either an LLM provider API key or a Virtual API Key. See [LLM Proxy Authentication](/docs/platform-llm-proxy-authentication).
 */
export const archestraChatCompletionsWithDefaultAgent = <ThrowOnError extends boolean = false>(options: Options<ArchestraChatCompletionsWithDefaultAgentData, ThrowOnError>) => (options.client ?? client).post<ArchestraChatCompletionsWithDefaultAgentResponses, ArchestraChatCompletionsWithDefaultAgentErrors, ThrowOnError>({
    url: '/v1/archestra/chat/completions',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Create a chat completion with Archestra for a specific agent
 *
 * Authentication:
 *
 * This route accepts either an LLM provider API key or a Virtual API Key. See [LLM Proxy Authentication](/docs/platform-llm-proxy-authentication).
 */
export const archestraChatCompletionsWithAgent = <ThrowOnError extends boolean = false>(options: Options<ArchestraChatCompletionsWithAgentData, ThrowOnError>) => (options.client ?? client).post<ArchestraChatCompletionsWithAgentResponses, ArchestraChatCompletionsWithAgentErrors, ThrowOnError>({
    url: '/v1/archestra/{agentId}/chat/completions',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get paginated audit log events for the organization. Requires auditLog:read permission (Admin only by default).
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `auditLog:read`: View audit log records of your own administrative actions
 */
export const getAuditLogs = <ThrowOnError extends boolean = false>(options?: Options<GetAuditLogsData, ThrowOnError>) => (options?.client ?? client).get<GetAuditLogsResponses, GetAuditLogsErrors, ThrowOnError>({ url: '/api/audit-logs', ...options });

/**
 * Get a single audit log event by ID. Requires auditLog:read permission (Admin only by default).
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `auditLog:read`: View audit log records of your own administrative actions
 */
export const getAuditLog = <ThrowOnError extends boolean = false>(options: Options<GetAuditLogData, ThrowOnError>) => (options.client ?? client).get<GetAuditLogResponses, GetAuditLogErrors, ThrowOnError>({ url: '/api/audit-logs/{id}', ...options });

/**
 * Report whether a two-factor sign-in challenge is currently pending
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * None (no additional RBAC permission required)
 */
export const getAuthState = <ThrowOnError extends boolean = false>(options?: Options<GetAuthStateData, ThrowOnError>) => (options?.client ?? client).get<GetAuthStateResponses, unknown, ThrowOnError>({ url: '/api/auth-state', ...options });

/**
 * Get default credentials status
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * None (no additional RBAC permission required)
 */
export const getDefaultCredentialsStatus = <ThrowOnError extends boolean = false>(options?: Options<GetDefaultCredentialsStatusData, ThrowOnError>) => (options?.client ?? client).get<GetDefaultCredentialsStatusResponses, GetDefaultCredentialsStatusErrors, ThrowOnError>({ url: '/api/auth/default-credentials-status', ...options });

export const postApiAuthOrganizationRemoveMember = <ThrowOnError extends boolean = false>(options?: Options<PostApiAuthOrganizationRemoveMemberData, ThrowOnError>) => (options?.client ?? client).post<PostApiAuthOrganizationRemoveMemberResponses, unknown, ThrowOnError>({ url: '/api/auth/organization/remove-member', ...options });

/**
 * Get OAuth client name by client_id
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * None (no additional RBAC permission required)
 */
export const getOAuthClientInfo = <ThrowOnError extends boolean = false>(options: Options<GetOAuthClientInfoData, ThrowOnError>) => (options.client ?? client).get<GetOAuthClientInfoResponses, unknown, ThrowOnError>({ url: '/api/auth/oauth2/client-info', ...options });

export const getApiAuthOauth2Authorize = <ThrowOnError extends boolean = false>(options?: Options<GetApiAuthOauth2AuthorizeData, ThrowOnError>) => (options?.client ?? client).get<GetApiAuthOauth2AuthorizeResponses, unknown, ThrowOnError>({ url: '/api/auth/oauth2/authorize', ...options });

export const postApiAuthOauth2Token = <ThrowOnError extends boolean = false>(options?: Options<PostApiAuthOauth2TokenData, ThrowOnError>) => (options?.client ?? client).post<PostApiAuthOauth2TokenResponses, unknown, ThrowOnError>({ url: '/api/auth/oauth2/token', ...options });

export const postApiAuthOauth2Revoke = <ThrowOnError extends boolean = false>(options?: Options<PostApiAuthOauth2RevokeData, ThrowOnError>) => (options?.client ?? client).post<PostApiAuthOauth2RevokeResponses, unknown, ThrowOnError>({ url: '/api/auth/oauth2/revoke', ...options });

/**
 * Submit OAuth consent decision (accept or deny)
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * None (no additional RBAC permission required)
 */
export const submitOAuthConsent = <ThrowOnError extends boolean = false>(options: Options<SubmitOAuthConsentData, ThrowOnError>) => (options.client ?? client).post<SubmitOAuthConsentResponses, SubmitOAuthConsentErrors, ThrowOnError>({
    url: '/api/auth/oauth2/consent',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

export const postApiAuthOauth2Register = <ThrowOnError extends boolean = false>(options: Options<PostApiAuthOauth2RegisterData, ThrowOnError>) => (options.client ?? client).post<PostApiAuthOauth2RegisterResponses, unknown, ThrowOnError>({
    url: '/api/auth/oauth2/register',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

export const postApiAuthSignInSso = <ThrowOnError extends boolean = false>(options?: Options<PostApiAuthSignInSsoData, ThrowOnError>) => (options?.client ?? client).post<PostApiAuthSignInSsoResponses, unknown, ThrowOnError>({ url: '/api/auth/sign-in/sso', ...options });

export const getApiAuthBy__ = <ThrowOnError extends boolean = false>(options: Options<GetApiAuthBy__Data, ThrowOnError>) => (options.client ?? client).get<GetApiAuthBy__Responses, unknown, ThrowOnError>({ url: '/api/auth/{*}', ...options });

export const postApiAuthBy__ = <ThrowOnError extends boolean = false>(options: Options<PostApiAuthBy__Data, ThrowOnError>) => (options.client ?? client).post<PostApiAuthBy__Responses, unknown, ThrowOnError>({ url: '/api/auth/{*}', ...options });

/**
 * Get all supported policy operators
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `toolPolicy:read`: View tools, tool invocation policies, and trusted data policies
 */
export const getOperators = <ThrowOnError extends boolean = false>(options?: Options<GetOperatorsData, ThrowOnError>) => (options?.client ?? client).get<GetOperatorsResponses, GetOperatorsErrors, ThrowOnError>({ url: '/api/autonomy-policies/operators', ...options });

/**
 * Get all tool invocation policies
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `toolPolicy:read`: View tools, tool invocation policies, and trusted data policies
 */
export const getToolInvocationPolicies = <ThrowOnError extends boolean = false>(options?: Options<GetToolInvocationPoliciesData, ThrowOnError>) => (options?.client ?? client).get<GetToolInvocationPoliciesResponses, GetToolInvocationPoliciesErrors, ThrowOnError>({ url: '/api/autonomy-policies/tool-invocation', ...options });

/**
 * Create a new tool invocation policy
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `toolPolicy:create`: Register tools and create security policies
 */
export const createToolInvocationPolicy = <ThrowOnError extends boolean = false>(options: Options<CreateToolInvocationPolicyData, ThrowOnError>) => (options.client ?? client).post<CreateToolInvocationPolicyResponses, CreateToolInvocationPolicyErrors, ThrowOnError>({
    url: '/api/autonomy-policies/tool-invocation',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete a tool invocation policy
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `toolPolicy:delete`: Remove tools and security policies
 */
export const deleteToolInvocationPolicy = <ThrowOnError extends boolean = false>(options: Options<DeleteToolInvocationPolicyData, ThrowOnError>) => (options.client ?? client).delete<DeleteToolInvocationPolicyResponses, DeleteToolInvocationPolicyErrors, ThrowOnError>({ url: '/api/autonomy-policies/tool-invocation/{id}', ...options });

/**
 * Get tool invocation policy by ID
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `toolPolicy:read`: View tools, tool invocation policies, and trusted data policies
 */
export const getToolInvocationPolicy = <ThrowOnError extends boolean = false>(options: Options<GetToolInvocationPolicyData, ThrowOnError>) => (options.client ?? client).get<GetToolInvocationPolicyResponses, GetToolInvocationPolicyErrors, ThrowOnError>({ url: '/api/autonomy-policies/tool-invocation/{id}', ...options });

/**
 * Update a tool invocation policy
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `toolPolicy:update`: Modify tools, tool configuration, and security policies
 */
export const updateToolInvocationPolicy = <ThrowOnError extends boolean = false>(options: Options<UpdateToolInvocationPolicyData, ThrowOnError>) => (options.client ?? client).put<UpdateToolInvocationPolicyResponses, UpdateToolInvocationPolicyErrors, ThrowOnError>({
    url: '/api/autonomy-policies/tool-invocation/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get all trusted data policies
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `toolPolicy:read`: View tools, tool invocation policies, and trusted data policies
 */
export const getTrustedDataPolicies = <ThrowOnError extends boolean = false>(options?: Options<GetTrustedDataPoliciesData, ThrowOnError>) => (options?.client ?? client).get<GetTrustedDataPoliciesResponses, GetTrustedDataPoliciesErrors, ThrowOnError>({ url: '/api/trusted-data-policies', ...options });

/**
 * Create a new trusted data policy
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `toolPolicy:create`: Register tools and create security policies
 */
export const createTrustedDataPolicy = <ThrowOnError extends boolean = false>(options: Options<CreateTrustedDataPolicyData, ThrowOnError>) => (options.client ?? client).post<CreateTrustedDataPolicyResponses, CreateTrustedDataPolicyErrors, ThrowOnError>({
    url: '/api/trusted-data-policies',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete a trusted data policy
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `toolPolicy:delete`: Remove tools and security policies
 */
export const deleteTrustedDataPolicy = <ThrowOnError extends boolean = false>(options: Options<DeleteTrustedDataPolicyData, ThrowOnError>) => (options.client ?? client).delete<DeleteTrustedDataPolicyResponses, DeleteTrustedDataPolicyErrors, ThrowOnError>({ url: '/api/trusted-data-policies/{id}', ...options });

/**
 * Get trusted data policy by ID
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `toolPolicy:read`: View tools, tool invocation policies, and trusted data policies
 */
export const getTrustedDataPolicy = <ThrowOnError extends boolean = false>(options: Options<GetTrustedDataPolicyData, ThrowOnError>) => (options.client ?? client).get<GetTrustedDataPolicyResponses, GetTrustedDataPolicyErrors, ThrowOnError>({ url: '/api/trusted-data-policies/{id}', ...options });

/**
 * Update a trusted data policy
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `toolPolicy:update`: Modify tools, tool configuration, and security policies
 */
export const updateTrustedDataPolicy = <ThrowOnError extends boolean = false>(options: Options<UpdateTrustedDataPolicyData, ThrowOnError>) => (options.client ?? client).put<UpdateTrustedDataPolicyResponses, UpdateTrustedDataPolicyErrors, ThrowOnError>({
    url: '/api/trusted-data-policies/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Bulk upsert default tool invocation policies (empty conditions) for multiple tools
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `toolPolicy:update`: Modify tools, tool configuration, and security policies
 */
export const bulkUpsertDefaultCallPolicy = <ThrowOnError extends boolean = false>(options: Options<BulkUpsertDefaultCallPolicyData, ThrowOnError>) => (options.client ?? client).post<BulkUpsertDefaultCallPolicyResponses, BulkUpsertDefaultCallPolicyErrors, ThrowOnError>({
    url: '/api/tool-invocation/bulk-default',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Bulk upsert default trusted data policies (empty conditions) for multiple tools
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `toolPolicy:update`: Modify tools, tool configuration, and security policies
 */
export const bulkUpsertDefaultResultPolicy = <ThrowOnError extends boolean = false>(options: Options<BulkUpsertDefaultResultPolicyData, ThrowOnError>) => (options.client ?? client).post<BulkUpsertDefaultResultPolicyResponses, BulkUpsertDefaultResultPolicyErrors, ThrowOnError>({
    url: '/api/trusted-data-policies/bulk-default',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Create embeddings with Azure AI Foundry (uses default agent)
 *
 * Authentication:
 *
 * This route accepts either an LLM provider API key or a Virtual API Key. See [LLM Proxy Authentication](/docs/platform-llm-proxy-authentication).
 */
export const azureEmbeddingsWithDefaultAgent = <ThrowOnError extends boolean = false>(options: Options<AzureEmbeddingsWithDefaultAgentData, ThrowOnError>) => (options.client ?? client).post<AzureEmbeddingsWithDefaultAgentResponses, AzureEmbeddingsWithDefaultAgentErrors, ThrowOnError>({
    url: '/v1/azure/embeddings',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Create embeddings with Azure AI Foundry for a specific agent
 *
 * Authentication:
 *
 * This route accepts either an LLM provider API key or a Virtual API Key. See [LLM Proxy Authentication](/docs/platform-llm-proxy-authentication).
 */
export const azureEmbeddingsWithAgent = <ThrowOnError extends boolean = false>(options: Options<AzureEmbeddingsWithAgentData, ThrowOnError>) => (options.client ?? client).post<AzureEmbeddingsWithAgentResponses, AzureEmbeddingsWithAgentErrors, ThrowOnError>({
    url: '/v1/azure/{agentId}/embeddings',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Create a chat completion with Azure AI Foundry (uses default agent)
 *
 * Authentication:
 *
 * This route accepts either an LLM provider API key or a Virtual API Key. See [LLM Proxy Authentication](/docs/platform-llm-proxy-authentication).
 */
export const azureChatCompletionsWithDefaultAgent = <ThrowOnError extends boolean = false>(options: Options<AzureChatCompletionsWithDefaultAgentData, ThrowOnError>) => (options.client ?? client).post<AzureChatCompletionsWithDefaultAgentResponses, AzureChatCompletionsWithDefaultAgentErrors, ThrowOnError>({
    url: '/v1/azure/chat/completions',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Create a response with Azure AI Foundry (uses default agent)
 *
 * Authentication:
 *
 * This route accepts either an LLM provider API key or a Virtual API Key. See [LLM Proxy Authentication](/docs/platform-llm-proxy-authentication).
 */
export const azureResponsesWithDefaultAgent = <ThrowOnError extends boolean = false>(options: Options<AzureResponsesWithDefaultAgentData, ThrowOnError>) => (options.client ?? client).post<AzureResponsesWithDefaultAgentResponses, AzureResponsesWithDefaultAgentErrors, ThrowOnError>({
    url: '/v1/azure/responses',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Create a response with Azure AI Foundry for a specific agent
 *
 * Authentication:
 *
 * This route accepts either an LLM provider API key or a Virtual API Key. See [LLM Proxy Authentication](/docs/platform-llm-proxy-authentication).
 */
export const azureResponsesWithAgent = <ThrowOnError extends boolean = false>(options: Options<AzureResponsesWithAgentData, ThrowOnError>) => (options.client ?? client).post<AzureResponsesWithAgentResponses, AzureResponsesWithAgentErrors, ThrowOnError>({
    url: '/v1/azure/{agentId}/responses',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Create a chat completion with Azure AI Foundry for a specific agent
 *
 * Authentication:
 *
 * This route accepts either an LLM provider API key or a Virtual API Key. See [LLM Proxy Authentication](/docs/platform-llm-proxy-authentication).
 */
export const azureChatCompletionsWithAgent = <ThrowOnError extends boolean = false>(options: Options<AzureChatCompletionsWithAgentData, ThrowOnError>) => (options.client ?? client).post<AzureChatCompletionsWithAgentResponses, AzureChatCompletionsWithAgentErrors, ThrowOnError>({
    url: '/v1/azure/{agentId}/chat/completions',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Call Amazon Bedrock models using the OpenAI ChatCompletions wire format (default agent)
 *
 * Authentication:
 *
 * This route accepts either an LLM provider API key or a Virtual API Key. See [LLM Proxy Authentication](/docs/platform-llm-proxy-authentication).
 */
export const bedrockOpenaiChatCompletionsWithDefaultAgent = <ThrowOnError extends boolean = false>(options: Options<BedrockOpenaiChatCompletionsWithDefaultAgentData, ThrowOnError>) => (options.client ?? client).post<BedrockOpenaiChatCompletionsWithDefaultAgentResponses, unknown, ThrowOnError>({
    url: '/v1/bedrock/openai/chat/completions',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Call Amazon Bedrock models using the OpenAI ChatCompletions wire format (specific agent)
 *
 * Authentication:
 *
 * This route accepts either an LLM provider API key or a Virtual API Key. See [LLM Proxy Authentication](/docs/platform-llm-proxy-authentication).
 */
export const bedrockOpenaiChatCompletionsWithAgent = <ThrowOnError extends boolean = false>(options: Options<BedrockOpenaiChatCompletionsWithAgentData, ThrowOnError>) => (options.client ?? client).post<BedrockOpenaiChatCompletionsWithAgentResponses, unknown, ThrowOnError>({
    url: '/v1/bedrock/openai/{agentId}/chat/completions',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List Amazon Bedrock models (via ListInferenceProfiles) in OpenAI models-list format
 *
 * Authentication:
 *
 * This route accepts either an LLM provider API key or a Virtual API Key. See [LLM Proxy Authentication](/docs/platform-llm-proxy-authentication).
 */
export const bedrockOpenaiListModelsWithDefaultAgent = <ThrowOnError extends boolean = false>(options?: Options<BedrockOpenaiListModelsWithDefaultAgentData, ThrowOnError>) => (options?.client ?? client).get<BedrockOpenaiListModelsWithDefaultAgentResponses, BedrockOpenaiListModelsWithDefaultAgentErrors, ThrowOnError>({ url: '/v1/bedrock/openai/models', ...options });

/**
 * List Amazon Bedrock models (via ListInferenceProfiles) in OpenAI models-list format (specific agent)
 *
 * Authentication:
 *
 * This route accepts either an LLM provider API key or a Virtual API Key. See [LLM Proxy Authentication](/docs/platform-llm-proxy-authentication).
 */
export const bedrockOpenaiListModelsWithAgent = <ThrowOnError extends boolean = false>(options: Options<BedrockOpenaiListModelsWithAgentData, ThrowOnError>) => (options.client ?? client).get<BedrockOpenaiListModelsWithAgentResponses, BedrockOpenaiListModelsWithAgentErrors, ThrowOnError>({ url: '/v1/bedrock/openai/{agentId}/models', ...options });

/**
 * Send a message to Amazon Bedrock using the default agent
 *
 * Authentication:
 *
 * This route accepts either an LLM provider API key or a Virtual API Key. See [LLM Proxy Authentication](/docs/platform-llm-proxy-authentication).
 */
export const bedrockConverseWithDefaultAgent = <ThrowOnError extends boolean = false>(options: Options<BedrockConverseWithDefaultAgentData, ThrowOnError>) => (options.client ?? client).post<BedrockConverseWithDefaultAgentResponses, BedrockConverseWithDefaultAgentErrors, ThrowOnError>({
    url: '/v1/bedrock/converse',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Send a message to Amazon Bedrock for a specific agent
 *
 * Authentication:
 *
 * This route accepts either an LLM provider API key or a Virtual API Key. See [LLM Proxy Authentication](/docs/platform-llm-proxy-authentication).
 */
export const bedrockConverseWithAgent = <ThrowOnError extends boolean = false>(options: Options<BedrockConverseWithAgentData, ThrowOnError>) => (options.client ?? client).post<BedrockConverseWithAgentResponses, BedrockConverseWithAgentErrors, ThrowOnError>({
    url: '/v1/bedrock/{agentId}/converse',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Stream a message response from Amazon Bedrock using the default agent
 *
 * Authentication:
 *
 * This route accepts either an LLM provider API key or a Virtual API Key. See [LLM Proxy Authentication](/docs/platform-llm-proxy-authentication).
 */
export const bedrockConverseStreamWithDefaultAgent = <ThrowOnError extends boolean = false>(options: Options<BedrockConverseStreamWithDefaultAgentData, ThrowOnError>) => (options.client ?? client).post<BedrockConverseStreamWithDefaultAgentResponses, unknown, ThrowOnError>({
    url: '/v1/bedrock/converse-stream',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Stream a message response from Amazon Bedrock for a specific agent
 *
 * Authentication:
 *
 * This route accepts either an LLM provider API key or a Virtual API Key. See [LLM Proxy Authentication](/docs/platform-llm-proxy-authentication).
 */
export const bedrockConverseStreamWithAgent = <ThrowOnError extends boolean = false>(options: Options<BedrockConverseStreamWithAgentData, ThrowOnError>) => (options.client ?? client).post<BedrockConverseStreamWithAgentResponses, unknown, ThrowOnError>({
    url: '/v1/bedrock/{agentId}/converse-stream',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Send a message to Amazon Bedrock for a specific agent (AI SDK format)
 *
 * Authentication:
 *
 * This route accepts either an LLM provider API key or a Virtual API Key. See [LLM Proxy Authentication](/docs/platform-llm-proxy-authentication).
 */
export const bedrockConverseWithAgentAndModel = <ThrowOnError extends boolean = false>(options: Options<BedrockConverseWithAgentAndModelData, ThrowOnError>) => (options.client ?? client).post<BedrockConverseWithAgentAndModelResponses, BedrockConverseWithAgentAndModelErrors, ThrowOnError>({
    url: '/v1/bedrock/{agentId}/model/{modelId}/converse',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Stream a message response from Amazon Bedrock for a specific agent (AI SDK format)
 *
 * Authentication:
 *
 * This route accepts either an LLM provider API key or a Virtual API Key. See [LLM Proxy Authentication](/docs/platform-llm-proxy-authentication).
 */
export const bedrockConverseStreamWithAgentAndModel = <ThrowOnError extends boolean = false>(options: Options<BedrockConverseStreamWithAgentAndModelData, ThrowOnError>) => (options.client ?? client).post<BedrockConverseStreamWithAgentAndModelResponses, unknown, ThrowOnError>({
    url: '/v1/bedrock/{agentId}/model/{modelId}/converse-stream',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Invoke an Anthropic model on Amazon Bedrock (Anthropic Messages format, default agent)
 *
 * Authentication:
 *
 * This route accepts either an LLM provider API key or a Virtual API Key. See [LLM Proxy Authentication](/docs/platform-llm-proxy-authentication).
 */
export const bedrockInvokeWithDefaultAgentAndModel = <ThrowOnError extends boolean = false>(options: Options<BedrockInvokeWithDefaultAgentAndModelData, ThrowOnError>) => (options.client ?? client).post<BedrockInvokeWithDefaultAgentAndModelResponses, BedrockInvokeWithDefaultAgentAndModelErrors, ThrowOnError>({
    url: '/v1/bedrock/model/{modelId}/invoke',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Invoke an Anthropic model on Amazon Bedrock (Anthropic Messages format) for a specific agent
 *
 * Authentication:
 *
 * This route accepts either an LLM provider API key or a Virtual API Key. See [LLM Proxy Authentication](/docs/platform-llm-proxy-authentication).
 */
export const bedrockInvokeWithAgentAndModel = <ThrowOnError extends boolean = false>(options: Options<BedrockInvokeWithAgentAndModelData, ThrowOnError>) => (options.client ?? client).post<BedrockInvokeWithAgentAndModelResponses, BedrockInvokeWithAgentAndModelErrors, ThrowOnError>({
    url: '/v1/bedrock/{agentId}/model/{modelId}/invoke',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Stream an Anthropic model response from Amazon Bedrock (Anthropic Messages format, default agent)
 *
 * Authentication:
 *
 * This route accepts either an LLM provider API key or a Virtual API Key. See [LLM Proxy Authentication](/docs/platform-llm-proxy-authentication).
 */
export const bedrockInvokeStreamWithDefaultAgentAndModel = <ThrowOnError extends boolean = false>(options: Options<BedrockInvokeStreamWithDefaultAgentAndModelData, ThrowOnError>) => (options.client ?? client).post<BedrockInvokeStreamWithDefaultAgentAndModelResponses, unknown, ThrowOnError>({
    url: '/v1/bedrock/model/{modelId}/invoke-with-response-stream',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Stream an Anthropic model response from Amazon Bedrock (Anthropic Messages format) for a specific agent
 *
 * Authentication:
 *
 * This route accepts either an LLM provider API key or a Virtual API Key. See [LLM Proxy Authentication](/docs/platform-llm-proxy-authentication).
 */
export const bedrockInvokeStreamWithAgentAndModel = <ThrowOnError extends boolean = false>(options: Options<BedrockInvokeStreamWithAgentAndModelData, ThrowOnError>) => (options.client ?? client).post<BedrockInvokeStreamWithAgentAndModelResponses, unknown, ThrowOnError>({
    url: '/v1/bedrock/{agentId}/model/{modelId}/invoke-with-response-stream',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Create a chat completion with Cerebras (uses default agent)
 *
 * Authentication:
 *
 * This route accepts either an LLM provider API key or a Virtual API Key. See [LLM Proxy Authentication](/docs/platform-llm-proxy-authentication).
 */
export const cerebrasChatCompletionsWithDefaultAgent = <ThrowOnError extends boolean = false>(options: Options<CerebrasChatCompletionsWithDefaultAgentData, ThrowOnError>) => (options.client ?? client).post<CerebrasChatCompletionsWithDefaultAgentResponses, CerebrasChatCompletionsWithDefaultAgentErrors, ThrowOnError>({
    url: '/v1/cerebras/chat/completions',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Create a chat completion with Cerebras for a specific agent
 *
 * Authentication:
 *
 * This route accepts either an LLM provider API key or a Virtual API Key. See [LLM Proxy Authentication](/docs/platform-llm-proxy-authentication).
 */
export const cerebrasChatCompletionsWithAgent = <ThrowOnError extends boolean = false>(options: Options<CerebrasChatCompletionsWithAgentData, ThrowOnError>) => (options.client ?? client).post<CerebrasChatCompletionsWithAgentResponses, CerebrasChatCompletionsWithAgentErrors, ThrowOnError>({
    url: '/v1/cerebras/{agentId}/chat/completions',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Stream chat response with MCP tools (useChat format)
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `chat:read`: View and access chat conversations
 */
export const streamChat = <ThrowOnError extends boolean = false>(options: Options<StreamChatData, ThrowOnError>) => (options.client ?? client).post<unknown, StreamChatErrors, ThrowOnError>({
    url: '/api/chat',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Resolve a pending MCP elicitation request from chat
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `chat:read`: View and access chat conversations
 */
export const resolveChatMcpElicitation = <ThrowOnError extends boolean = false>(options: Options<ResolveChatMcpElicitationData, ThrowOnError>) => (options.client ?? client).post<ResolveChatMcpElicitationResponses, ResolveChatMcpElicitationErrors, ThrowOnError>({
    url: '/api/chat/elicitation/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Cancel a long-running MCP task started from chat
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `chat:read`: View and access chat conversations
 */
export const cancelChatMcpTask = <ThrowOnError extends boolean = false>(options: Options<CancelChatMcpTaskData, ThrowOnError>) => (options.client ?? client).post<CancelChatMcpTaskResponses, CancelChatMcpTaskErrors, ThrowOnError>({ url: '/api/chat/tasks/{taskId}/cancel', ...options });

/**
 * Stop a running chat stream for a conversation
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `chat:read`: View and access chat conversations
 */
export const stopChatStream = <ThrowOnError extends boolean = false>(options: Options<StopChatStreamData, ThrowOnError>) => (options.client ?? client).post<StopChatStreamResponses, StopChatStreamErrors, ThrowOnError>({ url: '/api/chat/conversations/{id}/stop', ...options });

/**
 * Reconnect to an active chat stream for a conversation
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `chat:read`: View and access chat conversations
 */
export const getActiveChatRun = <ThrowOnError extends boolean = false>(options: Options<GetActiveChatRunData, ThrowOnError>) => (options.client ?? client).get<GetActiveChatRunResponses, GetActiveChatRunErrors, ThrowOnError>({ url: '/api/chat/conversations/{id}/active-run', ...options });

/**
 * List all conversations for current user with agent details. Optionally filter by search query.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `chat:read`: View and access chat conversations
 */
export const getChatConversations = <ThrowOnError extends boolean = false>(options?: Options<GetChatConversationsData, ThrowOnError>) => (options?.client ?? client).get<GetChatConversationsResponses, GetChatConversationsErrors, ThrowOnError>({ url: '/api/chat/conversations', ...options });

/**
 * Create a new conversation with an agent
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `chat:create`: Start new chat conversations
 */
export const createChatConversation = <ThrowOnError extends boolean = false>(options: Options<CreateChatConversationData, ThrowOnError>) => (options.client ?? client).post<CreateChatConversationResponses, CreateChatConversationErrors, ThrowOnError>({
    url: '/api/chat/conversations',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List the current user's soft-deleted conversations (the Trash view), newest deletion first.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `chat:delete`: Delete chat conversations
 */
export const getDeletedChatConversations = <ThrowOnError extends boolean = false>(options?: Options<GetDeletedChatConversationsData, ThrowOnError>) => (options?.client ?? client).get<GetDeletedChatConversationsResponses, GetDeletedChatConversationsErrors, ThrowOnError>({ url: '/api/chat/conversations/deleted', ...options });

/**
 * Delete a conversation
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `chat:delete`: Delete chat conversations
 */
export const deleteChatConversation = <ThrowOnError extends boolean = false>(options: Options<DeleteChatConversationData, ThrowOnError>) => (options.client ?? client).delete<DeleteChatConversationResponses, DeleteChatConversationErrors, ThrowOnError>({ url: '/api/chat/conversations/{id}', ...options });

/**
 * Get conversation with messages
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `chat:read`: View and access chat conversations
 */
export const getChatConversation = <ThrowOnError extends boolean = false>(options: Options<GetChatConversationData, ThrowOnError>) => (options.client ?? client).get<GetChatConversationResponses, GetChatConversationErrors, ThrowOnError>({ url: '/api/chat/conversations/{id}', ...options });

/**
 * Update conversation title, model, agent, API key, or project
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `chat:update`: Edit chat messages and conversation settings
 */
export const updateChatConversation = <ThrowOnError extends boolean = false>(options: Options<UpdateChatConversationData, ThrowOnError>) => (options.client ?? client).patch<UpdateChatConversationResponses, UpdateChatConversationErrors, ThrowOnError>({
    url: '/api/chat/conversations/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Toggle per-conversation hook debug mode (admin only). When on, hook runs surface inline as expandable debug chips for admins.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `chat:update`: Edit chat messages and conversation settings
 */
export const setConversationHooksDebug = <ThrowOnError extends boolean = false>(options: Options<SetConversationHooksDebugData, ThrowOnError>) => (options.client ?? client).post<SetConversationHooksDebugResponses, SetConversationHooksDebugErrors, ThrowOnError>({
    url: '/api/chat/conversations/{id}/hooks-debug',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Mark a conversation read by its owner, clearing the sidebar new-messages indicator.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `chat:update`: Edit chat messages and conversation settings
 */
export const markChatConversationRead = <ThrowOnError extends boolean = false>(options: Options<MarkChatConversationReadData, ThrowOnError>) => (options.client ?? client).post<MarkChatConversationReadResponses, MarkChatConversationReadErrors, ThrowOnError>({ url: '/api/chat/conversations/{id}/read', ...options });

/**
 * List files for a conversation: this chat's own outputs, user attachments, and — for a project chat — every file in the project (metadata only).
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `chat:read`: View and access chat conversations
 */
export const getChatConversationFiles = <ThrowOnError extends boolean = false>(options: Options<GetChatConversationFilesData, ThrowOnError>) => (options.client ?? client).get<GetChatConversationFilesResponses, GetChatConversationFilesErrors, ThrowOnError>({ url: '/api/chat/conversations/{id}/files', ...options });

/**
 * Stream the bytes of a chat attachment by id. Auth'd to the org.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `chat:read`: View and access chat conversations
 */
export const getChatAttachmentContent = <ThrowOnError extends boolean = false>(options: Options<GetChatAttachmentContentData, ThrowOnError>) => (options.client ?? client).get<unknown, GetChatAttachmentContentErrors, ThrowOnError>({ url: '/api/chat/attachments/{id}/content', ...options });

/**
 * Soft-delete a chat attachment by id. Owner-gated: only the conversation owner may remove its attachments.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `chat:update`: Edit chat messages and conversation settings
 */
export const deleteChatAttachment = <ThrowOnError extends boolean = false>(options: Options<DeleteChatAttachmentData, ThrowOnError>) => (options.client ?? client).delete<DeleteChatAttachmentResponses, DeleteChatAttachmentErrors, ThrowOnError>({ url: '/api/chat/attachments/{id}', ...options });

/**
 * Create a new conversation from an accessible conversation
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `chat:create`: Start new chat conversations
 */
export const forkChatConversation = <ThrowOnError extends boolean = false>(options: Options<ForkChatConversationData, ThrowOnError>) => (options.client ?? client).post<ForkChatConversationResponses, ForkChatConversationErrors, ThrowOnError>({
    url: '/api/chat/conversations/{id}/fork',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get MCP tools available for an agent via MCP Gateway
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `agent:read`: View and list agents
 */
export const getChatAgentMcpTools = <ThrowOnError extends boolean = false>(options: Options<GetChatAgentMcpToolsData, ThrowOnError>) => (options.client ?? client).get<GetChatAgentMcpToolsResponses, GetChatAgentMcpToolsErrors, ThrowOnError>({ url: '/api/chat/agents/{agentId}/mcp-tools', ...options });

/**
 * Restore a soft-deleted conversation
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `chat:delete`: Delete chat conversations
 */
export const restoreChatConversation = <ThrowOnError extends boolean = false>(options: Options<RestoreChatConversationData, ThrowOnError>) => (options.client ?? client).post<RestoreChatConversationResponses, RestoreChatConversationErrors, ThrowOnError>({ url: '/api/chat/conversations/{id}/restore', ...options });

/**
 * Clear a conversation's recorded chat errors
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `chat:update`: Edit chat messages and conversation settings
 */
export const clearChatConversationErrors = <ThrowOnError extends boolean = false>(options: Options<ClearChatConversationErrorsData, ThrowOnError>) => (options.client ?? client).delete<ClearChatConversationErrorsResponses, ClearChatConversationErrorsErrors, ThrowOnError>({ url: '/api/chat/conversations/{id}/chat-errors', ...options });

/**
 * Compact older chat history for model context
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `chat:update`: Edit chat messages and conversation settings
 */
export const compactChatConversation = <ThrowOnError extends boolean = false>(options: Options<CompactChatConversationData, ThrowOnError>) => (options.client ?? client).post<CompactChatConversationResponses, CompactChatConversationErrors, ThrowOnError>({ url: '/api/chat/conversations/{id}/compact', ...options });

/**
 * Revoke sharing of a conversation
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `chat:update`: Edit chat messages and conversation settings
 */
export const unshareConversation = <ThrowOnError extends boolean = false>(options: Options<UnshareConversationData, ThrowOnError>) => (options.client ?? client).delete<UnshareConversationResponses, UnshareConversationErrors, ThrowOnError>({ url: '/api/chat/conversations/{id}/share', ...options });

/**
 * Get share status for a conversation
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `chat:read`: View and access chat conversations
 */
export const getConversationShare = <ThrowOnError extends boolean = false>(options: Options<GetConversationShareData, ThrowOnError>) => (options.client ?? client).get<GetConversationShareResponses, GetConversationShareErrors, ThrowOnError>({ url: '/api/chat/conversations/{id}/share', ...options });

/**
 * Share a conversation with your organization, specific teams, or specific users
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `chat:update`: Edit chat messages and conversation settings
 */
export const shareConversation = <ThrowOnError extends boolean = false>(options: Options<ShareConversationData, ThrowOnError>) => (options.client ?? client).post<ShareConversationResponses, ShareConversationErrors, ThrowOnError>({
    url: '/api/chat/conversations/{id}/share',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get a shared conversation by share ID
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `chat:read`: View and access chat conversations
 */
export const getSharedConversation = <ThrowOnError extends boolean = false>(options: Options<GetSharedConversationData, ThrowOnError>) => (options.client ?? client).get<GetSharedConversationResponses, GetSharedConversationErrors, ThrowOnError>({ url: '/api/chat/shared/{shareId}', ...options });

/**
 * Create a new conversation from a shared conversation's messages
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `chat:create`: Start new chat conversations
 */
export const forkSharedConversation = <ThrowOnError extends boolean = false>(options: Options<ForkSharedConversationData, ThrowOnError>) => (options.client ?? client).post<ForkSharedConversationResponses, ForkSharedConversationErrors, ThrowOnError>({
    url: '/api/chat/shared/{shareId}/fork',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Generate a title for the conversation based on the first user message and assistant response
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `chat:update`: Edit chat messages and conversation settings
 */
export const generateChatConversationTitle = <ThrowOnError extends boolean = false>(options: Options<GenerateChatConversationTitleData, ThrowOnError>) => (options.client ?? client).post<GenerateChatConversationTitleResponses, GenerateChatConversationTitleErrors, ThrowOnError>({
    url: '/api/chat/conversations/{id}/generate-title',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Update a specific text part in a message
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `chat:update`: Edit chat messages and conversation settings
 */
export const updateChatMessage = <ThrowOnError extends boolean = false>(options: Options<UpdateChatMessageData, ThrowOnError>) => (options.client ?? client).patch<UpdateChatMessageResponses, UpdateChatMessageErrors, ThrowOnError>({
    url: '/api/chat/messages/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Set or clear the owner's thumbs feedback on an assistant message
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `chat:update`: Edit chat messages and conversation settings
 */
export const setChatMessageFeedback = <ThrowOnError extends boolean = false>(options: Options<SetChatMessageFeedbackData, ThrowOnError>) => (options.client ?? client).patch<SetChatMessageFeedbackResponses, SetChatMessageFeedbackErrors, ThrowOnError>({
    url: '/api/chat/messages/{id}/feedback',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Clear custom tool selection for a conversation (revert to all tools enabled)
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `chat:update`: Edit chat messages and conversation settings
 */
export const deleteConversationEnabledTools = <ThrowOnError extends boolean = false>(options: Options<DeleteConversationEnabledToolsData, ThrowOnError>) => (options.client ?? client).delete<DeleteConversationEnabledToolsResponses, DeleteConversationEnabledToolsErrors, ThrowOnError>({ url: '/api/chat/conversations/{id}/enabled-tools', ...options });

/**
 * Get enabled tools for a conversation. Empty array means all profile tools are enabled (default).
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `chat:read`: View and access chat conversations
 */
export const getConversationEnabledTools = <ThrowOnError extends boolean = false>(options: Options<GetConversationEnabledToolsData, ThrowOnError>) => (options.client ?? client).get<GetConversationEnabledToolsResponses, GetConversationEnabledToolsErrors, ThrowOnError>({ url: '/api/chat/conversations/{id}/enabled-tools', ...options });

/**
 * Set enabled tools for a conversation. Replaces all existing selections.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `chat:update`: Edit chat messages and conversation settings
 */
export const updateConversationEnabledTools = <ThrowOnError extends boolean = false>(options: Options<UpdateConversationEnabledToolsData, ThrowOnError>) => (options.client ?? client).put<UpdateConversationEnabledToolsResponses, UpdateConversationEnabledToolsErrors, ThrowOnError>({
    url: '/api/chat/conversations/{id}/enabled-tools',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * MS Teams Bot Framework webhook endpoint
 */
export const postApiWebhooksChatopsMsTeams = <ThrowOnError extends boolean = false>(options: Options<PostApiWebhooksChatopsMsTeamsData, ThrowOnError>) => (options.client ?? client).post<PostApiWebhooksChatopsMsTeamsResponses, PostApiWebhooksChatopsMsTeamsErrors, ThrowOnError>({
    url: '/api/webhooks/chatops/ms-teams',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Slack Events API webhook endpoint
 */
export const postApiWebhooksChatopsSlack = <ThrowOnError extends boolean = false>(options: Options<PostApiWebhooksChatopsSlackData, ThrowOnError>) => (options.client ?? client).post<PostApiWebhooksChatopsSlackResponses, PostApiWebhooksChatopsSlackErrors, ThrowOnError>({
    url: '/api/webhooks/chatops/slack',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Slack interactive components endpoint
 */
export const postApiWebhooksChatopsSlackInteractive = <ThrowOnError extends boolean = false>(options: Options<PostApiWebhooksChatopsSlackInteractiveData, ThrowOnError>) => (options.client ?? client).post<PostApiWebhooksChatopsSlackInteractiveResponses, PostApiWebhooksChatopsSlackInteractiveErrors, ThrowOnError>({
    url: '/api/webhooks/chatops/slack/interactive',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Slack slash commands endpoint
 */
export const postApiWebhooksChatopsSlackSlashCommand = <ThrowOnError extends boolean = false>(options: Options<PostApiWebhooksChatopsSlackSlashCommandData, ThrowOnError>) => (options.client ?? client).post<PostApiWebhooksChatopsSlackSlashCommandResponses, PostApiWebhooksChatopsSlackSlashCommandErrors, ThrowOnError>({
    url: '/api/webhooks/chatops/slack/slash-command',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get chatops provider configuration status
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `agentTrigger:read`: View agent trigger configurations (Slack, MS Teams, email)
 */
export const getChatOpsStatus = <ThrowOnError extends boolean = false>(options?: Options<GetChatOpsStatusData, ThrowOnError>) => (options?.client ?? client).get<GetChatOpsStatusResponses, GetChatOpsStatusErrors, ThrowOnError>({ url: '/api/chatops/status', ...options });

/**
 * List chatops channel bindings with pagination
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `agentTrigger:read`: View agent trigger configurations (Slack, MS Teams, email)
 */
export const listChatOpsBindings = <ThrowOnError extends boolean = false>(options?: Options<ListChatOpsBindingsData, ThrowOnError>) => (options?.client ?? client).get<ListChatOpsBindingsResponses, ListChatOpsBindingsErrors, ThrowOnError>({ url: '/api/chatops/bindings', ...options });

/**
 * Bulk-update agent assignment for multiple channel bindings
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `agentTrigger:update`: Modify agent trigger configurations
 */
export const bulkUpdateChatOpsBindings = <ThrowOnError extends boolean = false>(options: Options<BulkUpdateChatOpsBindingsData, ThrowOnError>) => (options.client ?? client).patch<BulkUpdateChatOpsBindingsResponses, BulkUpdateChatOpsBindingsErrors, ThrowOnError>({
    url: '/api/chatops/bindings',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete a chatops channel binding
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `agentTrigger:delete`: Remove agent triggers
 */
export const deleteChatOpsBinding = <ThrowOnError extends boolean = false>(options: Options<DeleteChatOpsBindingData, ThrowOnError>) => (options.client ?? client).delete<DeleteChatOpsBindingResponses, DeleteChatOpsBindingErrors, ThrowOnError>({ url: '/api/chatops/bindings/{id}', ...options });

/**
 * Update a chatops channel binding
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `agentTrigger:update`: Modify agent trigger configurations
 */
export const updateChatOpsBinding = <ThrowOnError extends boolean = false>(options: Options<UpdateChatOpsBindingData, ThrowOnError>) => (options.client ?? client).patch<UpdateChatOpsBindingResponses, UpdateChatOpsBindingErrors, ThrowOnError>({
    url: '/api/chatops/bindings/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Create a pending DM binding so an agent can be pre-assigned before the first DM interaction
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `agentTrigger:create`: Set up new agent triggers
 */
export const createChatOpsDmBinding = <ThrowOnError extends boolean = false>(options: Options<CreateChatOpsDmBindingData, ThrowOnError>) => (options.client ?? client).post<CreateChatOpsDmBindingResponses, CreateChatOpsDmBindingErrors, ThrowOnError>({
    url: '/api/chatops/bindings/dm',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Update MS Teams chatops configuration
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `agentTrigger:update`: Modify agent trigger configurations
 */
export const updateChatOpsConfigInQuickstart = <ThrowOnError extends boolean = false>(options: Options<UpdateChatOpsConfigInQuickstartData, ThrowOnError>) => (options.client ?? client).put<UpdateChatOpsConfigInQuickstartResponses, UpdateChatOpsConfigInQuickstartErrors, ThrowOnError>({
    url: '/api/chatops/config/ms-teams',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Stop the ngrok tunnel and clear its credentials
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `agentTrigger:update`: Modify agent trigger configurations
 */
export const disconnectNgrok = <ThrowOnError extends boolean = false>(options?: Options<DisconnectNgrokData, ThrowOnError>) => (options?.client ?? client).delete<DisconnectNgrokResponses, DisconnectNgrokErrors, ThrowOnError>({ url: '/api/chatops/config/ngrok', ...options });

/**
 * Get saved ngrok configuration (token redacted)
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `agentTrigger:read`: View agent trigger configurations (Slack, MS Teams, email)
 */
export const getNgrokConfig = <ThrowOnError extends boolean = false>(options?: Options<GetNgrokConfigData, ThrowOnError>) => (options?.client ?? client).get<GetNgrokConfigResponses, GetNgrokConfigErrors, ThrowOnError>({ url: '/api/chatops/config/ngrok', ...options });

/**
 * Connect an ngrok tunnel for inbound chatops webhooks
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `agentTrigger:update`: Modify agent trigger configurations
 */
export const connectNgrok = <ThrowOnError extends boolean = false>(options: Options<ConnectNgrokData, ThrowOnError>) => (options.client ?? client).put<ConnectNgrokResponses, ConnectNgrokErrors, ThrowOnError>({
    url: '/api/chatops/config/ngrok',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Update Slack chatops configuration
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `agentTrigger:update`: Modify agent trigger configurations
 */
export const updateSlackChatOpsConfig = <ThrowOnError extends boolean = false>(options: Options<UpdateSlackChatOpsConfigData, ThrowOnError>) => (options.client ?? client).put<UpdateSlackChatOpsConfigResponses, UpdateSlackChatOpsConfigErrors, ThrowOnError>({
    url: '/api/chatops/config/slack',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Update Telegram chatops configuration
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `agentTrigger:update`: Modify agent trigger configurations
 */
export const updateTelegramChatOpsConfig = <ThrowOnError extends boolean = false>(options: Options<UpdateTelegramChatOpsConfigData, ThrowOnError>) => (options.client ?? client).put<UpdateTelegramChatOpsConfigResponses, UpdateTelegramChatOpsConfigErrors, ThrowOnError>({
    url: '/api/chatops/config/telegram',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Generate a one-shot code that links the current user's Telegram account via a t.me deep link
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * None (no additional RBAC permission required)
 */
export const generateTelegramLinkCode = <ThrowOnError extends boolean = false>(options?: Options<GenerateTelegramLinkCodeData, ThrowOnError>) => (options?.client ?? client).post<GenerateTelegramLinkCodeResponses, GenerateTelegramLinkCodeErrors, ThrowOnError>({ url: '/api/chatops/telegram/link-code', ...options });

/**
 * Link the current user's Telegram account using a code from the bot
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * None (no additional RBAC permission required)
 */
export const linkTelegramChatOpsAccount = <ThrowOnError extends boolean = false>(options: Options<LinkTelegramChatOpsAccountData, ThrowOnError>) => (options.client ?? client).post<LinkTelegramChatOpsAccountResponses, LinkTelegramChatOpsAccountErrors, ThrowOnError>({
    url: '/api/chatops/telegram/link',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Refresh channel discovery cache for a chatops provider
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `agentTrigger:read`: View agent trigger configurations (Slack, MS Teams, email)
 */
export const refreshChatOpsChannelDiscovery = <ThrowOnError extends boolean = false>(options: Options<RefreshChatOpsChannelDiscoveryData, ThrowOnError>) => (options.client ?? client).post<RefreshChatOpsChannelDiscoveryResponses, RefreshChatOpsChannelDiscoveryErrors, ThrowOnError>({
    url: '/api/chatops/channel-discovery/refresh',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Send a chat request to Cohere using the default agent
 *
 * Authentication:
 *
 * This route accepts either an LLM provider API key or a Virtual API Key. See [LLM Proxy Authentication](/docs/platform-llm-proxy-authentication).
 */
export const cohereChatWithDefaultAgent = <ThrowOnError extends boolean = false>(options: Options<CohereChatWithDefaultAgentData, ThrowOnError>) => (options.client ?? client).post<CohereChatWithDefaultAgentResponses, CohereChatWithDefaultAgentErrors, ThrowOnError>({
    url: '/v1/cohere/chat',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Send a chat request to Cohere using a specific agent
 *
 * Authentication:
 *
 * This route accepts either an LLM provider API key or a Virtual API Key. See [LLM Proxy Authentication](/docs/platform-llm-proxy-authentication).
 */
export const cohereChatWithAgent = <ThrowOnError extends boolean = false>(options: Options<CohereChatWithAgentData, ThrowOnError>) => (options.client ?? client).post<CohereChatWithAgentResponses, CohereChatWithAgentErrors, ThrowOnError>({
    url: '/v1/cohere/{agentId}/chat',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get public config
 *
 * Authentication:
 *
 * Not required.
 *
 * Authorization:
 *
 * None (no additional RBAC permission required)
 */
export const getPublicConfig = <ThrowOnError extends boolean = false>(options?: Options<GetPublicConfigData, ThrowOnError>) => (options?.client ?? client).get<GetPublicConfigResponses, unknown, ThrowOnError>({ url: '/api/config/public', ...options });

/**
 * Get platform configuration and feature flags
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * None (no additional RBAC permission required)
 */
export const getConfig = <ThrowOnError extends boolean = false>(options?: Options<GetConfigData, ThrowOnError>) => (options?.client ?? client).get<GetConfigResponses, unknown, ThrowOnError>({ url: '/api/config', ...options });

/**
 * Health of the remotes a connected client is wired to, one request for all of them. Public: used by the Claude Code startup guard before every launch, from machines with no session. Reports only ok/down per requested remote.
 */
export const getConnectionHealth = <ThrowOnError extends boolean = false>(options?: Options<GetConnectionHealthData, ThrowOnError>) => (options?.client ?? client).get<GetConnectionHealthResponses, GetConnectionHealthErrors, ThrowOnError>({ url: '/v1/health', ...options });

/**
 * Persist /connection wizard selections and return a one-time `curl | bash` command. The command's setup token is shown exactly once and expires after 15 minutes.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * None (no additional RBAC permission required)
 */
export const createConnectionSetup = <ThrowOnError extends boolean = false>(options: Options<CreateConnectionSetupData, ThrowOnError>) => (options.client ?? client).post<CreateConnectionSetupResponses, CreateConnectionSetupErrors, ThrowOnError>({
    url: '/api/connection-setups',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Provision (or reuse) the caller's personal connection virtual key for a provider and return its value once. Backs the manual /connection flow's virtual-key option; mirrors the auto-provisioning done by the one-command setup. Requires llmVirtualKey:create.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * None (no additional RBAC permission required)
 */
export const createConnectionVirtualKey = <ThrowOnError extends boolean = false>(options: Options<CreateConnectionVirtualKeyData, ThrowOnError>) => (options.client ?? client).post<CreateConnectionVirtualKeyResponses, CreateConnectionVirtualKeyErrors, ThrowOnError>({
    url: '/api/connection-setups/virtual-key',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Provision (or reuse) the caller's personal passthrough virtual key scoped to an LLM proxy and return its value once. Backs the manual /connection flow's X-Archestra-Virtual-Key attribution step for Claude Code and Claude Desktop. Requires llmVirtualKey:create.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * None (no additional RBAC permission required)
 */
export const createConnectionPassthroughKey = <ThrowOnError extends boolean = false>(options: Options<CreateConnectionPassthroughKeyData, ThrowOnError>) => (options.client ?? client).post<CreateConnectionPassthroughKeyResponses, CreateConnectionPassthroughKeyErrors, ThrowOnError>({
    url: '/api/connection-setups/passthrough-key',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Serve the rendered one-time setup script for a connection setup. Authenticates via the one-time token in the path; the token is consumed atomically on the first successful render.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * None (no additional RBAC permission required)
 */
export const getConnectionSetupScript = <ThrowOnError extends boolean = false>(options: Options<GetConnectionSetupScriptData, ThrowOnError>) => (options.client ?? client).get<GetConnectionSetupScriptResponses, unknown, ThrowOnError>({ url: '/api/connection-setups/script/{token}', ...options });

/**
 * Create a chat completion with DeepSeek (uses default agent)
 *
 * Authentication:
 *
 * This route accepts either an LLM provider API key or a Virtual API Key. See [LLM Proxy Authentication](/docs/platform-llm-proxy-authentication).
 */
export const deepseekChatCompletionsWithDefaultAgent = <ThrowOnError extends boolean = false>(options: Options<DeepseekChatCompletionsWithDefaultAgentData, ThrowOnError>) => (options.client ?? client).post<DeepseekChatCompletionsWithDefaultAgentResponses, DeepseekChatCompletionsWithDefaultAgentErrors, ThrowOnError>({
    url: '/v1/deepseek/chat/completions',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Create a chat completion with DeepSeek for a specific agent
 *
 * Authentication:
 *
 * This route accepts either an LLM provider API key or a Virtual API Key. See [LLM Proxy Authentication](/docs/platform-llm-proxy-authentication).
 */
export const deepseekChatCompletionsWithAgent = <ThrowOnError extends boolean = false>(options: Options<DeepseekChatCompletionsWithAgentData, ThrowOnError>) => (options.client ?? client).post<DeepseekChatCompletionsWithAgentResponses, DeepseekChatCompletionsWithAgentErrors, ThrowOnError>({
    url: '/v1/deepseek/{agentId}/chat/completions',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List per-environment default user limits for the org.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `llmLimit:read`: View token usage limits
 */
export const listDefaultUserLimits = <ThrowOnError extends boolean = false>(options?: Options<ListDefaultUserLimitsData, ThrowOnError>) => (options?.client ?? client).get<ListDefaultUserLimitsResponses, ListDefaultUserLimitsErrors, ThrowOnError>({ url: '/api/default-user-limits', ...options });

/**
 * Create a default user limit. Omit environmentId for the org-wide default, or set it for a per-environment override.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `llmLimit:create`: Create new usage limits
 */
export const createDefaultUserLimit = <ThrowOnError extends boolean = false>(options: Options<CreateDefaultUserLimitData, ThrowOnError>) => (options.client ?? client).post<CreateDefaultUserLimitResponses, CreateDefaultUserLimitErrors, ThrowOnError>({
    url: '/api/default-user-limits',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete a per-environment default user limit.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `llmLimit:delete`: Remove usage limits
 */
export const deleteDefaultUserLimit = <ThrowOnError extends boolean = false>(options: Options<DeleteDefaultUserLimitData, ThrowOnError>) => (options.client ?? client).delete<DeleteDefaultUserLimitResponses, DeleteDefaultUserLimitErrors, ThrowOnError>({ url: '/api/default-user-limits/{id}', ...options });

/**
 * Update a per-environment default user limit.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `llmLimit:update`: Modify existing usage limits
 */
export const updateDefaultUserLimit = <ThrowOnError extends boolean = false>(options: Options<UpdateDefaultUserLimitData, ThrowOnError>) => (options.client ?? client).patch<UpdateDefaultUserLimitResponses, UpdateDefaultUserLimitErrors, ThrowOnError>({
    url: '/api/default-user-limits/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List org-level deployment environments with their assigned catalog counts, plus the count of catalog items with no environment (the default environment).
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `environment:read`: View and list deployment environments
 */
export const listEnvironments = <ThrowOnError extends boolean = false>(options?: Options<ListEnvironmentsData, ThrowOnError>) => (options?.client ?? client).get<ListEnvironmentsResponses, ListEnvironmentsErrors, ThrowOnError>({ url: '/api/environments', ...options });

/**
 * Create an org-level deployment environment.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `environment:create`: Create deployment environments
 */
export const createEnvironment = <ThrowOnError extends boolean = false>(options: Options<CreateEnvironmentData, ThrowOnError>) => (options.client ?? client).post<CreateEnvironmentResponses, CreateEnvironmentErrors, ThrowOnError>({
    url: '/api/environments',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Configure which environment newly created resources of each kind land in when their creator does not pick one. Omitted kinds are left unchanged; an explicit null resets that kind to the default environment.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `environment:update`: Modify deployment environments, including the org default environment
 */
export const updateEnvironmentResourceDefaults = <ThrowOnError extends boolean = false>(options: Options<UpdateEnvironmentResourceDefaultsData, ThrowOnError>) => (options.client ?? client).patch<UpdateEnvironmentResourceDefaultsResponses, UpdateEnvironmentResourceDefaultsErrors, ThrowOnError>({
    url: '/api/environments/defaults',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete an org-level environment. Fails with 409 if any catalog items are still assigned to it.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `environment:delete`: Delete deployment environments
 */
export const deleteEnvironment = <ThrowOnError extends boolean = false>(options: Options<DeleteEnvironmentData, ThrowOnError>) => (options.client ?? client).delete<DeleteEnvironmentResponses, DeleteEnvironmentErrors, ThrowOnError>({ url: '/api/environments/{id}', ...options });

/**
 * Update an environment's name, description, namespace, network policy, and restricted flag. When the namespace or network policy changes and the runtime is enabled, all MCP servers assigned to this environment are reconciled.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `environment:update`: Modify deployment environments, including the org default environment
 */
export const updateEnvironment = <ThrowOnError extends boolean = false>(options: Options<UpdateEnvironmentData, ThrowOnError>) => (options.client ?? client).patch<UpdateEnvironmentResponses, UpdateEnvironmentErrors, ThrowOnError>({
    url: '/api/environments/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete several org-level environments in one request. An environment that still has catalog items assigned to it cannot be deleted; it is reported in `failed` with that reason while the rest of the batch still applies.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `environment:delete`: Delete deployment environments
 */
export const bulkDeleteEnvironments = <ThrowOnError extends boolean = false>(options: Options<BulkDeleteEnvironmentsData, ThrowOnError>) => (options.client ?? client).delete<BulkDeleteEnvironmentsResponses, BulkDeleteEnvironmentsErrors, ThrowOnError>({
    url: '/api/environments/bulk',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List Skills dynamically projected from the caller's accessible MCP server installations.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `mcpServerInstallation:read`: View installed MCP servers and their status
 * `skill:read`: View and use agent skills within your scope (org, your teams, your own)
 */
export const getExternalMcpSkills = <ThrowOnError extends boolean = false>(options?: Options<GetExternalMcpSkillsData, ThrowOnError>) => (options?.client ?? client).get<GetExternalMcpSkillsResponses, GetExternalMcpSkillsErrors, ThrowOnError>({ url: '/api/skills/external', ...options });

/**
 * Get recent activation statistics for one external MCP Skill from an accessible installation.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `mcpServerInstallation:read`: View installed MCP servers and their status
 * `skill:read`: View and use agent skills within your scope (org, your teams, your own)
 */
export const getExternalMcpSkillUsageStatistics = <ThrowOnError extends boolean = false>(options: Options<GetExternalMcpSkillUsageStatisticsData, ThrowOnError>) => (options.client ?? client).get<GetExternalMcpSkillUsageStatisticsResponses, GetExternalMcpSkillUsageStatisticsErrors, ThrowOnError>({ url: '/api/skills/external/usage-statistics', ...options });

/**
 * Fetch and digest-verify the current bytes of an external MCP Skill from one accessible installation.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `mcpServerInstallation:read`: View installed MCP servers and their status
 * `skill:read`: View and use agent skills within your scope (org, your teams, your own)
 */
export const getExternalMcpSkill = <ThrowOnError extends boolean = false>(options: Options<GetExternalMcpSkillData, ThrowOnError>) => (options.client ?? client).get<GetExternalMcpSkillResponses, GetExternalMcpSkillErrors, ThrowOnError>({ url: '/api/skills/external/{id}', ...options });

/**
 * Create embeddings with Gemini (uses default agent)
 *
 * Authentication:
 *
 * This route accepts either an LLM provider API key or a Virtual API Key. See [LLM Proxy Authentication](/docs/platform-llm-proxy-authentication).
 */
export const geminiEmbeddingsWithDefaultAgent = <ThrowOnError extends boolean = false>(options: Options<GeminiEmbeddingsWithDefaultAgentData, ThrowOnError>) => (options.client ?? client).post<GeminiEmbeddingsWithDefaultAgentResponses, GeminiEmbeddingsWithDefaultAgentErrors, ThrowOnError>({
    url: '/v1/gemini/embeddings',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Create embeddings with Gemini for a specific agent
 *
 * Authentication:
 *
 * This route accepts either an LLM provider API key or a Virtual API Key. See [LLM Proxy Authentication](/docs/platform-llm-proxy-authentication).
 */
export const geminiEmbeddingsWithAgent = <ThrowOnError extends boolean = false>(options: Options<GeminiEmbeddingsWithAgentData, ThrowOnError>) => (options.client ?? client).post<GeminiEmbeddingsWithAgentResponses, GeminiEmbeddingsWithAgentErrors, ThrowOnError>({
    url: '/v1/gemini/{agentId}/embeddings',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Generate content using Gemini
 *
 * Generate content using Gemini (default agent)
 *
 * Authentication:
 *
 * This route accepts either an LLM provider API key or a Virtual API Key. See [LLM Proxy Authentication](/docs/platform-llm-proxy-authentication).
 */
export const postV1GeminiV1BetaModelsByModelGenerateContent = <ThrowOnError extends boolean = false>(options: Options<PostV1GeminiV1BetaModelsByModelGenerateContentData, ThrowOnError>) => (options.client ?? client).post<PostV1GeminiV1BetaModelsByModelGenerateContentResponses, PostV1GeminiV1BetaModelsByModelGenerateContentErrors, ThrowOnError>({
    url: '/v1/gemini/v1beta/models/{model}:generateContent',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Stream generated content using Gemini
 *
 * Stream generated content using Gemini (default agent)
 *
 * Authentication:
 *
 * This route accepts either an LLM provider API key or a Virtual API Key. See [LLM Proxy Authentication](/docs/platform-llm-proxy-authentication).
 */
export const postV1GeminiV1BetaModelsByModelStreamGenerateContent = <ThrowOnError extends boolean = false>(options: Options<PostV1GeminiV1BetaModelsByModelStreamGenerateContentData, ThrowOnError>) => (options.client ?? client).post<unknown, PostV1GeminiV1BetaModelsByModelStreamGenerateContentErrors, ThrowOnError>({
    url: '/v1/gemini/v1beta/models/{model}:streamGenerateContent',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Generate content using Gemini (specific agent)
 *
 * Generate content using Gemini with specific agent
 *
 * Authentication:
 *
 * This route accepts either an LLM provider API key or a Virtual API Key. See [LLM Proxy Authentication](/docs/platform-llm-proxy-authentication).
 */
export const postV1GeminiByAgentIdV1BetaModelsByModelGenerateContent = <ThrowOnError extends boolean = false>(options: Options<PostV1GeminiByAgentIdV1BetaModelsByModelGenerateContentData, ThrowOnError>) => (options.client ?? client).post<PostV1GeminiByAgentIdV1BetaModelsByModelGenerateContentResponses, PostV1GeminiByAgentIdV1BetaModelsByModelGenerateContentErrors, ThrowOnError>({
    url: '/v1/gemini/{agentId}/v1beta/models/{model}:generateContent',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Stream generated content using Gemini (specific agent)
 *
 * Stream generated content using Gemini with specific agent
 *
 * Authentication:
 *
 * This route accepts either an LLM provider API key or a Virtual API Key. See [LLM Proxy Authentication](/docs/platform-llm-proxy-authentication).
 */
export const postV1GeminiByAgentIdV1BetaModelsByModelStreamGenerateContent = <ThrowOnError extends boolean = false>(options: Options<PostV1GeminiByAgentIdV1BetaModelsByModelStreamGenerateContentData, ThrowOnError>) => (options.client ?? client).post<unknown, PostV1GeminiByAgentIdV1BetaModelsByModelStreamGenerateContentErrors, ThrowOnError>({
    url: '/v1/gemini/{agentId}/v1beta/models/{model}:streamGenerateContent',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List organization GitHub App configurations. The private key is never returned.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `githubAppConfig:read`: View GitHub App configurations
 */
export const listGithubAppConfigs = <ThrowOnError extends boolean = false>(options?: Options<ListGithubAppConfigsData, ThrowOnError>) => (options?.client ?? client).get<ListGithubAppConfigsResponses, ListGithubAppConfigsErrors, ThrowOnError>({ url: '/api/github-app-configs', ...options });

/**
 * Create a GitHub App configuration. The private key is stored as a secret and never returned.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `githubAppConfig:create`: Create GitHub App configurations
 */
export const createGithubAppConfig = <ThrowOnError extends boolean = false>(options: Options<CreateGithubAppConfigData, ThrowOnError>) => (options.client ?? client).post<CreateGithubAppConfigResponses, CreateGithubAppConfigErrors, ThrowOnError>({
    url: '/api/github-app-configs',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete a GitHub App configuration and its stored private key.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `githubAppConfig:delete`: Delete GitHub App configurations
 */
export const deleteGithubAppConfig = <ThrowOnError extends boolean = false>(options: Options<DeleteGithubAppConfigData, ThrowOnError>) => (options.client ?? client).delete<DeleteGithubAppConfigResponses, DeleteGithubAppConfigErrors, ThrowOnError>({ url: '/api/github-app-configs/{id}', ...options });

/**
 * Get a GitHub App configuration. The private key is never returned.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `githubAppConfig:read`: View GitHub App configurations
 */
export const getGithubAppConfig = <ThrowOnError extends boolean = false>(options: Options<GetGithubAppConfigData, ThrowOnError>) => (options.client ?? client).get<GetGithubAppConfigResponses, GetGithubAppConfigErrors, ThrowOnError>({ url: '/api/github-app-configs/{id}', ...options });

/**
 * Update a GitHub App configuration. Provide a private key only to rotate it.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `githubAppConfig:update`: Modify GitHub App configurations
 */
export const updateGithubAppConfig = <ThrowOnError extends boolean = false>(options: Options<UpdateGithubAppConfigData, ThrowOnError>) => (options.client ?? client).put<UpdateGithubAppConfigResponses, UpdateGithubAppConfigErrors, ThrowOnError>({
    url: '/api/github-app-configs/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Start the GitHub device flow used to connect a GitHub Copilot subscription
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * None (no additional RBAC permission required)
 */
export const githubCopilotDeviceAuthStart = <ThrowOnError extends boolean = false>(options?: Options<GithubCopilotDeviceAuthStartData, ThrowOnError>) => (options?.client ?? client).post<GithubCopilotDeviceAuthStartResponses, GithubCopilotDeviceAuthStartErrors, ThrowOnError>({ url: '/api/github-copilot-auth/device/start', ...options });

/**
 * Poll the GitHub device flow once; returns the GitHub OAuth token when the user has authorized
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * None (no additional RBAC permission required)
 */
export const githubCopilotDeviceAuthPoll = <ThrowOnError extends boolean = false>(options: Options<GithubCopilotDeviceAuthPollData, ThrowOnError>) => (options.client ?? client).post<GithubCopilotDeviceAuthPollResponses, GithubCopilotDeviceAuthPollErrors, ThrowOnError>({
    url: '/api/github-copilot-auth/device/poll',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Create a chat completion with GitHub Copilot (uses default agent)
 *
 * Authentication:
 *
 * This route accepts either an LLM provider API key or a Virtual API Key. See [LLM Proxy Authentication](/docs/platform-llm-proxy-authentication).
 */
export const githubCopilotChatCompletionsWithDefaultAgent = <ThrowOnError extends boolean = false>(options: Options<GithubCopilotChatCompletionsWithDefaultAgentData, ThrowOnError>) => (options.client ?? client).post<GithubCopilotChatCompletionsWithDefaultAgentResponses, GithubCopilotChatCompletionsWithDefaultAgentErrors, ThrowOnError>({
    url: '/v1/github-copilot/chat/completions',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Create a chat completion with GitHub Copilot for a specific agent
 *
 * Authentication:
 *
 * This route accepts either an LLM provider API key or a Virtual API Key. See [LLM Proxy Authentication](/docs/platform-llm-proxy-authentication).
 */
export const githubCopilotChatCompletionsWithAgent = <ThrowOnError extends boolean = false>(options: Options<GithubCopilotChatCompletionsWithAgentData, ThrowOnError>) => (options.client ?? client).post<GithubCopilotChatCompletionsWithAgentResponses, GithubCopilotChatCompletionsWithAgentErrors, ThrowOnError>({
    url: '/v1/github-copilot/{agentId}/chat/completions',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Create a response with GitHub Copilot (uses default agent)
 *
 * Authentication:
 *
 * This route accepts either an LLM provider API key or a Virtual API Key. See [LLM Proxy Authentication](/docs/platform-llm-proxy-authentication).
 */
export const githubCopilotResponsesWithDefaultAgent = <ThrowOnError extends boolean = false>(options: Options<GithubCopilotResponsesWithDefaultAgentData, ThrowOnError>) => (options.client ?? client).post<GithubCopilotResponsesWithDefaultAgentResponses, GithubCopilotResponsesWithDefaultAgentErrors, ThrowOnError>({
    url: '/v1/github-copilot/responses',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Create a response with GitHub Copilot for a specific agent
 *
 * Authentication:
 *
 * This route accepts either an LLM provider API key or a Virtual API Key. See [LLM Proxy Authentication](/docs/platform-llm-proxy-authentication).
 */
export const githubCopilotResponsesWithAgent = <ThrowOnError extends boolean = false>(options: Options<GithubCopilotResponsesWithAgentData, ThrowOnError>) => (options.client ?? client).post<GithubCopilotResponsesWithAgentResponses, GithubCopilotResponsesWithAgentErrors, ThrowOnError>({
    url: '/v1/github-copilot/{agentId}/responses',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List GitHub Copilot models (default agent)
 *
 * Authentication:
 *
 * This route accepts either an LLM provider API key or a Virtual API Key. See [LLM Proxy Authentication](/docs/platform-llm-proxy-authentication).
 */
export const githubCopilotListModelsWithDefaultAgent = <ThrowOnError extends boolean = false>(options?: Options<GithubCopilotListModelsWithDefaultAgentData, ThrowOnError>) => (options?.client ?? client).get<GithubCopilotListModelsWithDefaultAgentResponses, GithubCopilotListModelsWithDefaultAgentErrors, ThrowOnError>({ url: '/v1/github-copilot/models', ...options });

/**
 * List GitHub Copilot models (specific agent)
 *
 * Authentication:
 *
 * This route accepts either an LLM provider API key or a Virtual API Key. See [LLM Proxy Authentication](/docs/platform-llm-proxy-authentication).
 */
export const githubCopilotListModelsWithAgent = <ThrowOnError extends boolean = false>(options: Options<GithubCopilotListModelsWithAgentData, ThrowOnError>) => (options.client ?? client).get<GithubCopilotListModelsWithAgentResponses, GithubCopilotListModelsWithAgentErrors, ThrowOnError>({ url: '/v1/github-copilot/{agentId}/models', ...options });

/**
 * List organization GitHub personal access tokens. The token value is never returned.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `githubAppConfig:read`: View GitHub App configurations
 */
export const listGithubPats = <ThrowOnError extends boolean = false>(options?: Options<ListGithubPatsData, ThrowOnError>) => (options?.client ?? client).get<ListGithubPatsResponses, ListGithubPatsErrors, ThrowOnError>({ url: '/api/github-pats', ...options });

/**
 * Store a GitHub personal access token. The token is stored as a secret and never returned.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `githubAppConfig:create`: Create GitHub App configurations
 */
export const createGithubPat = <ThrowOnError extends boolean = false>(options: Options<CreateGithubPatData, ThrowOnError>) => (options.client ?? client).post<CreateGithubPatResponses, CreateGithubPatErrors, ThrowOnError>({
    url: '/api/github-pats',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete a stored GitHub token and its secret. Rejected while synced skills authenticate with it.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `githubAppConfig:delete`: Delete GitHub App configurations
 */
export const deleteGithubPat = <ThrowOnError extends boolean = false>(options: Options<DeleteGithubPatData, ThrowOnError>) => (options.client ?? client).delete<DeleteGithubPatResponses, DeleteGithubPatErrors, ThrowOnError>({ url: '/api/github-pats/{id}', ...options });

/**
 * Rename a stored GitHub token. Provide a token only to rotate it.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `githubAppConfig:update`: Modify GitHub App configurations
 */
export const updateGithubPat = <ThrowOnError extends boolean = false>(options: Options<UpdateGithubPatData, ThrowOnError>) => (options.client ?? client).put<UpdateGithubPatResponses, UpdateGithubPatErrors, ThrowOnError>({
    url: '/api/github-pats/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Create a chat completion with Groq (uses default agent)
 *
 * Authentication:
 *
 * This route accepts either an LLM provider API key or a Virtual API Key. See [LLM Proxy Authentication](/docs/platform-llm-proxy-authentication).
 */
export const groqChatCompletionsWithDefaultAgent = <ThrowOnError extends boolean = false>(options: Options<GroqChatCompletionsWithDefaultAgentData, ThrowOnError>) => (options.client ?? client).post<GroqChatCompletionsWithDefaultAgentResponses, GroqChatCompletionsWithDefaultAgentErrors, ThrowOnError>({
    url: '/v1/groq/chat/completions',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Create a chat completion with Groq for a specific agent
 *
 * Authentication:
 *
 * This route accepts either an LLM provider API key or a Virtual API Key. See [LLM Proxy Authentication](/docs/platform-llm-proxy-authentication).
 */
export const groqChatCompletionsWithAgent = <ThrowOnError extends boolean = false>(options: Options<GroqChatCompletionsWithAgentData, ThrowOnError>) => (options.client ?? client).post<GroqChatCompletionsWithAgentResponses, GroqChatCompletionsWithAgentErrors, ThrowOnError>({
    url: '/v1/groq/{agentId}/chat/completions',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

export const getHealth = <ThrowOnError extends boolean = false>(options?: Options<GetHealthData, ThrowOnError>) => (options?.client ?? client).get<GetHealthResponses, unknown, ThrowOnError>({ url: '/health', ...options });

export const getReady = <ThrowOnError extends boolean = false>(options?: Options<GetReadyData, ThrowOnError>) => (options?.client ?? client).get<GetReadyResponses, GetReadyErrors, ThrowOnError>({ url: '/ready', ...options });

/**
 * List hook files for an agent
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `agent:read`: View and list agents
 */
export const getHooks = <ThrowOnError extends boolean = false>(options: Options<GetHooksData, ThrowOnError>) => (options.client ?? client).get<GetHooksResponses, GetHooksErrors, ThrowOnError>({ url: '/api/hooks', ...options });

/**
 * Create a new hook file for an agent
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `agent:update`: Modify agent configuration and settings
 */
export const createHook = <ThrowOnError extends boolean = false>(options: Options<CreateHookData, ThrowOnError>) => (options.client ?? client).post<CreateHookResponses, CreateHookErrors, ThrowOnError>({
    url: '/api/hooks',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete a hook file
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `agent:update`: Modify agent configuration and settings
 */
export const deleteHook = <ThrowOnError extends boolean = false>(options: Options<DeleteHookData, ThrowOnError>) => (options.client ?? client).delete<DeleteHookResponses, DeleteHookErrors, ThrowOnError>({ url: '/api/hooks/{id}', ...options });

/**
 * Update an existing hook file
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `agent:update`: Modify agent configuration and settings
 */
export const updateHook = <ThrowOnError extends boolean = false>(options: Options<UpdateHookData, ThrowOnError>) => (options.client ?? client).put<UpdateHookResponses, UpdateHookErrors, ThrowOnError>({
    url: '/api/hooks/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Webhook endpoint for incoming email notifications
 */
export const postApiWebhooksIncomingEmail = <ThrowOnError extends boolean = false>(options: Options<PostApiWebhooksIncomingEmailData, ThrowOnError>) => (options.client ?? client).post<PostApiWebhooksIncomingEmailResponses, PostApiWebhooksIncomingEmailErrors, ThrowOnError>({
    url: '/api/webhooks/incoming-email',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get the email address for invoking an agent
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * None (no additional RBAC permission required)
 */
export const getAgentEmailAddress = <ThrowOnError extends boolean = false>(options: Options<GetAgentEmailAddressData, ThrowOnError>) => (options.client ?? client).get<GetAgentEmailAddressResponses, GetAgentEmailAddressErrors, ThrowOnError>({ url: '/api/agents/{agentId}/email-address', ...options });

/**
 * Get the current incoming email webhook subscription status
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `agentTrigger:read`: View agent trigger configurations (Slack, MS Teams, email)
 */
export const getIncomingEmailStatus = <ThrowOnError extends boolean = false>(options?: Options<GetIncomingEmailStatusData, ThrowOnError>) => (options?.client ?? client).get<GetIncomingEmailStatusResponses, GetIncomingEmailStatusErrors, ThrowOnError>({ url: '/api/incoming-email/status', ...options });

/**
 * Setup or renew incoming email webhook subscription
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `agentTrigger:create`: Set up new agent triggers
 */
export const setupIncomingEmailWebhook = <ThrowOnError extends boolean = false>(options: Options<SetupIncomingEmailWebhookData, ThrowOnError>) => (options.client ?? client).post<SetupIncomingEmailWebhookResponses, SetupIncomingEmailWebhookErrors, ThrowOnError>({
    url: '/api/incoming-email/setup',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Renew the incoming email webhook subscription
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `agentTrigger:update`: Modify agent trigger configurations
 */
export const renewIncomingEmailSubscription = <ThrowOnError extends boolean = false>(options?: Options<RenewIncomingEmailSubscriptionData, ThrowOnError>) => (options?.client ?? client).post<RenewIncomingEmailSubscriptionResponses, RenewIncomingEmailSubscriptionErrors, ThrowOnError>({ url: '/api/incoming-email/renew', ...options });

/**
 * Delete the incoming email webhook subscription
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `agentTrigger:delete`: Remove agent triggers
 */
export const deleteIncomingEmailSubscription = <ThrowOnError extends boolean = false>(options?: Options<DeleteIncomingEmailSubscriptionData, ThrowOnError>) => (options?.client ?? client).delete<DeleteIncomingEmailSubscriptionResponses, DeleteIncomingEmailSubscriptionErrors, ThrowOnError>({ url: '/api/incoming-email/subscription', ...options });

/**
 * Get all interactions with pagination and sorting
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `log:read`: View your own LLM proxy and MCP tool call logs
 */
export const getInteractions = <ThrowOnError extends boolean = false>(options?: Options<GetInteractionsData, ThrowOnError>) => (options?.client ?? client).get<GetInteractionsResponses, GetInteractionsErrors, ThrowOnError>({ url: '/api/interactions', ...options });

/**
 * Get all interaction sessions grouped by session ID with aggregated stats
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `log:read`: View your own LLM proxy and MCP tool call logs
 */
export const getInteractionSessions = <ThrowOnError extends boolean = false>(options?: Options<GetInteractionSessionsData, ThrowOnError>) => (options?.client ?? client).get<GetInteractionSessionsResponses, GetInteractionSessionsErrors, ThrowOnError>({ url: '/api/interactions/sessions', ...options });

/**
 * Get all unique external agent IDs with display names for filtering (from X-Archestra-Agent-Id header)
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `log:read`: View your own LLM proxy and MCP tool call logs
 */
export const getUniqueExternalAgentIds = <ThrowOnError extends boolean = false>(options?: Options<GetUniqueExternalAgentIdsData, ThrowOnError>) => (options?.client ?? client).get<GetUniqueExternalAgentIdsResponses, GetUniqueExternalAgentIdsErrors, ThrowOnError>({ url: '/api/interactions/external-agent-ids', ...options });

/**
 * Get all unique user IDs with names for filtering (from X-Archestra-User-Id header)
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `log:read`: View your own LLM proxy and MCP tool call logs
 */
export const getUniqueUserIds = <ThrowOnError extends boolean = false>(options?: Options<GetUniqueUserIdsData, ThrowOnError>) => (options?.client ?? client).get<GetUniqueUserIdsResponses, GetUniqueUserIdsErrors, ThrowOnError>({ url: '/api/interactions/user-ids', ...options });

/**
 * Get interaction by ID
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `log:read`: View your own LLM proxy and MCP tool call logs
 */
export const getInteraction = <ThrowOnError extends boolean = false>(options: Options<GetInteractionData, ThrowOnError>) => (options.client ?? client).get<GetInteractionResponses, GetInteractionErrors, ThrowOnError>({ url: '/api/interactions/{interactionId}', ...options });

/**
 * Get all Internal MCP catalog items
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `mcpRegistry:read`: Browse the MCP server registry
 */
export const getInternalMcpCatalog = <ThrowOnError extends boolean = false>(options?: Options<GetInternalMcpCatalogData, ThrowOnError>) => (options?.client ?? client).get<GetInternalMcpCatalogResponses, GetInternalMcpCatalogErrors, ThrowOnError>({ url: '/api/internal_mcp_catalog', ...options });

/**
 * Create a new Internal MCP catalog item
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `mcpRegistry:create`: Add servers to the MCP registry
 */
export const createInternalMcpCatalogItem = <ThrowOnError extends boolean = false>(options: Options<CreateInternalMcpCatalogItemData, ThrowOnError>) => (options.client ?? client).post<CreateInternalMcpCatalogItemResponses, CreateInternalMcpCatalogItemErrors, ThrowOnError>({
    url: '/api/internal_mcp_catalog',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Restore one dismissed catalog-level MCP alert to the calling user's view.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `mcpRegistry:read`: Browse the MCP server registry
 */
export const unmuteMcpCatalogAlert = <ThrowOnError extends boolean = false>(options: Options<UnmuteMcpCatalogAlertData, ThrowOnError>) => (options.client ?? client).delete<UnmuteMcpCatalogAlertResponses, UnmuteMcpCatalogAlertErrors, ThrowOnError>({ url: '/api/internal_mcp_catalog/{id}/alert-mutes/{kind}', ...options });

/**
 * Dismiss one catalog-level MCP alert for the calling user only. The fingerprint pins it to one failure episode.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `mcpRegistry:read`: Browse the MCP server registry
 */
export const muteMcpCatalogAlert = <ThrowOnError extends boolean = false>(options: Options<MuteMcpCatalogAlertData, ThrowOnError>) => (options.client ?? client).put<MuteMcpCatalogAlertResponses, MuteMcpCatalogAlertErrors, ThrowOnError>({
    url: '/api/internal_mcp_catalog/{id}/alert-mutes/{kind}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete an Internal MCP catalog item
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `mcpRegistry:delete`: Remove servers from the MCP registry
 */
export const deleteInternalMcpCatalogItem = <ThrowOnError extends boolean = false>(options: Options<DeleteInternalMcpCatalogItemData, ThrowOnError>) => (options.client ?? client).delete<DeleteInternalMcpCatalogItemResponses, DeleteInternalMcpCatalogItemErrors, ThrowOnError>({ url: '/api/internal_mcp_catalog/{id}', ...options });

/**
 * Get Internal MCP catalog item by ID
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `mcpRegistry:read`: Browse the MCP server registry
 */
export const getInternalMcpCatalogItem = <ThrowOnError extends boolean = false>(options: Options<GetInternalMcpCatalogItemData, ThrowOnError>) => (options.client ?? client).get<GetInternalMcpCatalogItemResponses, GetInternalMcpCatalogItemErrors, ThrowOnError>({ url: '/api/internal_mcp_catalog/{id}', ...options });

/**
 * Update an Internal MCP catalog item
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `mcpRegistry:update`: Modify MCP registry entries
 */
export const updateInternalMcpCatalogItem = <ThrowOnError extends boolean = false>(options: Options<UpdateInternalMcpCatalogItemData, ThrowOnError>) => (options.client ?? client).put<UpdateInternalMcpCatalogItemResponses, UpdateInternalMcpCatalogItemErrors, ThrowOnError>({
    url: '/api/internal_mcp_catalog/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get id/name/catalog for every listable tool across all catalog items the caller can see, in one request. Backs the tool pickers, which previously issued one request per catalog item.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `mcpRegistry:read`: Browse the MCP server registry
 */
export const getInternalMcpCatalogToolsBatch = <ThrowOnError extends boolean = false>(options?: Options<GetInternalMcpCatalogToolsBatchData, ThrowOnError>) => (options?.client ?? client).get<GetInternalMcpCatalogToolsBatchResponses, GetInternalMcpCatalogToolsBatchErrors, ThrowOnError>({ url: '/api/internal_mcp_catalog/tools', ...options });

/**
 * Get tools for a catalog item (including builtin Archestra tools)
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `mcpRegistry:read`: Browse the MCP server registry
 */
export const getInternalMcpCatalogTools = <ThrowOnError extends boolean = false>(options: Options<GetInternalMcpCatalogToolsData, ThrowOnError>) => (options.client ?? client).get<GetInternalMcpCatalogToolsResponses, GetInternalMcpCatalogToolsErrors, ThrowOnError>({ url: '/api/internal_mcp_catalog/{id}/tools', ...options });

/**
 * Reinstall the shared K8s Deployment for a multi-tenant local catalog and cascade tool sync to every install.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `mcpRegistry:update`: Modify MCP registry entries
 */
export const reinstallInternalMcpCatalogItem = <ThrowOnError extends boolean = false>(options: Options<ReinstallInternalMcpCatalogItemData, ThrowOnError>) => (options.client ?? client).post<ReinstallInternalMcpCatalogItemResponses, ReinstallInternalMcpCatalogItemErrors, ThrowOnError>({ url: '/api/internal_mcp_catalog/{id}/reinstall', ...options });

/**
 * Restart all local MCP server pods for a catalog so Kubernetes pulls the current configured image. Fan-out restarts are best effort: the request succeeds when at least one target restarts successfully, while failed installs are marked with their own error status.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `mcpRegistry:update`: Modify MCP registry entries
 */
export const refreshInternalMcpCatalogImage = <ThrowOnError extends boolean = false>(options: Options<RefreshInternalMcpCatalogImageData, ThrowOnError>) => (options.client ?? client).post<RefreshInternalMcpCatalogImageResponses, RefreshInternalMcpCatalogImageErrors, ThrowOnError>({ url: '/api/internal_mcp_catalog/{id}/refresh-image', ...options });

/**
 * List local catalog items in the org whose custom image is awaiting admin approval (blocked by the target environment's trusted image registries).
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `mcpServerInstallation:admin`: Approve or manage all MCP server installations
 */
export const listPendingImageApprovalCatalogItems = <ThrowOnError extends boolean = false>(options?: Options<ListPendingImageApprovalCatalogItemsData, ThrowOnError>) => (options?.client ?? client).get<ListPendingImageApprovalCatalogItemsResponses, ListPendingImageApprovalCatalogItemsErrors, ThrowOnError>({ url: '/api/internal_mcp_catalog/pending-image-approval', ...options });

/**
 * Approve a local catalog item's image so its installs proceed. Requires mcpServerInstallation:admin.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `mcpServerInstallation:admin`: Approve or manage all MCP server installations
 */
export const approveCatalogItemImage = <ThrowOnError extends boolean = false>(options: Options<ApproveCatalogItemImageData, ThrowOnError>) => (options.client ?? client).post<ApproveCatalogItemImageResponses, ApproveCatalogItemImageErrors, ThrowOnError>({ url: '/api/internal_mcp_catalog/{id}/approve', ...options });

/**
 * Delete an Internal MCP catalog item by name
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `mcpRegistry:delete`: Remove servers from the MCP registry
 */
export const deleteInternalMcpCatalogItemByName = <ThrowOnError extends boolean = false>(options: Options<DeleteInternalMcpCatalogItemByNameData, ThrowOnError>) => (options.client ?? client).delete<DeleteInternalMcpCatalogItemByNameResponses, DeleteInternalMcpCatalogItemByNameErrors, ThrowOnError>({ url: '/api/internal_mcp_catalog/by-name/{name}', ...options });

/**
 * Restore a soft-deleted Internal MCP catalog item and its cascaded installs and tools (flag-only reinstall).
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `mcpRegistry:manage-deleted`: View and restore soft-deleted MCP registry entries
 */
export const restoreInternalMcpCatalogItem = <ThrowOnError extends boolean = false>(options: Options<RestoreInternalMcpCatalogItemData, ThrowOnError>) => (options.client ?? client).post<RestoreInternalMcpCatalogItemResponses, RestoreInternalMcpCatalogItemErrors, ThrowOnError>({ url: '/api/internal_mcp_catalog/{id}/restore', ...options });

/**
 * Generate a deployment YAML template preview for a catalog item
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `mcpRegistry:read`: Browse the MCP server registry
 */
export const getDeploymentYamlPreview = <ThrowOnError extends boolean = false>(options: Options<GetDeploymentYamlPreviewData, ThrowOnError>) => (options.client ?? client).get<GetDeploymentYamlPreviewResponses, GetDeploymentYamlPreviewErrors, ThrowOnError>({ url: '/api/internal_mcp_catalog/{id}/deployment-yaml-preview', ...options });

/**
 * Validate a deployment YAML template
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `mcpRegistry:read`: Browse the MCP server registry
 */
export const validateDeploymentYaml = <ThrowOnError extends boolean = false>(options: Options<ValidateDeploymentYamlData, ThrowOnError>) => (options.client ?? client).post<ValidateDeploymentYamlResponses, ValidateDeploymentYamlErrors, ThrowOnError>({
    url: '/api/internal_mcp_catalog/validate-deployment-yaml',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Reset the deployment YAML to default by clearing the custom YAML
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `mcpRegistry:update`: Modify MCP registry entries
 */
export const resetDeploymentYaml = <ThrowOnError extends boolean = false>(options: Options<ResetDeploymentYamlData, ThrowOnError>) => (options.client ?? client).post<ResetDeploymentYamlResponses, ResetDeploymentYamlErrors, ThrowOnError>({ url: '/api/internal_mcp_catalog/{id}/reset-deployment-yaml', ...options });

/**
 * List Kubernetes docker-registry secrets available for imagePullSecrets
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `mcpRegistry:read`: Browse the MCP server registry
 */
export const getK8sImagePullSecrets = <ThrowOnError extends boolean = false>(options?: Options<GetK8sImagePullSecretsData, ThrowOnError>) => (options?.client ?? client).get<GetK8sImagePullSecretsResponses, GetK8sImagePullSecretsErrors, ThrowOnError>({ url: '/api/k8s/image-pull-secrets', ...options });

/**
 * Get all label keys used by catalog items
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `mcpRegistry:read`: Browse the MCP server registry
 */
export const getInternalMcpCatalogLabelKeys = <ThrowOnError extends boolean = false>(options?: Options<GetInternalMcpCatalogLabelKeysData, ThrowOnError>) => (options?.client ?? client).get<GetInternalMcpCatalogLabelKeysResponses, GetInternalMcpCatalogLabelKeysErrors, ThrowOnError>({ url: '/api/internal_mcp_catalog/labels/keys', ...options });

/**
 * Get all label values for catalog items
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `mcpRegistry:read`: Browse the MCP server registry
 */
export const getInternalMcpCatalogLabelValues = <ThrowOnError extends boolean = false>(options?: Options<GetInternalMcpCatalogLabelValuesData, ThrowOnError>) => (options?.client ?? client).get<GetInternalMcpCatalogLabelValuesResponses, GetInternalMcpCatalogLabelValuesErrors, ThrowOnError>({ url: '/api/internal_mcp_catalog/labels/values', ...options });

/**
 * Check if an invitation is valid and whether the user exists
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * None (no additional RBAC permission required)
 */
export const checkInvitation = <ThrowOnError extends boolean = false>(options: Options<CheckInvitationData, ThrowOnError>) => (options.client ?? client).get<CheckInvitationResponses, CheckInvitationErrors, ThrowOnError>({ url: '/api/invitation/{id}/check', ...options });

/**
 * Inspect Kubernetes capabilities available to Archestra.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `environment:update`: Modify deployment environments, including the org default environment
 */
export const getK8sCapabilities = <ThrowOnError extends boolean = false>(options?: Options<GetK8sCapabilitiesData, ThrowOnError>) => (options?.client ?? client).get<GetK8sCapabilitiesResponses, GetK8sCapabilitiesErrors, ThrowOnError>({ url: '/api/k8s/capabilities', ...options });

/**
 * Create a chat completion with Kimi (uses default agent)
 *
 * Authentication:
 *
 * This route accepts either an LLM provider API key or a Virtual API Key. See [LLM Proxy Authentication](/docs/platform-llm-proxy-authentication).
 */
export const kimiChatCompletionsWithDefaultAgent = <ThrowOnError extends boolean = false>(options: Options<KimiChatCompletionsWithDefaultAgentData, ThrowOnError>) => (options.client ?? client).post<KimiChatCompletionsWithDefaultAgentResponses, KimiChatCompletionsWithDefaultAgentErrors, ThrowOnError>({
    url: '/v1/kimi/chat/completions',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Create a chat completion with Kimi for a specific agent
 *
 * Authentication:
 *
 * This route accepts either an LLM provider API key or a Virtual API Key. See [LLM Proxy Authentication](/docs/platform-llm-proxy-authentication).
 */
export const kimiChatCompletionsWithAgent = <ThrowOnError extends boolean = false>(options: Options<KimiChatCompletionsWithAgentData, ThrowOnError>) => (options.client ?? client).post<KimiChatCompletionsWithAgentResponses, KimiChatCompletionsWithAgentErrors, ThrowOnError>({
    url: '/v1/kimi/{agentId}/chat/completions',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List all knowledge bases for the organization. `status=deleted` lists the soft-deleted ones instead — the trash view, which requires `knowledgeSource:delete`. Search and pagination behave identically on both slices, but the deleted slice carries identity only: `connectors` and `assignedAgents` come back empty and `totalDocsIndexed` zero, since those links resolve to nothing while the knowledge base is in the trash.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `knowledgeSource:read`: View Knowledge Bases and Connectors
 */
export const getKnowledgeBases = <ThrowOnError extends boolean = false>(options?: Options<GetKnowledgeBasesData, ThrowOnError>) => (options?.client ?? client).get<GetKnowledgeBasesResponses, GetKnowledgeBasesErrors, ThrowOnError>({ url: '/api/knowledge-bases', ...options });

/**
 * Create a new knowledge base
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `knowledgeSource:create`: Create Knowledge Bases and Connectors
 */
export const createKnowledgeBase = <ThrowOnError extends boolean = false>(options: Options<CreateKnowledgeBaseData, ThrowOnError>) => (options.client ?? client).post<CreateKnowledgeBaseResponses, CreateKnowledgeBaseErrors, ThrowOnError>({
    url: '/api/knowledge-bases',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete a knowledge base and remove its connector assignments
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `knowledgeSource:delete`: Delete Knowledge Bases and Connectors, view the deleted ones, and restore them
 */
export const deleteKnowledgeBase = <ThrowOnError extends boolean = false>(options: Options<DeleteKnowledgeBaseData, ThrowOnError>) => (options.client ?? client).delete<DeleteKnowledgeBaseResponses, DeleteKnowledgeBaseErrors, ThrowOnError>({ url: '/api/knowledge-bases/{id}', ...options });

/**
 * Get a knowledge base by ID
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `knowledgeSource:read`: View Knowledge Bases and Connectors
 */
export const getKnowledgeBase = <ThrowOnError extends boolean = false>(options: Options<GetKnowledgeBaseData, ThrowOnError>) => (options.client ?? client).get<GetKnowledgeBaseResponses, GetKnowledgeBaseErrors, ThrowOnError>({ url: '/api/knowledge-bases/{id}', ...options });

/**
 * Update a knowledge base
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `knowledgeSource:update`: Modify Knowledge Bases and Connectors
 */
export const updateKnowledgeBase = <ThrowOnError extends boolean = false>(options: Options<UpdateKnowledgeBaseData, ThrowOnError>) => (options.client ?? client).put<UpdateKnowledgeBaseResponses, UpdateKnowledgeBaseErrors, ThrowOnError>({
    url: '/api/knowledge-bases/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Soft-delete several knowledge bases in one request, removing their connector assignments. Ids the caller cannot see are reported in `failed` and leave the rest of the batch applied. There is no matching PATCH: a knowledge base has no visibility of its own — it is reached through the connectors and documents assigned to it — so its only editable fields are its name and description, which are per-row by nature.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `knowledgeSource:delete`: Delete Knowledge Bases and Connectors, view the deleted ones, and restore them
 */
export const bulkDeleteKnowledgeBases = <ThrowOnError extends boolean = false>(options: Options<BulkDeleteKnowledgeBasesData, ThrowOnError>) => (options.client ?? client).delete<BulkDeleteKnowledgeBasesResponses, BulkDeleteKnowledgeBasesErrors, ThrowOnError>({
    url: '/api/knowledge-bases/bulk',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Restore a soft-deleted knowledge base. Agent assignments and connector links survive deletion, so the restored knowledge base is immediately live for previously-assigned agents. 404 if there is no soft-deleted knowledge base with that id in the org.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `knowledgeSource:delete`: Delete Knowledge Bases and Connectors, view the deleted ones, and restore them
 */
export const restoreKnowledgeBase = <ThrowOnError extends boolean = false>(options: Options<RestoreKnowledgeBaseData, ThrowOnError>) => (options.client ?? client).post<RestoreKnowledgeBaseResponses, RestoreKnowledgeBaseErrors, ThrowOnError>({ url: '/api/knowledge-bases/{id}/restore', ...options });

/**
 * Permanently destroy a soft-deleted knowledge base (global admins only — a `knowledgeSource:delete` or `knowledgeSource:admin` grant reaches the trash, not past it). Irreversible, with no grace period: the row and its agent and connector assignments are destroyed. Its connectors survive, detached. 404 if there is no soft-deleted knowledge base with that id in the org, which is also the answer when it is still live or the caller is not a global admin. Restore wins a race.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `knowledgeSource:delete`: Delete Knowledge Bases and Connectors, view the deleted ones, and restore them
 */
export const permanentlyDeleteKnowledgeBase = <ThrowOnError extends boolean = false>(options: Options<PermanentlyDeleteKnowledgeBaseData, ThrowOnError>) => (options.client ?? client).delete<PermanentlyDeleteKnowledgeBaseResponses, PermanentlyDeleteKnowledgeBaseErrors, ThrowOnError>({ url: '/api/knowledge-bases/{id}/permanent', ...options });

/**
 * Check the health of a knowledge base
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `knowledgeSource:read`: View Knowledge Bases and Connectors
 */
export const getKnowledgeBaseHealth = <ThrowOnError extends boolean = false>(options: Options<GetKnowledgeBaseHealthData, ThrowOnError>) => (options.client ?? client).get<GetKnowledgeBaseHealthResponses, GetKnowledgeBaseHealthErrors, ThrowOnError>({ url: '/api/knowledge-bases/{id}/health', ...options });

/**
 * List all connectors for the organization. `status=deleted` lists the soft-deleted ones instead — the trash view, which requires `knowledgeSource:delete`. Search and connector-type filtering behave identically on both slices; `knowledgeBaseId` is an active-only filter and is rejected with a 400 alongside `status=deleted`. The deleted slice carries identity only: `assignedAgents` comes back empty, since those links resolve to nothing while the connector is in the trash. It is also the one slice that never hides a row whose stored `config` no longer matches a known connector shape — the trash is the only place restore and permanent delete are offered.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `knowledgeSource:read`: View Knowledge Bases and Connectors
 */
export const getConnectors = <ThrowOnError extends boolean = false>(options?: Options<GetConnectorsData, ThrowOnError>) => (options?.client ?? client).get<GetConnectorsResponses, GetConnectorsErrors, ThrowOnError>({ url: '/api/connectors', ...options });

/**
 * Create a new connector
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `knowledgeSource:create`: Create Knowledge Bases and Connectors
 */
export const createConnector = <ThrowOnError extends boolean = false>(options: Options<CreateConnectorData, ThrowOnError>) => (options.client ?? client).post<CreateConnectorResponses, CreateConnectorErrors, ThrowOnError>({
    url: '/api/connectors',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete a connector
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `knowledgeSource:delete`: Delete Knowledge Bases and Connectors, view the deleted ones, and restore them
 */
export const deleteConnector = <ThrowOnError extends boolean = false>(options: Options<DeleteConnectorData, ThrowOnError>) => (options.client ?? client).delete<DeleteConnectorResponses, DeleteConnectorErrors, ThrowOnError>({ url: '/api/connectors/{id}', ...options });

/**
 * Get a connector by ID
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `knowledgeSource:read`: View Knowledge Bases and Connectors
 */
export const getConnector = <ThrowOnError extends boolean = false>(options: Options<GetConnectorData, ThrowOnError>) => (options.client ?? client).get<GetConnectorResponses, GetConnectorErrors, ThrowOnError>({ url: '/api/connectors/{id}', ...options });

/**
 * Update a connector
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `knowledgeSource:update`: Modify Knowledge Bases and Connectors
 */
export const updateConnector = <ThrowOnError extends boolean = false>(options: Options<UpdateConnectorData, ThrowOnError>) => (options.client ?? client).put<UpdateConnectorResponses, UpdateConnectorErrors, ThrowOnError>({
    url: '/api/connectors/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List documents for a connector
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `knowledgeSource:read`: View Knowledge Bases and Connectors
 */
export const getConnectorDocuments = <ThrowOnError extends boolean = false>(options: Options<GetConnectorDocumentsData, ThrowOnError>) => (options.client ?? client).get<GetConnectorDocumentsResponses, GetConnectorDocumentsErrors, ThrowOnError>({ url: '/api/connectors/{id}/documents', ...options });

/**
 * Delete a connector document
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `knowledgeSource:delete`: Delete Knowledge Bases and Connectors, view the deleted ones, and restore them
 */
export const deleteConnectorDocument = <ThrowOnError extends boolean = false>(options: Options<DeleteConnectorDocumentData, ThrowOnError>) => (options.client ?? client).delete<DeleteConnectorDocumentResponses, DeleteConnectorDocumentErrors, ThrowOnError>({ url: '/api/connectors/{id}/documents/{docId}', ...options });

/**
 * Get a single connector document
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `knowledgeSource:read`: View Knowledge Bases and Connectors
 */
export const getConnectorDocument = <ThrowOnError extends boolean = false>(options: Options<GetConnectorDocumentData, ThrowOnError>) => (options.client ?? client).get<GetConnectorDocumentResponses, GetConnectorDocumentErrors, ThrowOnError>({ url: '/api/connectors/{id}/documents/{docId}', ...options });

/**
 * Delete several of a connector's synced documents in one request. Ids belonging to a different connector — or to no document at all — are reported in `failed` as not found and leave the rest of the batch applied. Deleting a synced document removes it until the next sync brings it back; to stop it returning, remove it at the source or narrow the connector's scope.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `knowledgeSource:delete`: Delete Knowledge Bases and Connectors, view the deleted ones, and restore them
 */
export const bulkDeleteConnectorDocuments = <ThrowOnError extends boolean = false>(options: Options<BulkDeleteConnectorDocumentsData, ThrowOnError>) => (options.client ?? client).delete<BulkDeleteConnectorDocumentsResponses, BulkDeleteConnectorDocumentsErrors, ThrowOnError>({
    url: '/api/connectors/{id}/documents/bulk',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Soft-delete several connectors in one request. As with the single delete, each one's queued syncs are cancelled and its stored credential is destroyed — a restored connector comes back disabled and re-authenticates. A connector the caller may not delete is reported in `failed` while the rest of the batch still applies.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `knowledgeSource:delete`: Delete Knowledge Bases and Connectors, view the deleted ones, and restore them
 */
export const bulkDeleteConnectors = <ThrowOnError extends boolean = false>(options: Options<BulkDeleteConnectorsData, ThrowOnError>) => (options.client ?? client).delete<BulkDeleteConnectorsResponses, BulkDeleteConnectorsErrors, ThrowOnError>({
    url: '/api/connectors/bulk',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Change who can see several connectors in one request. Every connector in the batch moves to the same audience, and the ACL refresh each one needs runs per connector exactly as the single update runs it. `auto-sync-permissions` is deliberately not accepted here: switching a connector to it fail-closes its whole corpus until a permission pass completes, and it is gated on the connector's own type, so it stays a per-connector decision.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `knowledgeSource:update`: Modify Knowledge Bases and Connectors
 */
export const bulkUpdateConnectors = <ThrowOnError extends boolean = false>(options: Options<BulkUpdateConnectorsData, ThrowOnError>) => (options.client ?? client).patch<BulkUpdateConnectorsResponses, BulkUpdateConnectorsErrors, ThrowOnError>({
    url: '/api/connectors/bulk',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Restore a soft-deleted connector. The stored credential was destroyed when the connector was deleted, so it is restored DISABLED — re-authenticate, then re-enable it to resume syncing. 404 if there is no soft-deleted connector with that id in the org that the caller may manage — restoring one is gated on the same visibility as editing or deleting it.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `knowledgeSource:delete`: Delete Knowledge Bases and Connectors, view the deleted ones, and restore them
 */
export const restoreConnector = <ThrowOnError extends boolean = false>(options: Options<RestoreConnectorData, ThrowOnError>) => (options.client ?? client).post<RestoreConnectorResponses, RestoreConnectorErrors, ThrowOnError>({ url: '/api/connectors/{id}/restore', ...options });

/**
 * Permanently destroy a soft-deleted connector (global admins only — a `knowledgeSource:delete` or `knowledgeSource:admin` grant reaches the trash, not past it). Irreversible, with no grace period: its synced documents and their chunks, run history, access mappings, and assignments are all destroyed. 404 if there is no soft-deleted connector with that id in the org, which is also the answer when it is still live or the caller is not a global admin. Restore wins a race.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `knowledgeSource:delete`: Delete Knowledge Bases and Connectors, view the deleted ones, and restore them
 */
export const permanentlyDeleteConnector = <ThrowOnError extends boolean = false>(options: Options<PermanentlyDeleteConnectorData, ThrowOnError>) => (options.client ?? client).delete<PermanentlyDeleteConnectorResponses, PermanentlyDeleteConnectorErrors, ThrowOnError>({ url: '/api/connectors/{id}/permanent', ...options });

/**
 * Manually trigger a connector sync
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `knowledgeSource:update`: Modify Knowledge Bases and Connectors
 */
export const syncConnector = <ThrowOnError extends boolean = false>(options: Options<SyncConnectorData, ThrowOnError>) => (options.client ?? client).post<SyncConnectorResponses, SyncConnectorErrors, ThrowOnError>({ url: '/api/connectors/{id}/sync', ...options });

/**
 * Manually trigger a permission-sync pass for an auto-sync-permissions connector
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `knowledgeSourceAutoSync:update`: Modify auto-sync-permissions connectors: settings, member mappings, and manual permission syncs
 */
export const triggerPermissionSync = <ThrowOnError extends boolean = false>(options: Options<TriggerPermissionSyncData, ThrowOnError>) => (options.client ?? client).post<TriggerPermissionSyncResponses, TriggerPermissionSyncErrors, ThrowOnError>({ url: '/api/connectors/{id}/permission-sync', ...options });

/**
 * Live ACL coverage for an auto-sync-permissions connector: how many ingested documents are tagged vs still fail-closed (awaiting a permission-sync pass)
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `knowledgeSourceAutoSync:read`: View auto-sync-permissions connectors: configuration, sync runs, user groups, and member mappings
 */
export const getPermissionSyncCoverage = <ThrowOnError extends boolean = false>(options: Options<GetPermissionSyncCoverageData, ThrowOnError>) => (options.client ?? client).get<GetPermissionSyncCoverageResponses, GetPermissionSyncCoverageErrors, ThrowOnError>({ url: '/api/connectors/{id}/permission-coverage', ...options });

/**
 * Synced external user groups for an auto-sync-permissions connector: each group's member emails, the Archestra org users they resolve to, and how many documents grant the group
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `knowledgeSourceAutoSync:read`: View auto-sync-permissions connectors: configuration, sync runs, user groups, and member mappings
 */
export const getConnectorUserGroups = <ThrowOnError extends boolean = false>(options: Options<GetConnectorUserGroupsData, ThrowOnError>) => (options.client ?? client).get<GetConnectorUserGroupsResponses, GetConnectorUserGroupsErrors, ThrowOnError>({ url: '/api/connectors/{id}/user-groups', ...options });

/**
 * Manually map an upstream member account to an Archestra user for an auto-sync-permissions connector — the admin escape hatch when the upstream hides the member's email from every credential
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `knowledgeSourceAutoSync:update`: Modify auto-sync-permissions connectors: settings, member mappings, and manual permission syncs
 */
export const upsertConnectorMemberOverride = <ThrowOnError extends boolean = false>(options: Options<UpsertConnectorMemberOverrideData, ThrowOnError>) => (options.client ?? client).put<UpsertConnectorMemberOverrideResponses, UpsertConnectorMemberOverrideErrors, ThrowOnError>({
    url: '/api/connectors/{id}/member-overrides',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Remove a manual member mapping; the member falls back to email-based resolution (fail-closed when the upstream hides their email)
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `knowledgeSourceAutoSync:update`: Modify auto-sync-permissions connectors: settings, member mappings, and manual permission syncs
 */
export const deleteConnectorMemberOverride = <ThrowOnError extends boolean = false>(options: Options<DeleteConnectorMemberOverrideData, ThrowOnError>) => (options.client ?? client).delete<DeleteConnectorMemberOverrideResponses, DeleteConnectorMemberOverrideErrors, ThrowOnError>({ url: '/api/connectors/{id}/member-overrides/{externalAccountId}', ...options });

/**
 * Force a full re-sync: deletes all documents, chunks, run history, and resets the checkpoint
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `knowledgeSource:update`: Modify Knowledge Bases and Connectors
 */
export const forceResyncConnector = <ThrowOnError extends boolean = false>(options: Options<ForceResyncConnectorData, ThrowOnError>) => (options.client ?? client).post<ForceResyncConnectorResponses, ForceResyncConnectorErrors, ThrowOnError>({ url: '/api/connectors/{id}/force-resync', ...options });

/**
 * Test a connector connection
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `knowledgeSource:read`: View Knowledge Bases and Connectors
 */
export const testConnectorConnection = <ThrowOnError extends boolean = false>(options: Options<TestConnectorConnectionData, ThrowOnError>) => (options.client ?? client).post<TestConnectorConnectionResponses, TestConnectorConnectionErrors, ThrowOnError>({ url: '/api/connectors/{id}/test', ...options });

/**
 * Begin the Google authorization-code flow for a Google Drive connector in individual mode. Returns the URL to send the browser to; the connector being authorized travels in a signed state parameter, because Google matches the redirect URI exactly.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `knowledgeSource:update`: Modify Knowledge Bases and Connectors
 */
export const startGoogleDriveConnectorOAuth = <ThrowOnError extends boolean = false>(options: Options<StartGoogleDriveConnectorOAuthData, ThrowOnError>) => (options.client ?? client).post<StartGoogleDriveConnectorOAuthResponses, StartGoogleDriveConnectorOAuthErrors, ThrowOnError>({
    url: '/api/connectors/{id}/gdrive/oauth/start',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Where Google returns the browser after an individual Google Drive authorization. Requires the session of the person who started the flow: the signed state proves only that this deployment issued a flow, so on its own it would let one person's authorization be redeemed onto another person's connector.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `knowledgeSource:update`: Modify Knowledge Bases and Connectors
 */
export const completeGoogleDriveConnectorOAuth = <ThrowOnError extends boolean = false>(options?: Options<CompleteGoogleDriveConnectorOAuthData, ThrowOnError>) => (options?.client ?? client).get<CompleteGoogleDriveConnectorOAuthResponses, unknown, ThrowOnError>({ url: '/api/connectors/gdrive/oauth/callback', ...options });

/**
 * List knowledge bases assigned to a connector
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `knowledgeSource:read`: View Knowledge Bases and Connectors
 */
export const getConnectorKnowledgeBases = <ThrowOnError extends boolean = false>(options: Options<GetConnectorKnowledgeBasesData, ThrowOnError>) => (options.client ?? client).get<GetConnectorKnowledgeBasesResponses, GetConnectorKnowledgeBasesErrors, ThrowOnError>({ url: '/api/connectors/{id}/knowledge-bases', ...options });

/**
 * Assign a connector to one or more knowledge bases
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `knowledgeSource:update`: Modify Knowledge Bases and Connectors
 */
export const assignConnectorToKnowledgeBases = <ThrowOnError extends boolean = false>(options: Options<AssignConnectorToKnowledgeBasesData, ThrowOnError>) => (options.client ?? client).post<AssignConnectorToKnowledgeBasesResponses, AssignConnectorToKnowledgeBasesErrors, ThrowOnError>({
    url: '/api/connectors/{id}/knowledge-bases',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Unassign a connector from a knowledge base
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `knowledgeSource:update`: Modify Knowledge Bases and Connectors
 */
export const unassignConnectorFromKnowledgeBase = <ThrowOnError extends boolean = false>(options: Options<UnassignConnectorFromKnowledgeBaseData, ThrowOnError>) => (options.client ?? client).delete<UnassignConnectorFromKnowledgeBaseResponses, UnassignConnectorFromKnowledgeBaseErrors, ThrowOnError>({ url: '/api/connectors/{id}/knowledge-bases/{kbId}', ...options });

/**
 * List connector runs
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `knowledgeSource:read`: View Knowledge Bases and Connectors
 */
export const getConnectorRuns = <ThrowOnError extends boolean = false>(options: Options<GetConnectorRunsData, ThrowOnError>) => (options.client ?? client).get<GetConnectorRunsResponses, GetConnectorRunsErrors, ThrowOnError>({ url: '/api/connectors/{id}/runs', ...options });

/**
 * Get a single connector run (including logs)
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `knowledgeSource:read`: View Knowledge Bases and Connectors
 */
export const getConnectorRun = <ThrowOnError extends boolean = false>(options: Options<GetConnectorRunData, ThrowOnError>) => (options.client ?? client).get<GetConnectorRunResponses, GetConnectorRunErrors, ThrowOnError>({ url: '/api/connectors/{id}/runs/{runId}', ...options });

/**
 * Cancel an in-progress document sync. Documents already ingested remain available; the worker stops before writing its next source batch.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `knowledgeSource:update`: Modify Knowledge Bases and Connectors
 */
export const cancelConnectorRun = <ThrowOnError extends boolean = false>(options: Options<CancelConnectorRunData, ThrowOnError>) => (options.client ?? client).post<CancelConnectorRunResponses, CancelConnectorRunErrors, ThrowOnError>({ url: '/api/connectors/{id}/runs/{runId}/cancel', ...options });

/**
 * List documents in the knowledge file repository
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `knowledgeSource:read`: View Knowledge Bases and Connectors
 */
export const getKnowledgeFiles = <ThrowOnError extends boolean = false>(options?: Options<GetKnowledgeFilesData, ThrowOnError>) => (options?.client ?? client).get<GetKnowledgeFilesResponses, GetKnowledgeFilesErrors, ThrowOnError>({ url: '/api/knowledge-files', ...options });

/**
 * Upload a document into the knowledge file repository
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `knowledgeSource:create`: Create Knowledge Bases and Connectors
 */
export const uploadKnowledgeFile = <ThrowOnError extends boolean = false>(options: Options<UploadKnowledgeFileData, ThrowOnError>) => (options.client ?? client).post<UploadKnowledgeFileResponses, UploadKnowledgeFileErrors, ThrowOnError>({
    url: '/api/knowledge-files',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Copy a file attached to a chat into the knowledge file repository
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `knowledgeSource:create`: Create Knowledge Bases and Connectors
 */
export const promoteAttachmentToKnowledgeFile = <ThrowOnError extends boolean = false>(options: Options<PromoteAttachmentToKnowledgeFileData, ThrowOnError>) => (options.client ?? client).post<PromoteAttachmentToKnowledgeFileResponses, PromoteAttachmentToKnowledgeFileErrors, ThrowOnError>({
    url: '/api/knowledge-files/from-attachment',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Stream the bytes of a repository document. Safe types render inline (PDF, images, plain text); everything else downloads.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `knowledgeSource:read`: View Knowledge Bases and Connectors
 */
export const getKnowledgeFileContent = <ThrowOnError extends boolean = false>(options: Options<GetKnowledgeFileContentData, ThrowOnError>) => (options.client ?? client).get<GetKnowledgeFileContentResponses, unknown, ThrowOnError>({ url: '/api/knowledge-files/{fileId}/content', ...options });

/**
 * Delete a repository document and every document indexed from it
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `knowledgeSource:delete`: Delete Knowledge Bases and Connectors, view the deleted ones, and restore them
 */
export const deleteKnowledgeFile = <ThrowOnError extends boolean = false>(options: Options<DeleteKnowledgeFileData, ThrowOnError>) => (options.client ?? client).delete<DeleteKnowledgeFileResponses, DeleteKnowledgeFileErrors, ThrowOnError>({ url: '/api/knowledge-files/{fileId}', ...options });

/**
 * Rename a document, move it, or change who can see it
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `knowledgeSource:update`: Modify Knowledge Bases and Connectors
 */
export const updateKnowledgeFile = <ThrowOnError extends boolean = false>(options: Options<UpdateKnowledgeFileData, ThrowOnError>) => (options.client ?? client).patch<UpdateKnowledgeFileResponses, UpdateKnowledgeFileErrors, ThrowOnError>({
    url: '/api/knowledge-files/{fileId}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete several repository documents in one request, along with every document indexed from each. Ids the caller cannot see are reported in `failed` and leave the rest of the batch applied.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `knowledgeSource:delete`: Delete Knowledge Bases and Connectors, view the deleted ones, and restore them
 */
export const bulkDeleteKnowledgeFiles = <ThrowOnError extends boolean = false>(options: Options<BulkDeleteKnowledgeFilesData, ThrowOnError>) => (options.client ?? client).delete<BulkDeleteKnowledgeFilesResponses, BulkDeleteKnowledgeFilesErrors, ThrowOnError>({
    url: '/api/knowledge-files/bulk',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Update several repository documents in one request. Today the only bulk-editable surface is who can see them, and every document in the batch is moved to the same audience. The target teams are validated once for the whole request (a 400 changes nothing); documents the caller cannot see are reported in `failed` and leave the rest of the batch applied.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `knowledgeSource:update`: Modify Knowledge Bases and Connectors
 */
export const bulkUpdateKnowledgeFiles = <ThrowOnError extends boolean = false>(options: Options<BulkUpdateKnowledgeFilesData, ThrowOnError>) => (options.client ?? client).patch<BulkUpdateKnowledgeFilesResponses, BulkUpdateKnowledgeFilesErrors, ThrowOnError>({
    url: '/api/knowledge-files/bulk',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Index repository documents into a knowledge base, creating it when a name is given
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `knowledgeSource:update`: Modify Knowledge Bases and Connectors
 */
export const indexKnowledgeFiles = <ThrowOnError extends boolean = false>(options: Options<IndexKnowledgeFilesData, ThrowOnError>) => (options.client ?? client).post<IndexKnowledgeFilesResponses, IndexKnowledgeFilesErrors, ThrowOnError>({
    url: '/api/knowledge-files/index',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List knowledge file directories
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `knowledgeSource:read`: View Knowledge Bases and Connectors
 */
export const getKnowledgeDirectories = <ThrowOnError extends boolean = false>(options?: Options<GetKnowledgeDirectoriesData, ThrowOnError>) => (options?.client ?? client).get<GetKnowledgeDirectoriesResponses, GetKnowledgeDirectoriesErrors, ThrowOnError>({ url: '/api/knowledge-directories', ...options });

/**
 * Create a knowledge file directory
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `knowledgeSource:create`: Create Knowledge Bases and Connectors
 */
export const createKnowledgeDirectory = <ThrowOnError extends boolean = false>(options: Options<CreateKnowledgeDirectoryData, ThrowOnError>) => (options.client ?? client).post<CreateKnowledgeDirectoryResponses, CreateKnowledgeDirectoryErrors, ThrowOnError>({
    url: '/api/knowledge-directories',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete a directory; its files move to the repository root
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `knowledgeSource:delete`: Delete Knowledge Bases and Connectors, view the deleted ones, and restore them
 */
export const deleteKnowledgeDirectory = <ThrowOnError extends boolean = false>(options: Options<DeleteKnowledgeDirectoryData, ThrowOnError>) => (options.client ?? client).delete<DeleteKnowledgeDirectoryResponses, DeleteKnowledgeDirectoryErrors, ThrowOnError>({ url: '/api/knowledge-directories/{directoryId}', ...options });

/**
 * Rename a directory or change who can see it
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `knowledgeSource:update`: Modify Knowledge Bases and Connectors
 */
export const updateKnowledgeDirectory = <ThrowOnError extends boolean = false>(options: Options<UpdateKnowledgeDirectoryData, ThrowOnError>) => (options.client ?? client).patch<UpdateKnowledgeDirectoryResponses, UpdateKnowledgeDirectoryErrors, ThrowOnError>({
    url: '/api/knowledge-directories/{directoryId}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete several directories in one request. As with the single delete, the files inside each move to the repository root rather than being destroyed. Ids that match no directory are reported in `failed` and leave the rest of the batch applied.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `knowledgeSource:delete`: Delete Knowledge Bases and Connectors, view the deleted ones, and restore them
 */
export const bulkDeleteKnowledgeDirectories = <ThrowOnError extends boolean = false>(options: Options<BulkDeleteKnowledgeDirectoriesData, ThrowOnError>) => (options.client ?? client).delete<BulkDeleteKnowledgeDirectoriesResponses, BulkDeleteKnowledgeDirectoriesErrors, ThrowOnError>({
    url: '/api/knowledge-directories/bulk',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Change who can see several directories in one request. The target teams are validated once for the whole request; per-directory problems are reported in `failed` and leave the rest applied. Directories are the other half of a repository selection, so this is the companion to the documents bulk update.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `knowledgeSource:update`: Modify Knowledge Bases and Connectors
 */
export const bulkUpdateKnowledgeDirectories = <ThrowOnError extends boolean = false>(options: Options<BulkUpdateKnowledgeDirectoriesData, ThrowOnError>) => (options.client ?? client).patch<BulkUpdateKnowledgeDirectoriesResponses, BulkUpdateKnowledgeDirectoriesErrors, ThrowOnError>({
    url: '/api/knowledge-directories/bulk',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get all limits with optional filtering and per-model usage breakdown
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `llmLimit:read`: View token usage limits
 */
export const getLimits = <ThrowOnError extends boolean = false>(options?: Options<GetLimitsData, ThrowOnError>) => (options?.client ?? client).get<GetLimitsResponses, GetLimitsErrors, ThrowOnError>({ url: '/api/limits', ...options });

/**
 * Create a new limit
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `llmLimit:create`: Create new usage limits
 */
export const createLimit = <ThrowOnError extends boolean = false>(options: Options<CreateLimitData, ThrowOnError>) => (options.client ?? client).post<CreateLimitResponses, CreateLimitErrors, ThrowOnError>({
    url: '/api/limits',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete a limit
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `llmLimit:delete`: Remove usage limits
 */
export const deleteLimit = <ThrowOnError extends boolean = false>(options: Options<DeleteLimitData, ThrowOnError>) => (options.client ?? client).delete<DeleteLimitResponses, DeleteLimitErrors, ThrowOnError>({ url: '/api/limits/{id}', ...options });

/**
 * Get a limit by ID
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `llmLimit:read`: View token usage limits
 */
export const getLimit = <ThrowOnError extends boolean = false>(options: Options<GetLimitData, ThrowOnError>) => (options.client ?? client).get<GetLimitResponses, GetLimitErrors, ThrowOnError>({ url: '/api/limits/{id}', ...options });

/**
 * Update a limit
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `llmLimit:update`: Modify existing usage limits
 */
export const updateLimit = <ThrowOnError extends boolean = false>(options: Options<UpdateLimitData, ThrowOnError>) => (options.client ?? client).patch<UpdateLimitResponses, UpdateLimitErrors, ThrowOnError>({
    url: '/api/limits/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete several limits in one request. Missing or out-of-organization ids are reported in `failed` while visible limits are deleted.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `llmLimit:delete`: Remove usage limits
 */
export const bulkDeleteLimits = <ThrowOnError extends boolean = false>(options: Options<BulkDeleteLimitsData, ThrowOnError>) => (options.client ?? client).delete<BulkDeleteLimitsResponses, BulkDeleteLimitsErrors, ThrowOnError>({
    url: '/api/limits/bulk',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get available LLM models from configured provider API keys. Models are fetched from the provider-backed catalog and include capabilities when available.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `llmModel:read`: View synced LLM models and capabilities
 */
export const getLlmModels = <ThrowOnError extends boolean = false>(options?: Options<GetLlmModelsData, ThrowOnError>) => (options?.client ?? client).get<GetLlmModelsResponses, GetLlmModelsErrors, ThrowOnError>({ url: '/api/llm-models/available', ...options });

/**
 * Sync models from providers for all visible API keys and store them in the database
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `llmModel:update`: Modify LLM model pricing, modality and generation-parameter settings
 */
export const syncLlmModels = <ThrowOnError extends boolean = false>(options?: Options<SyncLlmModelsData, ThrowOnError>) => (options?.client ?? client).post<SyncLlmModelsResponses, SyncLlmModelsErrors, ThrowOnError>({ url: '/api/llm-models/sync', ...options });

/**
 * Get all synced LLM models with their linked provider API keys.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `llmModel:read`: View synced LLM models and capabilities
 */
export const getModelsWithApiKeys = <ThrowOnError extends boolean = false>(options?: Options<GetModelsWithApiKeysData, ThrowOnError>) => (options?.client ?? client).get<GetModelsWithApiKeysResponses, GetModelsWithApiKeysErrors, ThrowOnError>({ url: '/api/llm-models', ...options });

/**
 * Update several LLM models in one request. Today the only bulk-editable field is `ignored`, which hides a model from the pickers without deleting anything. Ids that match no model are reported in `failed` and leave the rest of the batch applied.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `llmModel:update`: Modify LLM model pricing, modality and generation-parameter settings
 */
export const bulkUpdateModels = <ThrowOnError extends boolean = false>(options: Options<BulkUpdateModelsData, ThrowOnError>) => (options.client ?? client).patch<BulkUpdateModelsResponses, BulkUpdateModelsErrors, ThrowOnError>({
    url: '/api/llm-models/bulk',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Update LLM model details including custom pricing and modalities.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `llmModel:update`: Modify LLM model pricing, modality and generation-parameter settings
 */
export const updateModel = <ThrowOnError extends boolean = false>(options: Options<UpdateModelData, ThrowOnError>) => (options.client ?? client).patch<UpdateModelResponses, UpdateModelErrors, ThrowOnError>({
    url: '/api/llm-models/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List LLM OAuth clients that can access LLM proxies
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `llmOauthClient:read`: View LLM OAuth client registrations
 */
export const getLlmOauthClients = <ThrowOnError extends boolean = false>(options?: Options<GetLlmOauthClientsData, ThrowOnError>) => (options?.client ?? client).get<GetLlmOauthClientsResponses, GetLlmOauthClientsErrors, ThrowOnError>({ url: '/api/llm-oauth-clients', ...options });

/**
 * Create an LLM OAuth client and return its client secret once
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `llmOauthClient:create`: Create LLM OAuth client registrations
 */
export const createLlmOauthClient = <ThrowOnError extends boolean = false>(options: Options<CreateLlmOauthClientData, ThrowOnError>) => (options.client ?? client).post<CreateLlmOauthClientResponses, CreateLlmOauthClientErrors, ThrowOnError>({
    url: '/api/llm-oauth-clients',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete an LLM OAuth client
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `llmOauthClient:delete`: Delete LLM OAuth client registrations
 */
export const deleteLlmOauthClient = <ThrowOnError extends boolean = false>(options: Options<DeleteLlmOauthClientData, ThrowOnError>) => (options.client ?? client).delete<DeleteLlmOauthClientResponses, DeleteLlmOauthClientErrors, ThrowOnError>({ url: '/api/llm-oauth-clients/{id}', ...options });

/**
 * Update an LLM OAuth client
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `llmOauthClient:update`: Modify LLM OAuth client registrations
 */
export const updateLlmOauthClient = <ThrowOnError extends boolean = false>(options: Options<UpdateLlmOauthClientData, ThrowOnError>) => (options.client ?? client).put<UpdateLlmOauthClientResponses, UpdateLlmOauthClientErrors, ThrowOnError>({
    url: '/api/llm-oauth-clients/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Rotate an LLM OAuth client's client secret
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `llmOauthClient:update`: Modify LLM OAuth client registrations
 */
export const rotateLlmOauthClientSecret = <ThrowOnError extends boolean = false>(options: Options<RotateLlmOauthClientSecretData, ThrowOnError>) => (options.client ?? client).post<RotateLlmOauthClientSecretResponses, RotateLlmOauthClientSecretErrors, ThrowOnError>({ url: '/api/llm-oauth-clients/{id}/rotate-secret', ...options });

/**
 * Delete several LLM OAuth clients in one request. Each id is authorized exactly as the single-client delete authorizes its own, so a client the caller cannot see or manage is reported in `failed` while the rest of the batch still applies.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `llmOauthClient:delete`: Delete LLM OAuth client registrations
 */
export const bulkDeleteLlmOauthClients = <ThrowOnError extends boolean = false>(options: Options<BulkDeleteLlmOauthClientsData, ThrowOnError>) => (options.client ?? client).delete<BulkDeleteLlmOauthClientsResponses, BulkDeleteLlmOauthClientsErrors, ThrowOnError>({
    url: '/api/llm-oauth-clients/bulk',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get all LLM provider API keys visible to the current user based on scope access
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `llmProviderApiKey:read`: View LLM provider API keys
 */
export const getLlmProviderApiKeys = <ThrowOnError extends boolean = false>(options?: Options<GetLlmProviderApiKeysData, ThrowOnError>) => (options?.client ?? client).get<GetLlmProviderApiKeysResponses, GetLlmProviderApiKeysErrors, ThrowOnError>({ url: '/api/llm-provider-api-keys', ...options });

/**
 * Create a new LLM provider API key with specified visibility
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * None (no additional RBAC permission required)
 */
export const createLlmProviderApiKey = <ThrowOnError extends boolean = false>(options: Options<CreateLlmProviderApiKeyData, ThrowOnError>) => (options.client ?? client).post<CreateLlmProviderApiKeyResponses, CreateLlmProviderApiKeyErrors, ThrowOnError>({
    url: '/api/llm-provider-api-keys',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get LLM provider API keys available for the current user to use
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `llmProviderApiKey:read`: View LLM provider API keys
 */
export const getAvailableLlmProviderApiKeys = <ThrowOnError extends boolean = false>(options?: Options<GetAvailableLlmProviderApiKeysData, ThrowOnError>) => (options?.client ?? client).get<GetAvailableLlmProviderApiKeysResponses, GetAvailableLlmProviderApiKeysErrors, ThrowOnError>({ url: '/api/llm-provider-api-keys/available', ...options });

/**
 * Delete an LLM provider API key
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `llmProviderApiKey:delete`: Remove LLM provider API keys
 */
export const deleteLlmProviderApiKey = <ThrowOnError extends boolean = false>(options: Options<DeleteLlmProviderApiKeyData, ThrowOnError>) => (options.client ?? client).delete<DeleteLlmProviderApiKeyResponses, DeleteLlmProviderApiKeyErrors, ThrowOnError>({ url: '/api/llm-provider-api-keys/{id}', ...options });

/**
 * Get a specific LLM provider API key
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `llmProviderApiKey:read`: View LLM provider API keys
 */
export const getLlmProviderApiKey = <ThrowOnError extends boolean = false>(options: Options<GetLlmProviderApiKeyData, ThrowOnError>) => (options.client ?? client).get<GetLlmProviderApiKeyResponses, GetLlmProviderApiKeyErrors, ThrowOnError>({ url: '/api/llm-provider-api-keys/{id}', ...options });

/**
 * Update an LLM provider API key (name, API key value, visibility, or team)
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `llmProviderApiKey:update`: Modify LLM provider API key configuration and visibility
 */
export const updateLlmProviderApiKey = <ThrowOnError extends boolean = false>(options: Options<UpdateLlmProviderApiKeyData, ThrowOnError>) => (options.client ?? client).patch<UpdateLlmProviderApiKeyResponses, UpdateLlmProviderApiKeyErrors, ThrowOnError>({
    url: '/api/llm-provider-api-keys/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Re-authenticate the caller's own personal subscription key in place, rotating its stored credential
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * None (no additional RBAC permission required)
 */
export const reconnectLlmProviderApiKey = <ThrowOnError extends boolean = false>(options: Options<ReconnectLlmProviderApiKeyData, ThrowOnError>) => (options.client ?? client).post<ReconnectLlmProviderApiKeyResponses, ReconnectLlmProviderApiKeyErrors, ThrowOnError>({
    url: '/api/llm-provider-api-keys/{id}/reconnect',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete several LLM provider API keys in one request. Keys outside the caller's organization, protected system keys, and keys that are still in use are reported in `failed` while the remaining keys are deleted.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `llmProviderApiKey:delete`: Remove LLM provider API keys
 * `llmProviderApiKey:read`: View LLM provider API keys
 */
export const bulkDeleteLlmProviderApiKeys = <ThrowOnError extends boolean = false>(options: Options<BulkDeleteLlmProviderApiKeysData, ThrowOnError>) => (options.client ?? client).delete<BulkDeleteLlmProviderApiKeysResponses, BulkDeleteLlmProviderApiKeysErrors, ThrowOnError>({
    url: '/api/llm-provider-api-keys/bulk',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get the LLM Proxy
 *
 * Authentication:
 *
 * This route accepts either an LLM provider API key or a Virtual API Key. See [LLM Proxy Authentication](/docs/platform-llm-proxy-authentication).
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `llmProxy:read`: View the LLM Proxy and its connection details
 */
export const getLlmProxy = <ThrowOnError extends boolean = false>(options?: Options<GetLlmProxyData, ThrowOnError>) => (options?.client ?? client).get<GetLlmProxyResponses, GetLlmProxyErrors, ThrowOnError>({ url: '/api/llm-proxy', ...options });

/**
 * Update the LLM Proxy configuration
 *
 * Authentication:
 *
 * This route accepts either an LLM provider API key or a Virtual API Key. See [LLM Proxy Authentication](/docs/platform-llm-proxy-authentication).
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `llmProxy:update`: Modify LLM Proxy configuration
 */
export const updateLlmProxy = <ThrowOnError extends boolean = false>(options: Options<UpdateLlmProxyData, ThrowOnError>) => (options.client ?? client).patch<UpdateLlmProxyResponses, UpdateLlmProxyErrors, ThrowOnError>({
    url: '/api/llm-proxy',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Proxy an MCP App's runtime requests with session auth
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * None (no additional RBAC permission required)
 */
export const mcpAppProxyPost = <ThrowOnError extends boolean = false>(options: Options<McpAppProxyPostData, ThrowOnError>) => (options.client ?? client).post<McpAppProxyPostResponses, unknown, ThrowOnError>({
    url: '/api/mcp/app/{appId}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

export const mcpGatewayGet = <ThrowOnError extends boolean = false>(options: Options<McpGatewayGetData, ThrowOnError>) => (options.client ?? client).get<McpGatewayGetResponses, McpGatewayGetErrors, ThrowOnError>({ url: '/v1/mcp/{profileId}', ...options });

export const mcpGatewayPost = <ThrowOnError extends boolean = false>(options: Options<McpGatewayPostData, ThrowOnError>) => (options.client ?? client).post<McpGatewayPostResponses, unknown, ThrowOnError>({
    url: '/v1/mcp/{profileId}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List MCP OAuth clients that can access MCP gateways
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `mcpOauthClient:read`: View MCP OAuth client registrations
 */
export const getMcpOauthClients = <ThrowOnError extends boolean = false>(options?: Options<GetMcpOauthClientsData, ThrowOnError>) => (options?.client ?? client).get<GetMcpOauthClientsResponses, GetMcpOauthClientsErrors, ThrowOnError>({ url: '/api/mcp-oauth-clients', ...options });

/**
 * Create an MCP OAuth client and return its client secret once
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `mcpOauthClient:create`: Create MCP OAuth client registrations
 */
export const createMcpOauthClient = <ThrowOnError extends boolean = false>(options: Options<CreateMcpOauthClientData, ThrowOnError>) => (options.client ?? client).post<CreateMcpOauthClientResponses, CreateMcpOauthClientErrors, ThrowOnError>({
    url: '/api/mcp-oauth-clients',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete an MCP OAuth client
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `mcpOauthClient:delete`: Delete MCP OAuth client registrations
 */
export const deleteMcpOauthClient = <ThrowOnError extends boolean = false>(options: Options<DeleteMcpOauthClientData, ThrowOnError>) => (options.client ?? client).delete<DeleteMcpOauthClientResponses, DeleteMcpOauthClientErrors, ThrowOnError>({ url: '/api/mcp-oauth-clients/{id}', ...options });

/**
 * Update an MCP OAuth client
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `mcpOauthClient:update`: Modify MCP OAuth client registrations
 */
export const updateMcpOauthClient = <ThrowOnError extends boolean = false>(options: Options<UpdateMcpOauthClientData, ThrowOnError>) => (options.client ?? client).put<UpdateMcpOauthClientResponses, UpdateMcpOauthClientErrors, ThrowOnError>({
    url: '/api/mcp-oauth-clients/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Rotate an MCP OAuth client's client secret
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `mcpOauthClient:update`: Modify MCP OAuth client registrations
 */
export const rotateMcpOauthClientSecret = <ThrowOnError extends boolean = false>(options: Options<RotateMcpOauthClientSecretData, ThrowOnError>) => (options.client ?? client).post<RotateMcpOauthClientSecretResponses, RotateMcpOauthClientSecretErrors, ThrowOnError>({ url: '/api/mcp-oauth-clients/{id}/rotate-secret', ...options });

/**
 * Proxy MCP Gateway requests with session auth
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * None (no additional RBAC permission required)
 */
export const mcpProxyPost = <ThrowOnError extends boolean = false>(options: Options<McpProxyPostData, ThrowOnError>) => (options.client ?? client).post<McpProxyPostResponses, unknown, ThrowOnError>({
    url: '/api/mcp/{agentId}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Proxy a server-scoped MCP App's runtime requests with session auth
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * None (no additional RBAC permission required)
 */
export const mcpServerProxyPost = <ThrowOnError extends boolean = false>(options: Options<McpServerProxyPostData, ThrowOnError>) => (options.client ?? client).post<McpServerProxyPostResponses, unknown, ThrowOnError>({
    url: '/api/mcp/server/{mcpServerId}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get the organization's auto-mode agents (implicit access to all tools). The set is org-wide — these agents can reach every MCP server — so it is served once here instead of being embedded on every server row.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `mcpServerInstallation:read`: View installed MCP servers and their status
 */
export const getMcpServerAutoModeAgents = <ThrowOnError extends boolean = false>(options?: Options<GetMcpServerAutoModeAgentsData, ThrowOnError>) => (options?.client ?? client).get<GetMcpServerAutoModeAgentsResponses, GetMcpServerAutoModeAgentsErrors, ThrowOnError>({ url: '/api/mcp_server/auto_mode_agents', ...options });

/**
 * Get all installed MCP servers
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `mcpServerInstallation:read`: View installed MCP servers and their status
 */
export const getMcpServers = <ThrowOnError extends boolean = false>(options?: Options<GetMcpServersData, ThrowOnError>) => (options?.client ?? client).get<GetMcpServersResponses, GetMcpServersErrors, ThrowOnError>({ url: '/api/mcp_server', ...options });

/**
 * Install an MCP server (from catalog or custom)
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `mcpServerInstallation:create`: Install MCP servers from the registry
 */
export const installMcpServer = <ThrowOnError extends boolean = false>(options: Options<InstallMcpServerData, ThrowOnError>) => (options.client ?? client).post<InstallMcpServerResponses, InstallMcpServerErrors, ThrowOnError>({
    url: '/api/mcp_server',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete/uninstall an MCP server
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `mcpServerInstallation:delete`: Uninstall MCP servers
 */
export const deleteMcpServer = <ThrowOnError extends boolean = false>(options: Options<DeleteMcpServerData, ThrowOnError>) => (options.client ?? client).delete<DeleteMcpServerResponses, DeleteMcpServerErrors, ThrowOnError>({ url: '/api/mcp_server/{id}', ...options });

/**
 * Get MCP server by ID
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `mcpServerInstallation:read`: View installed MCP servers and their status
 */
export const getMcpServer = <ThrowOnError extends boolean = false>(options: Options<GetMcpServerData, ThrowOnError>) => (options.client ?? client).get<GetMcpServerResponses, GetMcpServerErrors, ThrowOnError>({ url: '/api/mcp_server/{id}', ...options });

/**
 * Update MCP server secret after re-authentication (clears OAuth refresh errors)
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `mcpServerInstallation:create`: Install MCP servers from the registry
 */
export const reauthenticateMcpServer = <ThrowOnError extends boolean = false>(options: Options<ReauthenticateMcpServerData, ThrowOnError>) => (options.client ?? client).patch<ReauthenticateMcpServerResponses, ReauthenticateMcpServerErrors, ThrowOnError>({
    url: '/api/mcp_server/{id}/reauthenticate',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Uninstall several MCP servers in one request. Each id is authorized exactly as the single uninstall authorizes its own, so a built-in server, an app-backing server (owned by the Apps lifecycle), or one the caller may not revoke is reported in `failed` while the rest of the batch still applies. Uninstall is a recoverable soft delete: stored credentials are retained so a restore recovers them, and only the live Kubernetes Secret is torn down.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `mcpServerInstallation:delete`: Uninstall MCP servers
 */
export const bulkDeleteMcpServers = <ThrowOnError extends boolean = false>(options: Options<BulkDeleteMcpServersData, ThrowOnError>) => (options.client ?? client).delete<BulkDeleteMcpServersResponses, BulkDeleteMcpServersErrors, ThrowOnError>({
    url: '/api/mcp_server/bulk',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Restore a soft-deleted (uninstalled) MCP server. Flag-only: the server is marked for manual reinstall, not re-provisioned.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `mcpServerInstallation:manage-deleted`: View and restore soft-deleted (uninstalled) MCP servers
 */
export const restoreMcpServer = <ThrowOnError extends boolean = false>(options: Options<RestoreMcpServerData, ThrowOnError>) => (options.client ?? client).post<RestoreMcpServerResponses, RestoreMcpServerErrors, ThrowOnError>({ url: '/api/mcp_server/{id}/restore', ...options });

/**
 * Restore one dismissed MCP connection alert to the calling user's view.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `mcpServerInstallation:read`: View installed MCP servers and their status
 */
export const unmuteMcpServerAlert = <ThrowOnError extends boolean = false>(options: Options<UnmuteMcpServerAlertData, ThrowOnError>) => (options.client ?? client).delete<UnmuteMcpServerAlertResponses, UnmuteMcpServerAlertErrors, ThrowOnError>({ url: '/api/mcp_server/{id}/alert-mutes/{kind}', ...options });

/**
 * Dismiss one alert on an MCP connection for the calling user only. The fingerprint pins it to one failure episode; dismissing again replaces the previous decision.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `mcpServerInstallation:read`: View installed MCP servers and their status
 */
export const muteMcpServerAlert = <ThrowOnError extends boolean = false>(options: Options<MuteMcpServerAlertData, ThrowOnError>) => (options.client ?? client).put<MuteMcpServerAlertResponses, MuteMcpServerAlertErrors, ThrowOnError>({
    url: '/api/mcp_server/{id}/alert-mutes/{kind}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get the installation status of an MCP server (for polling during local server installation)
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `mcpServerInstallation:read`: View installed MCP servers and their status
 */
export const getMcpServerInstallationStatus = <ThrowOnError extends boolean = false>(options: Options<GetMcpServerInstallationStatusData, ThrowOnError>) => (options.client ?? client).get<GetMcpServerInstallationStatusResponses, GetMcpServerInstallationStatusErrors, ThrowOnError>({ url: '/api/mcp_server/{id}/installation-status', ...options });

/**
 * Get all tools for an MCP server
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `mcpServerInstallation:read`: View installed MCP servers and their status
 */
export const getMcpServerTools = <ThrowOnError extends boolean = false>(options: Options<GetMcpServerToolsData, ThrowOnError>) => (options.client ?? client).get<GetMcpServerToolsResponses, GetMcpServerToolsErrors, ThrowOnError>({ url: '/api/mcp_server/{id}/tools', ...options });

/**
 * Inspect a running MCP server (list tools or call a tool)
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `mcpServerInstallation:read`: View installed MCP servers and their status
 */
export const inspectMcpServer = <ThrowOnError extends boolean = false>(options: Options<InspectMcpServerData, ThrowOnError>) => (options.client ?? client).post<InspectMcpServerResponses, InspectMcpServerErrors, ThrowOnError>({
    url: '/api/mcp_server/{id}/inspect',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Reinstall an MCP server without losing tool assignments and policies
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `mcpServerInstallation:create`: Install MCP servers from the registry
 */
export const reinstallMcpServer = <ThrowOnError extends boolean = false>(options: Options<ReinstallMcpServerData, ThrowOnError>) => (options.client ?? client).post<ReinstallMcpServerResponses, ReinstallMcpServerErrors, ThrowOnError>({
    url: '/api/mcp_server/{id}/reinstall',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Destroy and redeploy a stuck MCP server deployment from a clean slate
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `mcpServerInstallation:admin`: Approve or manage all MCP server installations
 */
export const hardResetMcpServer = <ThrowOnError extends boolean = false>(options: Options<HardResetMcpServerData, ThrowOnError>) => (options.client ?? client).post<HardResetMcpServerResponses, HardResetMcpServerErrors, ThrowOnError>({ url: '/api/mcp_server/{id}/hard-reset', ...options });

/**
 * Re-discover an MCP server's tools from the live server and refresh the stored tool catalog without reinstalling
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `mcpServerInstallation:create`: Install MCP servers from the registry
 */
export const reloadMcpServerTools = <ThrowOnError extends boolean = false>(options: Options<ReloadMcpServerToolsData, ThrowOnError>) => (options.client ?? client).post<ReloadMcpServerToolsResponses, ReloadMcpServerToolsErrors, ThrowOnError>({ url: '/api/mcp_server/{id}/reload-tools', ...options });

/**
 * Get all MCP tool calls with pagination and sorting
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `log:read`: View your own LLM proxy and MCP tool call logs
 */
export const getMcpToolCalls = <ThrowOnError extends boolean = false>(options?: Options<GetMcpToolCallsData, ThrowOnError>) => (options?.client ?? client).get<GetMcpToolCallsResponses, GetMcpToolCallsErrors, ThrowOnError>({ url: '/api/mcp-tool-calls', ...options });

/**
 * Get MCP tool call by ID
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `log:read`: View your own LLM proxy and MCP tool call logs
 */
export const getMcpToolCall = <ThrowOnError extends boolean = false>(options: Options<GetMcpToolCallData, ThrowOnError>) => (options.client ?? client).get<GetMcpToolCallResponses, GetMcpToolCallErrors, ThrowOnError>({ url: '/api/mcp-tool-calls/{mcpToolCallId}', ...options });

/**
 * Get all members of the organization with pagination and optional filters
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `member:read`: View organization members and their roles
 */
export const getMembers = <ThrowOnError extends boolean = false>(options?: Options<GetMembersData, ThrowOnError>) => (options?.client ?? client).get<GetMembersResponses, GetMembersErrors, ThrowOnError>({ url: '/api/members', ...options });

/**
 * Remove accepted members or withdraw pending-signup placeholders in one request. Each target keeps its kind in the partial outcome.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `member:delete`: Remove members from the organization
 */
export const bulkDeleteMembers = <ThrowOnError extends boolean = false>(options: Options<BulkDeleteMembersData, ThrowOnError>) => (options.client ?? client).delete<BulkDeleteMembersResponses, BulkDeleteMembersErrors, ThrowOnError>({
    url: '/api/members/bulk',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Serve the M-Files connector's VAF Add On install bootstrap: a tiny PowerShell script that fetches the static installer and runs it with the server-resolved package source. Identical for every caller — it carries no credentials or per-user state.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * None (no additional RBAC permission required)
 */
export const getMfilesVafAddOnScript = <ThrowOnError extends boolean = false>(options?: Options<GetMfilesVafAddOnScriptData, ThrowOnError>) => (options?.client ?? client).get<GetMfilesVafAddOnScriptResponses, unknown, ThrowOnError>({ url: '/api/mfiles-vaf-add-on/script', ...options });

/**
 * Resolve how the M-Files connector's VAF Add On is distributed to this installation: a verified direct package download URL (release asset or the dev source-ref CI build proxied by the backend), or null when the install script compiles from source instead. The connector form probes this for its download link.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * None (no additional RBAC permission required)
 */
export const getMfilesVafAddOnDistribution = <ThrowOnError extends boolean = false>(options?: Options<GetMfilesVafAddOnDistributionData, ThrowOnError>) => (options?.client ?? client).get<GetMfilesVafAddOnDistributionResponses, GetMfilesVafAddOnDistributionErrors, ThrowOnError>({ url: '/api/mfiles-vaf-add-on/distribution', ...options });

/**
 * Serve the M-Files connector's VAF Add On package built by CI for the configured development source ref (ARCHESTRA_KNOWLEDGE_BASE_MFILES_VAF_ADD_ON_SOURCE_REF). 404 when the override is unset or no CI build exists for it.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * None (no additional RBAC permission required)
 */
export const getMfilesVafAddOnPackage = <ThrowOnError extends boolean = false>(options?: Options<GetMfilesVafAddOnPackageData, ThrowOnError>) => (options?.client ?? client).get<GetMfilesVafAddOnPackageResponses, unknown, ThrowOnError>({ url: '/api/mfiles-vaf-add-on/package', ...options });

/**
 * Start the Entra ID device flow used to connect a Microsoft 365 Copilot account
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * None (no additional RBAC permission required)
 */
export const microsoft365CopilotDeviceAuthStart = <ThrowOnError extends boolean = false>(options?: Options<Microsoft365CopilotDeviceAuthStartData, ThrowOnError>) => (options?.client ?? client).post<Microsoft365CopilotDeviceAuthStartResponses, Microsoft365CopilotDeviceAuthStartErrors, ThrowOnError>({ url: '/api/microsoft-365-copilot-auth/device/start', ...options });

/**
 * Poll the Entra ID device flow once; returns the refresh token when the user has authorized
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * None (no additional RBAC permission required)
 */
export const microsoft365CopilotDeviceAuthPoll = <ThrowOnError extends boolean = false>(options: Options<Microsoft365CopilotDeviceAuthPollData, ThrowOnError>) => (options.client ?? client).post<Microsoft365CopilotDeviceAuthPollResponses, Microsoft365CopilotDeviceAuthPollErrors, ThrowOnError>({
    url: '/api/microsoft-365-copilot-auth/device/poll',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Create a chat completion with Microsoft 365 Copilot (uses default agent)
 *
 * Authentication:
 *
 * This route accepts either an LLM provider API key or a Virtual API Key. See [LLM Proxy Authentication](/docs/platform-llm-proxy-authentication).
 */
export const microsoft365CopilotChatCompletionsWithDefaultAgent = <ThrowOnError extends boolean = false>(options: Options<Microsoft365CopilotChatCompletionsWithDefaultAgentData, ThrowOnError>) => (options.client ?? client).post<Microsoft365CopilotChatCompletionsWithDefaultAgentResponses, Microsoft365CopilotChatCompletionsWithDefaultAgentErrors, ThrowOnError>({
    url: '/v1/microsoft-365-copilot/chat/completions',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Create a chat completion with Microsoft 365 Copilot for a specific agent
 *
 * Authentication:
 *
 * This route accepts either an LLM provider API key or a Virtual API Key. See [LLM Proxy Authentication](/docs/platform-llm-proxy-authentication).
 */
export const microsoft365CopilotChatCompletionsWithAgent = <ThrowOnError extends boolean = false>(options: Options<Microsoft365CopilotChatCompletionsWithAgentData, ThrowOnError>) => (options.client ?? client).post<Microsoft365CopilotChatCompletionsWithAgentResponses, Microsoft365CopilotChatCompletionsWithAgentErrors, ThrowOnError>({
    url: '/v1/microsoft-365-copilot/{agentId}/chat/completions',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List Microsoft 365 Copilot models (default agent)
 *
 * Authentication:
 *
 * This route accepts either an LLM provider API key or a Virtual API Key. See [LLM Proxy Authentication](/docs/platform-llm-proxy-authentication).
 */
export const microsoft365CopilotListModelsWithDefaultAgent = <ThrowOnError extends boolean = false>(options?: Options<Microsoft365CopilotListModelsWithDefaultAgentData, ThrowOnError>) => (options?.client ?? client).get<Microsoft365CopilotListModelsWithDefaultAgentResponses, Microsoft365CopilotListModelsWithDefaultAgentErrors, ThrowOnError>({ url: '/v1/microsoft-365-copilot/models', ...options });

/**
 * List Microsoft 365 Copilot models (specific agent)
 *
 * Authentication:
 *
 * This route accepts either an LLM provider API key or a Virtual API Key. See [LLM Proxy Authentication](/docs/platform-llm-proxy-authentication).
 */
export const microsoft365CopilotListModelsWithAgent = <ThrowOnError extends boolean = false>(options: Options<Microsoft365CopilotListModelsWithAgentData, ThrowOnError>) => (options.client ?? client).get<Microsoft365CopilotListModelsWithAgentResponses, Microsoft365CopilotListModelsWithAgentErrors, ThrowOnError>({ url: '/v1/microsoft-365-copilot/{agentId}/models', ...options });

/**
 * Create a chat completion with MiniMax (uses default agent)
 *
 * Authentication:
 *
 * This route accepts either an LLM provider API key or a Virtual API Key. See [LLM Proxy Authentication](/docs/platform-llm-proxy-authentication).
 */
export const minimaxChatCompletionsWithDefaultAgent = <ThrowOnError extends boolean = false>(options: Options<MinimaxChatCompletionsWithDefaultAgentData, ThrowOnError>) => (options.client ?? client).post<MinimaxChatCompletionsWithDefaultAgentResponses, MinimaxChatCompletionsWithDefaultAgentErrors, ThrowOnError>({
    url: '/v1/minimax/chat/completions',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Create a chat completion with MiniMax for a specific agent
 *
 * Authentication:
 *
 * This route accepts either an LLM provider API key or a Virtual API Key. See [LLM Proxy Authentication](/docs/platform-llm-proxy-authentication).
 */
export const minimaxChatCompletionsWithAgent = <ThrowOnError extends boolean = false>(options: Options<MinimaxChatCompletionsWithAgentData, ThrowOnError>) => (options.client ?? client).post<MinimaxChatCompletionsWithAgentResponses, MinimaxChatCompletionsWithAgentErrors, ThrowOnError>({
    url: '/v1/minimax/{agentId}/chat/completions',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Create embeddings with Mistral (uses default agent)
 *
 * Authentication:
 *
 * This route accepts either an LLM provider API key or a Virtual API Key. See [LLM Proxy Authentication](/docs/platform-llm-proxy-authentication).
 */
export const mistralEmbeddingsWithDefaultAgent = <ThrowOnError extends boolean = false>(options: Options<MistralEmbeddingsWithDefaultAgentData, ThrowOnError>) => (options.client ?? client).post<MistralEmbeddingsWithDefaultAgentResponses, MistralEmbeddingsWithDefaultAgentErrors, ThrowOnError>({
    url: '/v1/mistral/embeddings',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Create embeddings with Mistral for a specific agent
 *
 * Authentication:
 *
 * This route accepts either an LLM provider API key or a Virtual API Key. See [LLM Proxy Authentication](/docs/platform-llm-proxy-authentication).
 */
export const mistralEmbeddingsWithAgent = <ThrowOnError extends boolean = false>(options: Options<MistralEmbeddingsWithAgentData, ThrowOnError>) => (options.client ?? client).post<MistralEmbeddingsWithAgentResponses, MistralEmbeddingsWithAgentErrors, ThrowOnError>({
    url: '/v1/mistral/{agentId}/embeddings',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Create a chat completion with Mistral (uses default agent)
 *
 * Authentication:
 *
 * This route accepts either an LLM provider API key or a Virtual API Key. See [LLM Proxy Authentication](/docs/platform-llm-proxy-authentication).
 */
export const mistralChatCompletionsWithDefaultAgent = <ThrowOnError extends boolean = false>(options: Options<MistralChatCompletionsWithDefaultAgentData, ThrowOnError>) => (options.client ?? client).post<MistralChatCompletionsWithDefaultAgentResponses, MistralChatCompletionsWithDefaultAgentErrors, ThrowOnError>({
    url: '/v1/mistral/chat/completions',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Create a chat completion with Mistral for a specific agent
 *
 * Authentication:
 *
 * This route accepts either an LLM provider API key or a Virtual API Key. See [LLM Proxy Authentication](/docs/platform-llm-proxy-authentication).
 */
export const mistralChatCompletionsWithAgent = <ThrowOnError extends boolean = false>(options: Options<MistralChatCompletionsWithAgentData, ThrowOnError>) => (options.client ?? client).post<MistralChatCompletionsWithAgentResponses, MistralChatCompletionsWithAgentErrors, ThrowOnError>({
    url: '/v1/mistral/{agentId}/chat/completions',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List OpenAI-compatible model ids available through the model router (default LLM proxy)
 *
 * Authentication:
 *
 * This route accepts either an LLM provider API key or a Virtual API Key. See [LLM Proxy Authentication](/docs/platform-llm-proxy-authentication).
 */
export const modelRouterListModelsWithDefaultAgent = <ThrowOnError extends boolean = false>(options?: Options<ModelRouterListModelsWithDefaultAgentData, ThrowOnError>) => (options?.client ?? client).get<ModelRouterListModelsWithDefaultAgentResponses, ModelRouterListModelsWithDefaultAgentErrors, ThrowOnError>({ url: '/v1/model-router/models', ...options });

/**
 * List OpenAI-compatible model ids available through the model router (specific LLM proxy)
 *
 * Authentication:
 *
 * This route accepts either an LLM provider API key or a Virtual API Key. See [LLM Proxy Authentication](/docs/platform-llm-proxy-authentication).
 */
export const modelRouterListModelsWithAgent = <ThrowOnError extends boolean = false>(options: Options<ModelRouterListModelsWithAgentData, ThrowOnError>) => (options.client ?? client).get<ModelRouterListModelsWithAgentResponses, ModelRouterListModelsWithAgentErrors, ThrowOnError>({ url: '/v1/model-router/{agentId}/models', ...options });

/**
 * Create a response through the OpenAI-compatible model router (default LLM proxy)
 *
 * Authentication:
 *
 * This route accepts either an LLM provider API key or a Virtual API Key. See [LLM Proxy Authentication](/docs/platform-llm-proxy-authentication).
 */
export const modelRouterResponsesWithDefaultAgent = <ThrowOnError extends boolean = false>(options: Options<ModelRouterResponsesWithDefaultAgentData, ThrowOnError>) => (options.client ?? client).post<ModelRouterResponsesWithDefaultAgentResponses, ModelRouterResponsesWithDefaultAgentErrors, ThrowOnError>({
    url: '/v1/model-router/responses',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Create a response through the OpenAI-compatible model router (specific LLM proxy)
 *
 * Authentication:
 *
 * This route accepts either an LLM provider API key or a Virtual API Key. See [LLM Proxy Authentication](/docs/platform-llm-proxy-authentication).
 */
export const modelRouterResponsesWithAgent = <ThrowOnError extends boolean = false>(options: Options<ModelRouterResponsesWithAgentData, ThrowOnError>) => (options.client ?? client).post<ModelRouterResponsesWithAgentResponses, ModelRouterResponsesWithAgentErrors, ThrowOnError>({
    url: '/v1/model-router/{agentId}/responses',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Create embeddings through the OpenAI-compatible model router (default LLM proxy)
 *
 * Authentication:
 *
 * This route accepts either an LLM provider API key or a Virtual API Key. See [LLM Proxy Authentication](/docs/platform-llm-proxy-authentication).
 */
export const modelRouterEmbeddingsWithDefaultAgent = <ThrowOnError extends boolean = false>(options: Options<ModelRouterEmbeddingsWithDefaultAgentData, ThrowOnError>) => (options.client ?? client).post<ModelRouterEmbeddingsWithDefaultAgentResponses, ModelRouterEmbeddingsWithDefaultAgentErrors, ThrowOnError>({
    url: '/v1/model-router/embeddings',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Create embeddings through the OpenAI-compatible model router (specific LLM proxy)
 *
 * Authentication:
 *
 * This route accepts either an LLM provider API key or a Virtual API Key. See [LLM Proxy Authentication](/docs/platform-llm-proxy-authentication).
 */
export const modelRouterEmbeddingsWithAgent = <ThrowOnError extends boolean = false>(options: Options<ModelRouterEmbeddingsWithAgentData, ThrowOnError>) => (options.client ?? client).post<ModelRouterEmbeddingsWithAgentResponses, ModelRouterEmbeddingsWithAgentErrors, ThrowOnError>({
    url: '/v1/model-router/{agentId}/embeddings',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Create a chat completion through the OpenAI-compatible model router (default LLM proxy)
 *
 * Authentication:
 *
 * This route accepts either an LLM provider API key or a Virtual API Key. See [LLM Proxy Authentication](/docs/platform-llm-proxy-authentication).
 */
export const modelRouterChatCompletionsWithDefaultAgent = <ThrowOnError extends boolean = false>(options: Options<ModelRouterChatCompletionsWithDefaultAgentData, ThrowOnError>) => (options.client ?? client).post<ModelRouterChatCompletionsWithDefaultAgentResponses, ModelRouterChatCompletionsWithDefaultAgentErrors, ThrowOnError>({
    url: '/v1/model-router/chat/completions',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Create a chat completion through the OpenAI-compatible model router (specific LLM proxy)
 *
 * Authentication:
 *
 * This route accepts either an LLM provider API key or a Virtual API Key. See [LLM Proxy Authentication](/docs/platform-llm-proxy-authentication).
 */
export const modelRouterChatCompletionsWithAgent = <ThrowOnError extends boolean = false>(options: Options<ModelRouterChatCompletionsWithAgentData, ThrowOnError>) => (options.client ?? client).post<ModelRouterChatCompletionsWithAgentResponses, ModelRouterChatCompletionsWithAgentErrors, ThrowOnError>({
    url: '/v1/model-router/{agentId}/chat/completions',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Initiate OAuth flow for MCP server installation
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `mcpServerInstallation:create`: Install MCP servers from the registry
 */
export const initiateOAuth = <ThrowOnError extends boolean = false>(options: Options<InitiateOAuthData, ThrowOnError>) => (options.client ?? client).post<InitiateOAuthResponses, InitiateOAuthErrors, ThrowOnError>({
    url: '/api/oauth/initiate',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Handle OAuth callback and exchange code for tokens
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `mcpServerInstallation:create`: Install MCP servers from the registry
 */
export const handleOAuthCallback = <ThrowOnError extends boolean = false>(options: Options<HandleOAuthCallbackData, ThrowOnError>) => (options.client ?? client).post<HandleOAuthCallbackResponses, HandleOAuthCallbackErrors, ThrowOnError>({
    url: '/api/oauth/callback',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

export const getWellKnownOauthProtectedResourceBy__ = <ThrowOnError extends boolean = false>(options: Options<GetWellKnownOauthProtectedResourceBy__Data, ThrowOnError>) => (options.client ?? client).get<GetWellKnownOauthProtectedResourceBy__Responses, unknown, ThrowOnError>({ url: '/.well-known/oauth-protected-resource/{*}', ...options });

export const getWellKnownOauthAuthorizationServer = <ThrowOnError extends boolean = false>(options?: Options<GetWellKnownOauthAuthorizationServerData, ThrowOnError>) => (options?.client ?? client).get<GetWellKnownOauthAuthorizationServerResponses, unknown, ThrowOnError>({ url: '/.well-known/oauth-authorization-server', ...options });

/**
 * Create a chat completion with Ollama native /api/chat (uses default agent)
 *
 * Authentication:
 *
 * This route accepts either an LLM provider API key or a Virtual API Key. See [LLM Proxy Authentication](/docs/platform-llm-proxy-authentication).
 */
export const ollamaNativeChatWithDefaultAgent = <ThrowOnError extends boolean = false>(options: Options<OllamaNativeChatWithDefaultAgentData, ThrowOnError>) => (options.client ?? client).post<OllamaNativeChatWithDefaultAgentResponses, OllamaNativeChatWithDefaultAgentErrors, ThrowOnError>({
    url: '/v1/ollama-native/api/chat',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Create a chat completion with Ollama native /api/chat for a specific agent
 *
 * Authentication:
 *
 * This route accepts either an LLM provider API key or a Virtual API Key. See [LLM Proxy Authentication](/docs/platform-llm-proxy-authentication).
 */
export const ollamaNativeChatWithAgent = <ThrowOnError extends boolean = false>(options: Options<OllamaNativeChatWithAgentData, ThrowOnError>) => (options.client ?? client).post<OllamaNativeChatWithAgentResponses, OllamaNativeChatWithAgentErrors, ThrowOnError>({
    url: '/v1/ollama-native/{agentId}/api/chat',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Create embeddings with Ollama (uses default agent)
 *
 * Authentication:
 *
 * This route accepts either an LLM provider API key or a Virtual API Key. See [LLM Proxy Authentication](/docs/platform-llm-proxy-authentication).
 */
export const ollamaEmbeddingsWithDefaultAgent = <ThrowOnError extends boolean = false>(options: Options<OllamaEmbeddingsWithDefaultAgentData, ThrowOnError>) => (options.client ?? client).post<OllamaEmbeddingsWithDefaultAgentResponses, OllamaEmbeddingsWithDefaultAgentErrors, ThrowOnError>({
    url: '/v1/ollama/embeddings',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Create embeddings with Ollama for a specific agent
 *
 * Authentication:
 *
 * This route accepts either an LLM provider API key or a Virtual API Key. See [LLM Proxy Authentication](/docs/platform-llm-proxy-authentication).
 */
export const ollamaEmbeddingsWithAgent = <ThrowOnError extends boolean = false>(options: Options<OllamaEmbeddingsWithAgentData, ThrowOnError>) => (options.client ?? client).post<OllamaEmbeddingsWithAgentResponses, OllamaEmbeddingsWithAgentErrors, ThrowOnError>({
    url: '/v1/ollama/{agentId}/embeddings',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Create a chat completion with Ollama (uses default agent)
 *
 * Authentication:
 *
 * This route accepts either an LLM provider API key or a Virtual API Key. See [LLM Proxy Authentication](/docs/platform-llm-proxy-authentication).
 */
export const ollamaChatCompletionsWithDefaultAgent = <ThrowOnError extends boolean = false>(options: Options<OllamaChatCompletionsWithDefaultAgentData, ThrowOnError>) => (options.client ?? client).post<OllamaChatCompletionsWithDefaultAgentResponses, OllamaChatCompletionsWithDefaultAgentErrors, ThrowOnError>({
    url: '/v1/ollama/chat/completions',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Create a chat completion with Ollama for a specific agent
 *
 * Authentication:
 *
 * This route accepts either an LLM provider API key or a Virtual API Key. See [LLM Proxy Authentication](/docs/platform-llm-proxy-authentication).
 */
export const ollamaChatCompletionsWithAgent = <ThrowOnError extends boolean = false>(options: Options<OllamaChatCompletionsWithAgentData, ThrowOnError>) => (options.client ?? client).post<OllamaChatCompletionsWithAgentResponses, OllamaChatCompletionsWithAgentErrors, ThrowOnError>({
    url: '/v1/ollama/{agentId}/chat/completions',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get the onboarding nav items (red-dot nudges) the current user has already visited
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * None (no additional RBAC permission required)
 */
export const getOnboardingSeenNavItems = <ThrowOnError extends boolean = false>(options?: Options<GetOnboardingSeenNavItemsData, ThrowOnError>) => (options?.client ?? client).get<GetOnboardingSeenNavItemsResponses, GetOnboardingSeenNavItemsErrors, ThrowOnError>({ url: '/api/onboarding/seen-nav-items', ...options });

/**
 * Mark onboarding nav items (red-dot nudges) as visited for the current user; idempotent
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * None (no additional RBAC permission required)
 */
export const markOnboardingNavItemsSeen = <ThrowOnError extends boolean = false>(options: Options<MarkOnboardingNavItemsSeenData, ThrowOnError>) => (options.client ?? client).post<MarkOnboardingNavItemsSeenResponses, MarkOnboardingNavItemsSeenErrors, ThrowOnError>({
    url: '/api/onboarding/seen-nav-items',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Whether the first-login onboarding survey should be shown: unlicensed instance, no LLM/MCP activity yet, not already submitted for the organization
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `organizationSettings:update`: Customize organization appearance, authentication, etc
 */
export const getOnboardingSurveyEligibility = <ThrowOnError extends boolean = false>(options?: Options<GetOnboardingSurveyEligibilityData, ThrowOnError>) => (options?.client ?? client).get<GetOnboardingSurveyEligibilityResponses, GetOnboardingSurveyEligibilityErrors, ThrowOnError>({ url: '/api/onboarding/survey-eligibility', ...options });

/**
 * Submit the first-login onboarding survey; forwarded to the website and recorded once per organization
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `organizationSettings:update`: Customize organization appearance, authentication, etc
 */
export const submitOnboardingSurvey = <ThrowOnError extends boolean = false>(options: Options<SubmitOnboardingSurveyData, ThrowOnError>) => (options.client ?? client).post<SubmitOnboardingSurveyResponses, SubmitOnboardingSurveyErrors, ThrowOnError>({
    url: '/api/onboarding/survey',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * When the instance got activated for the feedback pop-up: an MCP server connected AND a successful tool call routed. Null until both happened, and always null on enterprise-licensed instances.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `organizationSettings:update`: Customize organization appearance, authentication, etc
 */
export const getFeedbackPopupActivation = <ThrowOnError extends boolean = false>(options?: Options<GetFeedbackPopupActivationData, ThrowOnError>) => (options?.client ?? client).get<GetFeedbackPopupActivationResponses, GetFeedbackPopupActivationErrors, ThrowOnError>({ url: '/api/onboarding/feedback-popup-activation', ...options });

/**
 * Create embeddings with OpenAI (uses default agent)
 *
 * Authentication:
 *
 * This route accepts either an LLM provider API key or a Virtual API Key. See [LLM Proxy Authentication](/docs/platform-llm-proxy-authentication).
 */
export const openAiEmbeddingsWithDefaultAgent = <ThrowOnError extends boolean = false>(options: Options<OpenAiEmbeddingsWithDefaultAgentData, ThrowOnError>) => (options.client ?? client).post<OpenAiEmbeddingsWithDefaultAgentResponses, OpenAiEmbeddingsWithDefaultAgentErrors, ThrowOnError>({
    url: '/v1/openai/embeddings',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Create embeddings with OpenAI for a specific agent
 *
 * Authentication:
 *
 * This route accepts either an LLM provider API key or a Virtual API Key. See [LLM Proxy Authentication](/docs/platform-llm-proxy-authentication).
 */
export const openAiEmbeddingsWithAgent = <ThrowOnError extends boolean = false>(options: Options<OpenAiEmbeddingsWithAgentData, ThrowOnError>) => (options.client ?? client).post<OpenAiEmbeddingsWithAgentResponses, OpenAiEmbeddingsWithAgentErrors, ThrowOnError>({
    url: '/v1/openai/{agentId}/embeddings',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Create a response with OpenAI (uses default agent)
 *
 * Authentication:
 *
 * This route accepts either an LLM provider API key or a Virtual API Key. See [LLM Proxy Authentication](/docs/platform-llm-proxy-authentication).
 */
export const openAiResponsesWithDefaultAgent = <ThrowOnError extends boolean = false>(options: Options<OpenAiResponsesWithDefaultAgentData, ThrowOnError>) => (options.client ?? client).post<OpenAiResponsesWithDefaultAgentResponses, OpenAiResponsesWithDefaultAgentErrors, ThrowOnError>({
    url: '/v1/openai/responses',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Create a response with OpenAI for a specific agent
 *
 * Authentication:
 *
 * This route accepts either an LLM provider API key or a Virtual API Key. See [LLM Proxy Authentication](/docs/platform-llm-proxy-authentication).
 */
export const openAiResponsesWithAgent = <ThrowOnError extends boolean = false>(options: Options<OpenAiResponsesWithAgentData, ThrowOnError>) => (options.client ?? client).post<OpenAiResponsesWithAgentResponses, OpenAiResponsesWithAgentErrors, ThrowOnError>({
    url: '/v1/openai/{agentId}/responses',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Create a chat completion with OpenAI (uses default agent)
 *
 * Authentication:
 *
 * This route accepts either an LLM provider API key or a Virtual API Key. See [LLM Proxy Authentication](/docs/platform-llm-proxy-authentication).
 */
export const openAiChatCompletionsWithDefaultAgent = <ThrowOnError extends boolean = false>(options: Options<OpenAiChatCompletionsWithDefaultAgentData, ThrowOnError>) => (options.client ?? client).post<OpenAiChatCompletionsWithDefaultAgentResponses, OpenAiChatCompletionsWithDefaultAgentErrors, ThrowOnError>({
    url: '/v1/openai/chat/completions',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Create a chat completion with OpenAI for a specific agent
 *
 * Authentication:
 *
 * This route accepts either an LLM provider API key or a Virtual API Key. See [LLM Proxy Authentication](/docs/platform-llm-proxy-authentication).
 */
export const openAiChatCompletionsWithAgent = <ThrowOnError extends boolean = false>(options: Options<OpenAiChatCompletionsWithAgentData, ThrowOnError>) => (options.client ?? client).post<OpenAiChatCompletionsWithAgentResponses, OpenAiChatCompletionsWithAgentErrors, ThrowOnError>({
    url: '/v1/openai/{agentId}/chat/completions',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List OpenAI models (default agent)
 *
 * Authentication:
 *
 * This route accepts either an LLM provider API key or a Virtual API Key. See [LLM Proxy Authentication](/docs/platform-llm-proxy-authentication).
 */
export const openAiListModelsWithDefaultAgent = <ThrowOnError extends boolean = false>(options?: Options<OpenAiListModelsWithDefaultAgentData, ThrowOnError>) => (options?.client ?? client).get<OpenAiListModelsWithDefaultAgentResponses, OpenAiListModelsWithDefaultAgentErrors, ThrowOnError>({ url: '/v1/openai/models', ...options });

/**
 * List OpenAI models (specific agent)
 *
 * Authentication:
 *
 * This route accepts either an LLM provider API key or a Virtual API Key. See [LLM Proxy Authentication](/docs/platform-llm-proxy-authentication).
 */
export const openAiListModelsWithAgent = <ThrowOnError extends boolean = false>(options: Options<OpenAiListModelsWithAgentData, ThrowOnError>) => (options.client ?? client).get<OpenAiListModelsWithAgentResponses, OpenAiListModelsWithAgentErrors, ThrowOnError>({ url: '/v1/openai/{agentId}/models', ...options });

/**
 * Start the ChatGPT/Codex OAuth device flow used to connect a ChatGPT subscription as an OpenAI provider credential
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * None (no additional RBAC permission required)
 */
export const openaiCodexDeviceAuthStart = <ThrowOnError extends boolean = false>(options?: Options<OpenaiCodexDeviceAuthStartData, ThrowOnError>) => (options?.client ?? client).post<OpenaiCodexDeviceAuthStartResponses, OpenaiCodexDeviceAuthStartErrors, ThrowOnError>({ url: '/api/openai-codex-auth/device/start', ...options });

/**
 * Poll the ChatGPT/Codex OAuth device flow once; returns the encoded provider credential when the user has authorized
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * None (no additional RBAC permission required)
 */
export const openaiCodexDeviceAuthPoll = <ThrowOnError extends boolean = false>(options: Options<OpenaiCodexDeviceAuthPollData, ThrowOnError>) => (options.client ?? client).post<OpenaiCodexDeviceAuthPollResponses, OpenaiCodexDeviceAuthPollErrors, ThrowOnError>({
    url: '/api/openai-codex-auth/device/poll',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Create a chat completion with OpenRouter (uses default agent)
 *
 * Authentication:
 *
 * This route accepts either an LLM provider API key or a Virtual API Key. See [LLM Proxy Authentication](/docs/platform-llm-proxy-authentication).
 */
export const openrouterChatCompletionsWithDefaultAgent = <ThrowOnError extends boolean = false>(options: Options<OpenrouterChatCompletionsWithDefaultAgentData, ThrowOnError>) => (options.client ?? client).post<OpenrouterChatCompletionsWithDefaultAgentResponses, OpenrouterChatCompletionsWithDefaultAgentErrors, ThrowOnError>({
    url: '/v1/openrouter/chat/completions',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Create a chat completion with OpenRouter for a specific agent
 *
 * Authentication:
 *
 * This route accepts either an LLM provider API key or a Virtual API Key. See [LLM Proxy Authentication](/docs/platform-llm-proxy-authentication).
 */
export const openrouterChatCompletionsWithAgent = <ThrowOnError extends boolean = false>(options: Options<OpenrouterChatCompletionsWithAgentData, ThrowOnError>) => (options.client ?? client).post<OpenrouterChatCompletionsWithAgentResponses, OpenrouterChatCompletionsWithAgentErrors, ThrowOnError>({
    url: '/v1/openrouter/{agentId}/chat/completions',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get all roles in the organization
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `ac:read`: View custom roles and their permissions
 */
export const getRoles = <ThrowOnError extends boolean = false>(options?: Options<GetRolesData, ThrowOnError>) => (options?.client ?? client).get<GetRolesResponses, GetRolesErrors, ThrowOnError>({ url: '/api/roles', ...options });

/**
 * Create a new custom role
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `ac:create`: Create new custom roles
 */
export const createRole = <ThrowOnError extends boolean = false>(options: Options<CreateRoleData, ThrowOnError>) => (options.client ?? client).post<CreateRoleResponses, CreateRoleErrors, ThrowOnError>({
    url: '/api/roles',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete a custom role
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `ac:delete`: Delete custom roles
 */
export const deleteRole = <ThrowOnError extends boolean = false>(options: Options<DeleteRoleData, ThrowOnError>) => (options.client ?? client).delete<DeleteRoleResponses, DeleteRoleErrors, ThrowOnError>({ url: '/api/roles/{roleId}', ...options });

/**
 * Get a specific role by ID
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `ac:read`: View custom roles and their permissions
 */
export const getRole = <ThrowOnError extends boolean = false>(options: Options<GetRoleData, ThrowOnError>) => (options.client ?? client).get<GetRoleResponses, GetRoleErrors, ThrowOnError>({ url: '/api/roles/{roleId}', ...options });

/**
 * Update a custom role
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `ac:update`: Modify custom role permissions
 */
export const updateRole = <ThrowOnError extends boolean = false>(options: Options<UpdateRoleData, ThrowOnError>) => (options.client ?? client).put<UpdateRoleResponses, UpdateRoleErrors, ThrowOnError>({
    url: '/api/roles/{roleId}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get organization details
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * None (no additional RBAC permission required)
 */
export const getOrganization = <ThrowOnError extends boolean = false>(options?: Options<GetOrganizationData, ThrowOnError>) => (options?.client ?? client).get<GetOrganizationResponses, GetOrganizationErrors, ThrowOnError>({ url: '/api/organization', ...options });

/**
 * Get organization appearance settings
 *
 * Authentication:
 *
 * Not required.
 *
 * Authorization:
 *
 * None (no additional RBAC permission required)
 */
export const getAppearanceSettings = <ThrowOnError extends boolean = false>(options?: Options<GetAppearanceSettingsData, ThrowOnError>) => (options?.client ?? client).get<GetAppearanceSettingsResponses, GetAppearanceSettingsErrors, ThrowOnError>({ url: '/api/organization/appearance-settings', ...options });

/**
 * Update appearance settings
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `organizationSettings:update`: Customize organization appearance, authentication, etc
 */
export const updateAppearanceSettings = <ThrowOnError extends boolean = false>(options: Options<UpdateAppearanceSettingsData, ThrowOnError>) => (options.client ?? client).patch<UpdateAppearanceSettingsResponses, UpdateAppearanceSettingsErrors, ThrowOnError>({
    url: '/api/organization/appearance-settings',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Update security settings (default tool guardrails, chat file uploads, Apps Hackathon recorder)
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `agentSettings:update`: Modify agent settings (default model, default agent, default tool guardrails, file uploads, Apps Hackathon recorder)
 */
export const updateSecuritySettings = <ThrowOnError extends boolean = false>(options: Options<UpdateSecuritySettingsData, ThrowOnError>) => (options.client ?? client).patch<UpdateSecuritySettingsResponses, UpdateSecuritySettingsErrors, ThrowOnError>({
    url: '/api/organization/security-settings',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Update LLM settings (TOON compression, compression scope)
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `llmSettings:update`: Modify LLM settings
 */
export const updateLlmSettings = <ThrowOnError extends boolean = false>(options: Options<UpdateLlmSettingsData, ThrowOnError>) => (options.client ?? client).patch<UpdateLlmSettingsResponses, UpdateLlmSettingsErrors, ThrowOnError>({
    url: '/api/organization/llm-settings',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Update MCP settings (online catalog availability, idle hibernation)
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `mcpSettings:update`: Modify MCP settings
 */
export const updateMcpSettings = <ThrowOnError extends boolean = false>(options: Options<UpdateMcpSettingsData, ThrowOnError>) => (options.client ?? client).patch<UpdateMcpSettingsResponses, UpdateMcpSettingsErrors, ThrowOnError>({
    url: '/api/organization/mcp-settings',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Update Skills settings (online catalog availability)
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `skillsSettings:update`: Modify Skills settings
 */
export const updateSkillsSettings = <ThrowOnError extends boolean = false>(options: Options<UpdateSkillsSettingsData, ThrowOnError>) => (options.client ?? client).patch<UpdateSkillsSettingsResponses, UpdateSkillsSettingsErrors, ThrowOnError>({
    url: '/api/organization/skills-settings',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Update agent settings (default model, default agent, skill slash commands)
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `agentSettings:update`: Modify agent settings (default model, default agent, default tool guardrails, file uploads, Apps Hackathon recorder)
 */
export const updateAgentSettings = <ThrowOnError extends boolean = false>(options: Options<UpdateAgentSettingsData, ThrowOnError>) => (options.client ?? client).patch<UpdateAgentSettingsResponses, UpdateAgentSettingsErrors, ThrowOnError>({
    url: '/api/organization/agent-settings',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Update /connection admin settings (default gateway/proxy, hidden clients/providers)
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `organizationSettings:update`: Customize organization appearance, authentication, etc
 */
export const updateConnectionSettings = <ThrowOnError extends boolean = false>(options: Options<UpdateConnectionSettingsData, ThrowOnError>) => (options.client ?? client).patch<UpdateConnectionSettingsResponses, UpdateConnectionSettingsErrors, ThrowOnError>({
    url: '/api/organization/connection-settings',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Customize the built-in integration catalogs: hide model providers, messaging channels, or knowledge connectors, and override how they are labelled. Omitted catalogs are left unchanged; null clears a catalog's overrides.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `organizationSettings:update`: Customize organization appearance, authentication, etc
 */
export const updateIntegrationSettings = <ThrowOnError extends boolean = false>(options: Options<UpdateIntegrationSettingsData, ThrowOnError>) => (options.client ?? client).patch<UpdateIntegrationSettingsResponses, UpdateIntegrationSettingsErrors, ThrowOnError>({
    url: '/api/organization/integration-settings',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Configure the implicit default environment (the deployment target referenced by internal_mcp_catalog.environment_id = null). Pass null for name to reset to the built-in 'Default' label, or null for namespace to unset it. Omitted fields are left unchanged. When the namespace or network policy changes and the runtime is enabled, all default-environment MCP servers are reconciled.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `environment:update`: Modify deployment environments, including the org default environment
 */
export const updateDefaultEnvironment = <ThrowOnError extends boolean = false>(options: Options<UpdateDefaultEnvironmentData, ThrowOnError>) => (options.client ?? client).patch<UpdateDefaultEnvironmentResponses, UpdateDefaultEnvironmentErrors, ThrowOnError>({
    url: '/api/organization/default-environment',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Update organization Auth settings
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `organizationSettings:update`: Customize organization appearance, authentication, etc
 */
export const updateAuthSettings = <ThrowOnError extends boolean = false>(options: Options<UpdateAuthSettingsData, ThrowOnError>) => (options.client ?? client).patch<UpdateAuthSettingsResponses, UpdateAuthSettingsErrors, ThrowOnError>({
    url: '/api/organization/auth-settings',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Update knowledge settings (embedding model)
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `knowledgeSettings:update`: Modify knowledge settings (embedding and reranking models)
 */
export const updateKnowledgeSettings = <ThrowOnError extends boolean = false>(options: Options<UpdateKnowledgeSettingsData, ThrowOnError>) => (options.client ?? client).patch<UpdateKnowledgeSettingsResponses, UpdateKnowledgeSettingsErrors, ThrowOnError>({
    url: '/api/organization/knowledge-settings',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Drop the embedding configuration, deleting all KB documents and resetting connector checkpoints
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `knowledgeSettings:update`: Modify knowledge settings (embedding and reranking models)
 */
export const dropEmbeddingConfig = <ThrowOnError extends boolean = false>(options?: Options<DropEmbeddingConfigData, ThrowOnError>) => (options?.client ?? client).post<DropEmbeddingConfigResponses, DropEmbeddingConfigErrors, ThrowOnError>({ url: '/api/organization/knowledge-settings/drop-embedding', ...options });

/**
 * Test the embedding connection by embedding a sample text
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `knowledgeSettings:update`: Modify knowledge settings (embedding and reranking models)
 */
export const testEmbeddingConnection = <ThrowOnError extends boolean = false>(options: Options<TestEmbeddingConnectionData, ThrowOnError>) => (options.client ?? client).post<TestEmbeddingConnectionResponses, TestEmbeddingConnectionErrors, ThrowOnError>({
    url: '/api/organization/knowledge-settings/test-embedding',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Test the reranker connection with a sample structured-output call
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `knowledgeSettings:update`: Modify knowledge settings (embedding and reranking models)
 */
export const testRerankerConnection = <ThrowOnError extends boolean = false>(options: Options<TestRerankerConnectionData, ThrowOnError>) => (options.client ?? client).post<TestRerankerConnectionResponses, TestRerankerConnectionErrors, ThrowOnError>({
    url: '/api/organization/knowledge-settings/test-reranker',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Where BM25 keyword ranking stands for the organization: whether its corpus statistics cover every language with indexed documents (until they do, keyword search ranks that language with PostgreSQL's built-in ts_rank), when they were last rebuilt and when the next rebuild is due
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `knowledgeSettings:read`: View knowledge settings (embedding and reranking models)
 */
export const getKeywordRankingStatus = <ThrowOnError extends boolean = false>(options?: Options<GetKeywordRankingStatusData, ThrowOnError>) => (options?.client ?? client).get<GetKeywordRankingStatusResponses, GetKeywordRankingStatusErrors, ThrowOnError>({ url: '/api/organization/knowledge-settings/keyword-ranking-status', ...options });

/**
 * Test the OCR configuration by sending a synthetic PDF page to the model
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `knowledgeSettings:update`: Modify knowledge settings (embedding and reranking models)
 */
export const testOcrConnection = <ThrowOnError extends boolean = false>(options: Options<TestOcrConnectionData, ThrowOnError>) => (options.client ?? client).post<TestOcrConnectionResponses, TestOcrConnectionErrors, ThrowOnError>({
    url: '/api/organization/knowledge-settings/test-ocr',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Mark organization onboarding as complete
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `organizationSettings:update`: Customize organization appearance, authentication, etc
 */
export const completeOnboarding = <ThrowOnError extends boolean = false>(options: Options<CompleteOnboardingData, ThrowOnError>) => (options.client ?? client).post<CompleteOnboardingResponses, CompleteOnboardingErrors, ThrowOnError>({
    url: '/api/organization/complete-onboarding',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Check if organization onboarding is complete
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * None (no additional RBAC permission required)
 */
export const getOnboardingStatus = <ThrowOnError extends boolean = false>(options?: Options<GetOnboardingStatusData, ThrowOnError>) => (options?.client ?? client).get<GetOnboardingStatusResponses, GetOnboardingStatusErrors, ThrowOnError>({ url: '/api/organization/onboarding-status', ...options });

/**
 * Get which members have completed signup (have an account record)
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * None (no additional RBAC permission required)
 */
export const getMemberSignupStatus = <ThrowOnError extends boolean = false>(options?: Options<GetMemberSignupStatusData, ThrowOnError>) => (options?.client ?? client).get<GetMemberSignupStatusResponses, GetMemberSignupStatusErrors, ThrowOnError>({ url: '/api/organization/members/signup-status', ...options });

/**
 * Delete an auto-provisioned member who hasn't completed signup
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `member:delete`: Remove members from the organization
 */
export const deletePendingSignupMember = <ThrowOnError extends boolean = false>(options: Options<DeletePendingSignupMemberData, ThrowOnError>) => (options.client ?? client).delete<DeletePendingSignupMemberResponses, DeletePendingSignupMemberErrors, ThrowOnError>({ url: '/api/organization/members/{userId}/pending-signup', ...options });

/**
 * List organization members visible to the caller. Callers with the member:read permission (admins and equivalent custom roles) receive the full organization roster; other authenticated users receive only the members they share a team with.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * None (no additional RBAC permission required)
 */
export const getOrganizationMembers = <ThrowOnError extends boolean = false>(options?: Options<GetOrganizationMembersData, ThrowOnError>) => (options?.client ?? client).get<GetOrganizationMembersResponses, GetOrganizationMembersErrors, ThrowOnError>({ url: '/api/organization/members', ...options });

/**
 * Get a member of the organization by user ID or email address
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `member:read`: View organization members and their roles
 */
export const getOrganizationMember = <ThrowOnError extends boolean = false>(options: Options<GetOrganizationMemberData, ThrowOnError>) => (options.client ?? client).get<GetOrganizationMemberResponses, GetOrganizationMemberErrors, ThrowOnError>({ url: '/api/organization/members/{idOrEmail}', ...options });

/**
 * Create a chat completion with Perplexity (uses default agent). Note: this endpoint does not support external tool calling; the /responses Agent API endpoint does.
 *
 * Authentication:
 *
 * This route accepts either an LLM provider API key or a Virtual API Key. See [LLM Proxy Authentication](/docs/platform-llm-proxy-authentication).
 */
export const perplexityChatCompletionsWithDefaultAgent = <ThrowOnError extends boolean = false>(options: Options<PerplexityChatCompletionsWithDefaultAgentData, ThrowOnError>) => (options.client ?? client).post<PerplexityChatCompletionsWithDefaultAgentResponses, PerplexityChatCompletionsWithDefaultAgentErrors, ThrowOnError>({
    url: '/v1/perplexity/chat/completions',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Create a chat completion with Perplexity for a specific agent. Note: this endpoint does not support external tool calling; the /responses Agent API endpoint does.
 *
 * Authentication:
 *
 * This route accepts either an LLM provider API key or a Virtual API Key. See [LLM Proxy Authentication](/docs/platform-llm-proxy-authentication).
 */
export const perplexityChatCompletionsWithAgent = <ThrowOnError extends boolean = false>(options: Options<PerplexityChatCompletionsWithAgentData, ThrowOnError>) => (options.client ?? client).post<PerplexityChatCompletionsWithAgentResponses, PerplexityChatCompletionsWithAgentErrors, ThrowOnError>({
    url: '/v1/perplexity/{agentId}/chat/completions',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Create a response with the Perplexity Agent API (uses default agent).
 *
 * Authentication:
 *
 * This route accepts either an LLM provider API key or a Virtual API Key. See [LLM Proxy Authentication](/docs/platform-llm-proxy-authentication).
 */
export const perplexityResponsesWithDefaultAgent = <ThrowOnError extends boolean = false>(options: Options<PerplexityResponsesWithDefaultAgentData, ThrowOnError>) => (options.client ?? client).post<PerplexityResponsesWithDefaultAgentResponses, PerplexityResponsesWithDefaultAgentErrors, ThrowOnError>({
    url: '/v1/perplexity/responses',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Create a response with the Perplexity Agent API for a specific agent.
 *
 * Authentication:
 *
 * This route accepts either an LLM provider API key or a Virtual API Key. See [LLM Proxy Authentication](/docs/platform-llm-proxy-authentication).
 */
export const perplexityResponsesWithAgent = <ThrowOnError extends boolean = false>(options: Options<PerplexityResponsesWithAgentData, ThrowOnError>) => (options.client ?? client).post<PerplexityResponsesWithAgentResponses, PerplexityResponsesWithAgentErrors, ThrowOnError>({
    url: '/v1/perplexity/{agentId}/responses',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List plugins for the organization
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `plugin:read`: View plugins and their file metadata
 */
export const getPlugins = <ThrowOnError extends boolean = false>(options?: Options<GetPluginsData, ThrowOnError>) => (options?.client ?? client).get<GetPluginsResponses, GetPluginsErrors, ThrowOnError>({ url: '/api/plugins', ...options });

/**
 * Create a plugin. Hook configuration bytes are stored verbatim.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `plugin:admin`: Publish executable plugins through connection marketplaces
 * `plugin:create`: Create plugins
 */
export const createPlugin = <ThrowOnError extends boolean = false>(options: Options<CreatePluginData, ThrowOnError>) => (options.client ?? client).post<CreatePluginResponses, CreatePluginErrors, ThrowOnError>({
    url: '/api/plugins',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Discover and normalize plugins advertised by a GitHub marketplace repository
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `plugin:admin`: Publish executable plugins through connection marketplaces
 * `plugin:create`: Create plugins
 */
export const discoverGithubPluginMarketplace = <ThrowOnError extends boolean = false>(options: Options<DiscoverGithubPluginMarketplaceData, ThrowOnError>) => (options.client ?? client).post<DiscoverGithubPluginMarketplaceResponses, DiscoverGithubPluginMarketplaceErrors, ThrowOnError>({
    url: '/api/plugins/github/marketplace/discover',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Import selected plugins from one reviewed GitHub marketplace snapshot
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `plugin:admin`: Publish executable plugins through connection marketplaces
 * `plugin:create`: Create plugins
 */
export const importGithubPluginMarketplace = <ThrowOnError extends boolean = false>(options: Options<ImportGithubPluginMarketplaceData, ThrowOnError>) => (options.client ?? client).post<ImportGithubPluginMarketplaceResponses, ImportGithubPluginMarketplaceErrors, ThrowOnError>({
    url: '/api/plugins/github/marketplace/import',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Resolve a GitHub ref and preview the exact by-value plugin bytes
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `plugin:admin`: Publish executable plugins through connection marketplaces
 * `plugin:create`: Create plugins
 */
export const previewGithubPlugin = <ThrowOnError extends boolean = false>(options: Options<PreviewGithubPluginData, ThrowOnError>) => (options.client ?? client).post<PreviewGithubPluginResponses, PreviewGithubPluginErrors, ThrowOnError>({
    url: '/api/plugins/github/preview',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Import and approve a plugin from an immutable GitHub commit
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `plugin:admin`: Publish executable plugins through connection marketplaces
 * `plugin:create`: Create plugins
 */
export const importGithubPlugin = <ThrowOnError extends boolean = false>(options: Options<ImportGithubPluginData, ThrowOnError>) => (options.client ?? client).post<ImportGithubPluginResponses, ImportGithubPluginErrors, ThrowOnError>({
    url: '/api/plugins/github/import',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Preview the latest tracked GitHub commit without changing approved bytes
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `plugin:admin`: Publish executable plugins through connection marketplaces
 * `plugin:update`: Modify plugin metadata and files
 */
export const previewGithubPluginUpdate = <ThrowOnError extends boolean = false>(options: Options<PreviewGithubPluginUpdateData, ThrowOnError>) => (options.client ?? client).post<PreviewGithubPluginUpdateResponses, PreviewGithubPluginUpdateErrors, ThrowOnError>({
    url: '/api/plugins/{id}/github/preview-update',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Re-fetch and explicitly approve the latest tracked GitHub commit
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `plugin:admin`: Publish executable plugins through connection marketplaces
 * `plugin:update`: Modify plugin metadata and files
 */
export const applyGithubPluginUpdate = <ThrowOnError extends boolean = false>(options: Options<ApplyGithubPluginUpdateData, ThrowOnError>) => (options.client ?? client).post<ApplyGithubPluginUpdateResponses, ApplyGithubPluginUpdateErrors, ThrowOnError>({
    url: '/api/plugins/{id}/github/apply-update',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Change or pause a Plugin GitHub sync schedule
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `plugin:admin`: Publish executable plugins through connection marketplaces
 * `plugin:update`: Modify plugin metadata and files
 */
export const updatePluginGithubSync = <ThrowOnError extends boolean = false>(options: Options<UpdatePluginGithubSyncData, ThrowOnError>) => (options.client ?? client).patch<UpdatePluginGithubSyncResponses, UpdatePluginGithubSyncErrors, ThrowOnError>({
    url: '/api/plugins/{id}/github/sync',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Queue an immediate Plugin GitHub source check
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `plugin:admin`: Publish executable plugins through connection marketplaces
 * `plugin:update`: Modify plugin metadata and files
 */
export const triggerPluginGithubSync = <ThrowOnError extends boolean = false>(options: Options<TriggerPluginGithubSyncData, ThrowOnError>) => (options.client ?? client).post<TriggerPluginGithubSyncResponses, TriggerPluginGithubSyncErrors, ThrowOnError>({ url: '/api/plugins/{id}/github/check', ...options });

/**
 * Delete a plugin
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `plugin:admin`: Publish executable plugins through connection marketplaces
 * `plugin:delete`: Delete plugins
 */
export const deletePlugin = <ThrowOnError extends boolean = false>(options: Options<DeletePluginData, ThrowOnError>) => (options.client ?? client).delete<DeletePluginResponses, DeletePluginErrors, ThrowOnError>({ url: '/api/plugins/{id}', ...options });

/**
 * Get one plugin and its opaque files
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `plugin:admin`: Publish executable plugins through connection marketplaces
 * `plugin:read`: View plugins and their file metadata
 */
export const getPlugin = <ThrowOnError extends boolean = false>(options: Options<GetPluginData, ThrowOnError>) => (options.client ?? client).get<GetPluginResponses, GetPluginErrors, ThrowOnError>({ url: '/api/plugins/{id}', ...options });

/**
 * Update plugin metadata, visibility, GitHub source settings, or files
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `plugin:admin`: Publish executable plugins through connection marketplaces
 * `plugin:update`: Modify plugin metadata and files
 */
export const updatePlugin = <ThrowOnError extends boolean = false>(options: Options<UpdatePluginData, ThrowOnError>) => (options.client ?? client).put<UpdatePluginResponses, UpdatePluginErrors, ThrowOnError>({
    url: '/api/plugins/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List the portable Skills embedded in the plugins visible to the caller.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `plugin:read`: View plugins and their file metadata
 * `skill:read`: View and use agent skills within your scope (org, your teams, your own)
 */
export const getPluginSkills = <ThrowOnError extends boolean = false>(options?: Options<GetPluginSkillsData, ThrowOnError>) => (options?.client ?? client).get<GetPluginSkillsResponses, GetPluginSkillsErrors, ThrowOnError>({ url: '/api/skills/plugins', ...options });

/**
 * Get recent activation statistics for one Skill from an accessible plugin.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `plugin:read`: View plugins and their file metadata
 * `skill:read`: View and use agent skills within your scope (org, your teams, your own)
 */
export const getPluginSkillUsageStatistics = <ThrowOnError extends boolean = false>(options: Options<GetPluginSkillUsageStatisticsData, ThrowOnError>) => (options.client ?? client).get<GetPluginSkillUsageStatisticsResponses, GetPluginSkillUsageStatisticsErrors, ThrowOnError>({ url: '/api/skills/plugins/{pluginId}/usage-statistics', ...options });

/**
 * Read one Skill embedded in a plugin, with its bundled resource files.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `plugin:read`: View plugins and their file metadata
 * `skill:read`: View and use agent skills within your scope (org, your teams, your own)
 */
export const getPluginSkill = <ThrowOnError extends boolean = false>(options: Options<GetPluginSkillData, ThrowOnError>) => (options.client ?? client).get<GetPluginSkillResponses, GetPluginSkillErrors, ThrowOnError>({ url: '/api/skills/plugins/{pluginId}', ...options });

/**
 * List projects the caller can see. `scope` is the project's share visibility: `personal` (private), `team` (shared with teams — narrow with `teamIds`), or `org` (org-wide); omitted = all visible. Admins additionally filter `personal` by owner via `authorIds` / `excludeAuthorIds` (ignored for non-admins). `search` matches name + description. `status=deleted` returns the org-wide soft-deleted projects for a project admin (empty for everyone else); the other filters do not apply to that slice.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `project:read`: View projects and your own chats inside them
 */
export const getProjects = <ThrowOnError extends boolean = false>(options?: Options<GetProjectsData, ThrowOnError>) => (options?.client ?? client).get<GetProjectsResponses, GetProjectsErrors, ThrowOnError>({ url: '/api/projects', ...options });

/**
 * Create a project. Files produced in its chats are owned by the project rather than the individual author.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `project:create`: Create projects
 */
export const createProject = <ThrowOnError extends boolean = false>(options: Options<CreateProjectData, ThrowOnError>) => (options.client ?? client).post<CreateProjectResponses, CreateProjectErrors, ThrowOnError>({
    url: '/api/projects',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Turn an existing chat into a project: create the project, move the chat into it, and re-point the chat's files to the project. Only the chat's owner may do this, and only for a user chat not already in a project. `name` defaults to the chat title.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `project:create`: Create projects
 */
export const createProjectFromConversation = <ThrowOnError extends boolean = false>(options: Options<CreateProjectFromConversationData, ThrowOnError>) => (options.client ?? client).post<CreateProjectFromConversationResponses, CreateProjectFromConversationErrors, ThrowOnError>({
    url: '/api/projects/from-conversation',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Soft-delete a project (owner or a project admin). Its chats detach and survive as ordinary conversations; its files and scheduled tasks are retained but hidden, and a project admin can restore them. Nothing is purged.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `project:delete`: Delete projects
 */
export const deleteProject = <ThrowOnError extends boolean = false>(options: Options<DeleteProjectData, ThrowOnError>) => (options.client ?? client).delete<DeleteProjectResponses, DeleteProjectErrors, ThrowOnError>({ url: '/api/projects/{id}', ...options });

/**
 * Project detail. Share team ids are included for the owner only.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `project:read`: View projects and your own chats inside them
 */
export const getProject = <ThrowOnError extends boolean = false>(options: Options<GetProjectData, ThrowOnError>) => (options.client ?? client).get<GetProjectResponses, GetProjectErrors, ThrowOnError>({ url: '/api/projects/{id}', ...options });

/**
 * Update a project's name, description, icon, and/or default agent (owner or a project admin). Only the provided fields change. The default agent must be an organization-wide chat agent; null clears it.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `project:update`: Edit project descriptions, instructions, and sharing
 */
export const updateProject = <ThrowOnError extends boolean = false>(options: Options<UpdateProjectData, ThrowOnError>) => (options.client ?? client).patch<UpdateProjectResponses, UpdateProjectErrors, ThrowOnError>({
    url: '/api/projects/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Set who can see the project (owner or a project admin): the whole organization, specific teams, or nobody (visibility "none" unshares).
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `project:update`: Edit project descriptions, instructions, and sharing
 */
export const setProjectShare = <ThrowOnError extends boolean = false>(options: Options<SetProjectShareData, ThrowOnError>) => (options.client ?? client).put<SetProjectShareResponses, SetProjectShareErrors, ThrowOnError>({
    url: '/api/projects/{id}/share',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Soft-delete several projects in one request (owner or project admin per project). As with the single-project delete, their chats detach and survive as ordinary conversations, and their files and scheduled tasks are retained but hidden. Nothing is purged, and a project the caller may not delete is reported in `failed` while the rest of the batch still applies.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `project:delete`: Delete projects
 */
export const bulkDeleteProjects = <ThrowOnError extends boolean = false>(options: Options<BulkDeleteProjectsData, ThrowOnError>) => (options.client ?? client).delete<BulkDeleteProjectsResponses, BulkDeleteProjectsErrors, ThrowOnError>({
    url: '/api/projects/bulk',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Update several projects in one request. Today the only bulk-editable surface is who can see them — the whole organization, named teams, named people, or nobody ("none" unshares) — and every project in the batch is moved to the same one. Per-project problems, such as an id the caller neither owns nor administers, are reported in `failed` and leave the rest of the batch applied.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `project:update`: Edit project descriptions, instructions, and sharing
 */
export const bulkUpdateProjects = <ThrowOnError extends boolean = false>(options: Options<BulkUpdateProjectsData, ThrowOnError>) => (options.client ?? client).patch<BulkUpdateProjectsResponses, BulkUpdateProjectsErrors, ThrowOnError>({
    url: '/api/projects/bulk',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Restore a soft-deleted project (project admins only). Brings back its retained files and scheduled tasks (schedules resume forward-only, no catch-up runs); chats do NOT re-attach, so the restored project reports zero chats. 404 if there is no soft-deleted project with that id in the org. Deleting frees the display name, so if the owner has since taken it, pass `name` to restore under a different one; restoring into a name that is still taken is a 409.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `project:delete`: Delete projects
 */
export const restoreProject = <ThrowOnError extends boolean = false>(options: Options<RestoreProjectData, ThrowOnError>) => (options.client ?? client).post<RestoreProjectResponses, RestoreProjectErrors, ThrowOnError>({
    url: '/api/projects/{id}/restore',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Permanently destroy a soft-deleted project (global admins only). Irreversible, with no grace period: the project, its files (records and stored contents), pins, share configuration, and scheduled tasks are all destroyed. Its chats are unaffected — they detached when it was deleted and survive as ordinary conversations. 404 if there is no soft-deleted project with that id in the org, which is also the answer when the project is still live or the caller is not a global admin. Restore wins a race: if a restore commits first, this returns 404 and the project stays.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * Restricted to the built-in Admin and Platform Admin roles. No permission grants it: `project:delete`, `project:admin`, and ownership of the project all reach the trash, not past it, and a service account never qualifies. A caller without it gets 404, not 403, so the endpoint never confirms the project exists.
 */
export const permanentlyDeleteProject = <ThrowOnError extends boolean = false>(options: Options<PermanentlyDeleteProjectData, ThrowOnError>) => (options.client ?? client).delete<PermanentlyDeleteProjectResponses, PermanentlyDeleteProjectErrors, ThrowOnError>({ url: '/api/projects/{id}/permanent', ...options });

/**
 * Files owned by the project, readable by anyone with project access.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `file:manage`: List, read, write, and delete files in chats and projects
 * `project:read`: View projects and your own chats inside them
 */
export const getProjectFiles = <ThrowOnError extends boolean = false>(options: Options<GetProjectFilesData, ThrowOnError>) => (options.client ?? client).get<GetProjectFilesResponses, GetProjectFilesErrors, ThrowOnError>({ url: '/api/projects/{id}/files', ...options });

/**
 * Upload one file into the project (drag-and-drop on the Files panel). The bytes are base64-encoded in the body; a colliding name is auto-renamed.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `file:manage`: List, read, write, and delete files in chats and projects
 * `project:read`: View projects and your own chats inside them
 */
export const uploadProjectFiles = <ThrowOnError extends boolean = false>(options: Options<UploadProjectFilesData, ThrowOnError>) => (options.client ?? client).post<UploadProjectFilesResponses, UploadProjectFilesErrors, ThrowOnError>({
    url: '/api/projects/{id}/files',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * The project's instructions (markdown). Readable by anyone with project access; empty until the owner first saves it. The content is injected into the system prompt of every chat in the project.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `project:read`: View projects and your own chats inside them
 */
export const getProjectInstructions = <ThrowOnError extends boolean = false>(options: Options<GetProjectInstructionsData, ThrowOnError>) => (options.client ?? client).get<GetProjectInstructionsResponses, GetProjectInstructionsErrors, ThrowOnError>({ url: '/api/projects/{id}/instructions', ...options });

/**
 * Set the project's instructions (owner only). The first save creates the instructions file; saving empty content keeps it but injects nothing.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `project:update`: Edit project descriptions, instructions, and sharing
 */
export const setProjectInstructions = <ThrowOnError extends boolean = false>(options: Options<SetProjectInstructionsData, ThrowOnError>) => (options.client ?? client).put<SetProjectInstructionsResponses, SetProjectInstructionsErrors, ThrowOnError>({
    url: '/api/projects/{id}/instructions',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * All chats in a project the caller can read. Chats authored by others require `project:read-all`; without it the caller sees only their own. `readOnly` marks chats authored by someone else (viewable, never writable).
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `project:read`: View projects and your own chats inside them
 */
export const getProjectConversations = <ThrowOnError extends boolean = false>(options: Options<GetProjectConversationsData, ThrowOnError>) => (options.client ?? client).get<GetProjectConversationsResponses, GetProjectConversationsErrors, ThrowOnError>({ url: '/api/projects/{id}/conversations', ...options });

/**
 * Remove the current user's pin on a project. Idempotent; allowed even if the project was since unshared from the user.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `project:read`: View projects and your own chats inside them
 */
export const unpinProject = <ThrowOnError extends boolean = false>(options: Options<UnpinProjectData, ThrowOnError>) => (options.client ?? client).delete<UnpinProjectResponses, UnpinProjectErrors, ThrowOnError>({ url: '/api/projects/{id}/pin', ...options });

/**
 * Pin a project to the current user's sidebar. Personal — does not affect other members. Any user who can read the project may pin it.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `project:read`: View projects and your own chats inside them
 */
export const pinProject = <ThrowOnError extends boolean = false>(options: Options<PinProjectData, ThrowOnError>) => (options.client ?? client).put<PinProjectResponses, PinProjectErrors, ThrowOnError>({ url: '/api/projects/{id}/pin', ...options });

/**
 * List scheduled agent triggers
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `scheduledTask:read`: View scheduled tasks and their run history
 */
export const getScheduleTriggers = <ThrowOnError extends boolean = false>(options?: Options<GetScheduleTriggersData, ThrowOnError>) => (options?.client ?? client).get<GetScheduleTriggersResponses, GetScheduleTriggersErrors, ThrowOnError>({ url: '/api/schedule-triggers', ...options });

/**
 * Create a scheduled agent trigger
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `scheduledTask:create`: Create new scheduled tasks and trigger runs
 */
export const createScheduleTrigger = <ThrowOnError extends boolean = false>(options: Options<CreateScheduleTriggerData, ThrowOnError>) => (options.client ?? client).post<CreateScheduleTriggerResponses, CreateScheduleTriggerErrors, ThrowOnError>({
    url: '/api/schedule-triggers',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete a scheduled agent trigger
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `scheduledTask:delete`: Delete scheduled tasks
 */
export const deleteScheduleTrigger = <ThrowOnError extends boolean = false>(options: Options<DeleteScheduleTriggerData, ThrowOnError>) => (options.client ?? client).delete<DeleteScheduleTriggerResponses, DeleteScheduleTriggerErrors, ThrowOnError>({ url: '/api/schedule-triggers/{id}', ...options });

/**
 * Get a scheduled agent trigger
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `scheduledTask:read`: View scheduled tasks and their run history
 */
export const getScheduleTrigger = <ThrowOnError extends boolean = false>(options: Options<GetScheduleTriggerData, ThrowOnError>) => (options.client ?? client).get<GetScheduleTriggerResponses, GetScheduleTriggerErrors, ThrowOnError>({ url: '/api/schedule-triggers/{id}', ...options });

/**
 * Update a scheduled agent trigger
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `scheduledTask:update`: Modify scheduled task configuration
 */
export const updateScheduleTrigger = <ThrowOnError extends boolean = false>(options: Options<UpdateScheduleTriggerData, ThrowOnError>) => (options.client ?? client).put<UpdateScheduleTriggerResponses, UpdateScheduleTriggerErrors, ThrowOnError>({
    url: '/api/schedule-triggers/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Enable a scheduled agent trigger
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `scheduledTask:update`: Modify scheduled task configuration
 */
export const enableScheduleTrigger = <ThrowOnError extends boolean = false>(options: Options<EnableScheduleTriggerData, ThrowOnError>) => (options.client ?? client).post<EnableScheduleTriggerResponses, EnableScheduleTriggerErrors, ThrowOnError>({ url: '/api/schedule-triggers/{id}/enable', ...options });

/**
 * Disable a scheduled agent trigger
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `scheduledTask:update`: Modify scheduled task configuration
 */
export const disableScheduleTrigger = <ThrowOnError extends boolean = false>(options: Options<DisableScheduleTriggerData, ThrowOnError>) => (options.client ?? client).post<DisableScheduleTriggerResponses, DisableScheduleTriggerErrors, ThrowOnError>({ url: '/api/schedule-triggers/{id}/disable', ...options });

/**
 * Run a scheduled agent trigger immediately
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `scheduledTask:create`: Create new scheduled tasks and trigger runs
 */
export const runScheduleTriggerNow = <ThrowOnError extends boolean = false>(options: Options<RunScheduleTriggerNowData, ThrowOnError>) => (options.client ?? client).post<RunScheduleTriggerNowResponses, RunScheduleTriggerNowErrors, ThrowOnError>({ url: '/api/schedule-triggers/{id}/run-now', ...options });

/**
 * List runs for a scheduled agent trigger
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `scheduledTask:read`: View scheduled tasks and their run history
 */
export const getScheduleTriggerRuns = <ThrowOnError extends boolean = false>(options: Options<GetScheduleTriggerRunsData, ThrowOnError>) => (options.client ?? client).get<GetScheduleTriggerRunsResponses, GetScheduleTriggerRunsErrors, ThrowOnError>({ url: '/api/schedule-triggers/{id}/runs', ...options });

/**
 * Get a single run for a scheduled agent trigger
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `scheduledTask:read`: View scheduled tasks and their run history
 */
export const getScheduleTriggerRun = <ThrowOnError extends boolean = false>(options: Options<GetScheduleTriggerRunData, ThrowOnError>) => (options.client ?? client).get<GetScheduleTriggerRunResponses, GetScheduleTriggerRunErrors, ThrowOnError>({ url: '/api/schedule-triggers/{id}/runs/{runId}', ...options });

/**
 * Create or return the chat conversation linked to a schedule run
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `scheduledTask:create`: Create new scheduled tasks and trigger runs
 */
export const createScheduleTriggerRunConversation = <ThrowOnError extends boolean = false>(options: Options<CreateScheduleTriggerRunConversationData, ThrowOnError>) => (options.client ?? client).post<CreateScheduleTriggerRunConversationResponses, CreateScheduleTriggerRunConversationErrors, ThrowOnError>({ url: '/api/schedule-triggers/{id}/runs/{runId}/conversation', ...options });

/**
 * Get the secrets manager type and configuration details (for Vault)
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `secret:read`: View secrets manager configuration
 */
export const getSecretsType = <ThrowOnError extends boolean = false>(options?: Options<GetSecretsTypeData, ThrowOnError>) => (options?.client ?? client).get<GetSecretsTypeResponses, GetSecretsTypeErrors, ThrowOnError>({ url: '/api/secrets/type', ...options });

/**
 * Get a secret by ID
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `secret:read`: View secrets manager configuration
 */
export const getSecret = <ThrowOnError extends boolean = false>(options: Options<GetSecretData, ThrowOnError>) => (options.client ?? client).get<GetSecretResponses, GetSecretErrors, ThrowOnError>({ url: '/api/secrets/{id}', ...options });

/**
 * Check connectivity to the secrets storage and return secret count.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `secret:update`: Modify secrets manager settings and test connectivity
 */
export const checkSecretsConnectivity = <ThrowOnError extends boolean = false>(options?: Options<CheckSecretsConnectivityData, ThrowOnError>) => (options?.client ?? client).post<CheckSecretsConnectivityResponses, CheckSecretsConnectivityErrors, ThrowOnError>({ url: '/api/secrets/check-connectivity', ...options });

/**
 * List organization service accounts
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `serviceAccount:read`: View service accounts
 */
export const getServiceAccounts = <ThrowOnError extends boolean = false>(options?: Options<GetServiceAccountsData, ThrowOnError>) => (options?.client ?? client).get<GetServiceAccountsResponses, GetServiceAccountsErrors, ThrowOnError>({ url: '/api/service-accounts', ...options });

/**
 * Create an organization service account
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `serviceAccount:create`: Create service accounts
 */
export const createServiceAccount = <ThrowOnError extends boolean = false>(options: Options<CreateServiceAccountData, ThrowOnError>) => (options.client ?? client).post<CreateServiceAccountResponses, CreateServiceAccountErrors, ThrowOnError>({
    url: '/api/service-accounts',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete an organization service account
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `serviceAccount:delete`: Delete service accounts
 */
export const deleteServiceAccount = <ThrowOnError extends boolean = false>(options: Options<DeleteServiceAccountData, ThrowOnError>) => (options.client ?? client).delete<DeleteServiceAccountResponses, DeleteServiceAccountErrors, ThrowOnError>({ url: '/api/service-accounts/{id}', ...options });

/**
 * Get one organization service account
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `serviceAccount:read`: View service accounts
 */
export const getServiceAccount = <ThrowOnError extends boolean = false>(options: Options<GetServiceAccountData, ThrowOnError>) => (options.client ?? client).get<GetServiceAccountResponses, GetServiceAccountErrors, ThrowOnError>({ url: '/api/service-accounts/{id}', ...options });

/**
 * Update an organization service account
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `serviceAccount:update`: Modify service accounts
 */
export const updateServiceAccount = <ThrowOnError extends boolean = false>(options: Options<UpdateServiceAccountData, ThrowOnError>) => (options.client ?? client).patch<UpdateServiceAccountResponses, UpdateServiceAccountErrors, ThrowOnError>({
    url: '/api/service-accounts/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete several service accounts in one request, along with their tokens. Ids outside the caller's organization are reported in `failed` as not found and leave the rest of the batch applied.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `serviceAccount:delete`: Delete service accounts
 */
export const bulkDeleteServiceAccounts = <ThrowOnError extends boolean = false>(options: Options<BulkDeleteServiceAccountsData, ThrowOnError>) => (options.client ?? client).delete<BulkDeleteServiceAccountsResponses, BulkDeleteServiceAccountsErrors, ThrowOnError>({
    url: '/api/service-accounts/bulk',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Enable or disable several service accounts in one request. A disabled account authenticates with none of its keys, so this is the reversible way to stop an automation without destroying the keys it would need to start again. Ids outside the caller's organization are reported in `failed` as not found and leave the rest of the batch applied. An account already in the requested state is reported as succeeded without being rewritten.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `serviceAccount:update`: Modify service accounts
 */
export const bulkSetServiceAccountsDisabled = <ThrowOnError extends boolean = false>(options: Options<BulkSetServiceAccountsDisabledData, ThrowOnError>) => (options.client ?? client).patch<BulkSetServiceAccountsDisabledResponses, BulkSetServiceAccountsDisabledErrors, ThrowOnError>({
    url: '/api/service-accounts/bulk',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Create a token for an organization service account
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `serviceAccount:update`: Modify service accounts
 */
export const createServiceAccountToken = <ThrowOnError extends boolean = false>(options: Options<CreateServiceAccountTokenData, ThrowOnError>) => (options.client ?? client).post<CreateServiceAccountTokenResponses, CreateServiceAccountTokenErrors, ThrowOnError>({
    url: '/api/service-accounts/{id}/tokens',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete one service account token
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `serviceAccount:update`: Modify service accounts
 */
export const deleteServiceAccountToken = <ThrowOnError extends boolean = false>(options: Options<DeleteServiceAccountTokenData, ThrowOnError>) => (options.client ?? client).delete<DeleteServiceAccountTokenResponses, DeleteServiceAccountTokenErrors, ThrowOnError>({ url: '/api/service-accounts/{id}/tokens/{tokenId}', ...options });

/**
 * Update one service account token
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `serviceAccount:update`: Modify service accounts
 */
export const updateServiceAccountToken = <ThrowOnError extends boolean = false>(options: Options<UpdateServiceAccountTokenData, ThrowOnError>) => (options.client ?? client).patch<UpdateServiceAccountTokenResponses, UpdateServiceAccountTokenErrors, ThrowOnError>({
    url: '/api/service-accounts/{id}/tokens/{tokenId}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Revoke several non-current sessions belonging to the authenticated user in one request. Sessions belonging to another user and the current session are reported in `failed`; the rest are revoked.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * None (no additional RBAC permission required)
 */
export const bulkRevokeSessions = <ThrowOnError extends boolean = false>(options: Options<BulkRevokeSessionsData, ThrowOnError>) => (options.client ?? client).delete<BulkRevokeSessionsResponses, BulkRevokeSessionsErrors, ThrowOnError>({
    url: '/api/sessions/bulk',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get the active site notification for the current organization
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `siteNotification:read`: View site-wide notifications
 */
export const getSiteNotification = <ThrowOnError extends boolean = false>(options?: Options<GetSiteNotificationData, ThrowOnError>) => (options?.client ?? client).get<GetSiteNotificationResponses, GetSiteNotificationErrors, ThrowOnError>({ url: '/api/site-notification', ...options });

/**
 * Create a new site notification
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `siteNotification:create`: Create new site notifications
 */
export const createSiteNotification = <ThrowOnError extends boolean = false>(options: Options<CreateSiteNotificationData, ThrowOnError>) => (options.client ?? client).post<CreateSiteNotificationResponses, CreateSiteNotificationErrors, ThrowOnError>({
    url: '/api/site-notification',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get the latest site notification for settings management
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `siteNotification:read`: View site-wide notifications
 */
export const getSiteNotificationSettings = <ThrowOnError extends boolean = false>(options?: Options<GetSiteNotificationSettingsData, ThrowOnError>) => (options?.client ?? client).get<GetSiteNotificationSettingsResponses, GetSiteNotificationSettingsErrors, ThrowOnError>({ url: '/api/site-notification/settings', ...options });

/**
 * Delete a site notification
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `siteNotification:delete`: Delete site notifications
 */
export const deleteSiteNotification = <ThrowOnError extends boolean = false>(options: Options<DeleteSiteNotificationData, ThrowOnError>) => (options.client ?? client).delete<DeleteSiteNotificationResponses, DeleteSiteNotificationErrors, ThrowOnError>({ url: '/api/site-notification/{id}', ...options });

/**
 * Update an existing site notification
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `siteNotification:update`: Modify site notifications
 */
export const updateSiteNotification = <ThrowOnError extends boolean = false>(options: Options<UpdateSiteNotificationData, ThrowOnError>) => (options.client ?? client).put<UpdateSiteNotificationResponses, UpdateSiteNotificationErrors, ThrowOnError>({
    url: '/api/site-notification/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * The static skill marketplace: one clone URL for the whole deployment, which every user installs with their own credential.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `skill:read`: View and use agent skills within your scope (org, your teams, your own)
 */
export const getSkillMarketplace = <ThrowOnError extends boolean = false>(options?: Options<GetSkillMarketplaceData, ThrowOnError>) => (options?.client ?? client).get<GetSkillMarketplaceResponses, GetSkillMarketplaceErrors, ThrowOnError>({ url: '/api/skill-marketplace', ...options });

/**
 * List all agent skills for the organization
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `skill:read`: View and use agent skills within your scope (org, your teams, your own)
 */
export const getSkills = <ThrowOnError extends boolean = false>(options?: Options<GetSkillsData, ThrowOnError>) => (options?.client ?? client).get<GetSkillsResponses, GetSkillsErrors, ThrowOnError>({ url: '/api/skills', ...options });

/**
 * Create a skill from a raw SKILL.md and resource files
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `skill:create`: Create new agent skills
 */
export const createSkill = <ThrowOnError extends boolean = false>(options: Options<CreateSkillData, ThrowOnError>) => (options.client ?? client).post<CreateSkillResponses, CreateSkillErrors, ThrowOnError>({
    url: '/api/skills',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Convert an internal agent into a new Agent Skill. The skill inherits the agent's scope. The source agent is left intact unless deleteAgent is set.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `agent:read`: View and list agents
 * `skill:create`: Create new agent skills
 */
export const convertAgentToSkill = <ThrowOnError extends boolean = false>(options: Options<ConvertAgentToSkillData, ThrowOnError>) => (options.client ?? client).post<ConvertAgentToSkillResponses, ConvertAgentToSkillErrors, ThrowOnError>({
    url: '/api/agents/{id}/convert-to-skill',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Suggest a skill description for an agent using an LLM, for the convert-to-skill flow. Does not modify the agent or create a skill.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `agent:read`: View and list agents
 * `chat:read`: View and access chat conversations
 * `skill:create`: Create new agent skills
 */
export const suggestSkillDescription = <ThrowOnError extends boolean = false>(options: Options<SuggestSkillDescriptionData, ThrowOnError>) => (options.client ?? client).post<SuggestSkillDescriptionResponses, SuggestSkillDescriptionErrors, ThrowOnError>({ url: '/api/agents/{id}/suggest-skill-description', ...options });

/**
 * Delete a skill and its resource files
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `skill:delete`: Delete agent skills
 */
export const deleteSkill = <ThrowOnError extends boolean = false>(options: Options<DeleteSkillData, ThrowOnError>) => (options.client ?? client).delete<DeleteSkillResponses, DeleteSkillErrors, ThrowOnError>({ url: '/api/skills/{id}', ...options });

/**
 * Get a skill with its resource files
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `skill:read`: View and use agent skills within your scope (org, your teams, your own)
 */
export const getSkill = <ThrowOnError extends boolean = false>(options: Options<GetSkillData, ThrowOnError>) => (options.client ?? client).get<GetSkillResponses, GetSkillErrors, ThrowOnError>({ url: '/api/skills/{id}', ...options });

/**
 * Update a skill's SKILL.md, resource files, and scope
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `skill:update`: Modify agent skills and their team assignments
 */
export const updateSkill = <ThrowOnError extends boolean = false>(options: Options<UpdateSkillData, ThrowOnError>) => (options.client ?? client).put<UpdateSkillResponses, UpdateSkillErrors, ThrowOnError>({
    url: '/api/skills/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Per-user activation counts for a skill over the last 30 days
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `skill:read`: View and use agent skills within your scope (org, your teams, your own)
 */
export const getSkillUsageStatistics = <ThrowOnError extends boolean = false>(options: Options<GetSkillUsageStatisticsData, ThrowOnError>) => (options.client ?? client).get<GetSkillUsageStatisticsResponses, GetSkillUsageStatisticsErrors, ThrowOnError>({ url: '/api/skills/{id}/usage-statistics', ...options });

/**
 * List a skill's version history, newest first, as metadata only (no SKILL.md body). Version numbers are contiguous from 1. Paginated.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `skill:read`: View and use agent skills within your scope (org, your teams, your own)
 */
export const getSkillVersions = <ThrowOnError extends boolean = false>(options: Options<GetSkillVersionsData, ThrowOnError>) => (options.client ?? client).get<GetSkillVersionsResponses, GetSkillVersionsErrors, ThrowOnError>({ url: '/api/skills/{id}/versions', ...options });

/**
 * Get one immutable skill version: the SKILL.md body it captured plus its resource-file snapshots.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `skill:read`: View and use agent skills within your scope (org, your teams, your own)
 */
export const getSkillVersion = <ThrowOnError extends boolean = false>(options: Options<GetSkillVersionData, ThrowOnError>) => (options.client ?? client).get<GetSkillVersionResponses, GetSkillVersionErrors, ThrowOnError>({ url: '/api/skills/{id}/versions/{version}', ...options });

/**
 * Move several skills to one visibility scope in a single request, replacing their team assignments (`scope = team`) or per-person grants (`scope = personal`). Nothing else about the skills is touched: no version is forked, and GitHub-synced skills are included because their read-only content is not involved. The target scope and teams are validated once for the whole request (400/403 changes nothing); per-skill problems — an id the caller cannot see or modify, or a name already taken in the target visibility namespace — are reported in `failed` and leave the rest of the batch applied. A skill already in the requested state is reported as succeeded without being rewritten.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `skill:update`: Modify agent skills and their team assignments
 */
export const bulkUpdateSkillsVisibility = <ThrowOnError extends boolean = false>(options: Options<BulkUpdateSkillsVisibilityData, ThrowOnError>) => (options.client ?? client).post<BulkUpdateSkillsVisibilityResponses, BulkUpdateSkillsVisibilityErrors, ThrowOnError>({
    url: '/api/skills/bulk-visibility',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List distinct GitHub repositories that skills in this organization were imported from
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `skill:read`: View and use agent skills within your scope (org, your teams, your own)
 */
export const getSkillSourceRepos = <ThrowOnError extends boolean = false>(options?: Options<GetSkillSourceReposData, ThrowOnError>) => (options?.client ?? client).get<GetSkillSourceReposResponses, GetSkillSourceReposErrors, ThrowOnError>({ url: '/api/skills/source-repos', ...options });

/**
 * Soft-delete several skills in one request. Each id is authorized exactly as the single-skill delete authorizes its own: an id the caller cannot see or modify is reported in `failed` and the rest of the batch still applies. Deleted skills keep their versions and resource files and can be restored from the trash.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `skill:delete`: Delete agent skills
 */
export const bulkDeleteSkills = <ThrowOnError extends boolean = false>(options: Options<BulkDeleteSkillsData, ThrowOnError>) => (options.client ?? client).post<BulkDeleteSkillsResponses, BulkDeleteSkillsErrors, ThrowOnError>({
    url: '/api/skills/bulk-delete',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Restore a soft-deleted skill
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `skill:delete`: Delete agent skills
 */
export const restoreSkill = <ThrowOnError extends boolean = false>(options: Options<RestoreSkillData, ThrowOnError>) => (options.client ?? client).post<RestoreSkillResponses, RestoreSkillErrors, ThrowOnError>({ url: '/api/skills/{id}/restore', ...options });

/**
 * Permanently destroy a soft-deleted skill (global admins only). Irreversible, with no grace period: every version and resource file is destroyed, along with the skill's team and user grants, environment assignments, usage events, and share links — so any public share URL for it stops working. 404 if there is no soft-deleted skill with that id in the org, which is also the answer when the skill is still live or the caller is not a global admin. 409 while a sandbox still has the skill mounted; that is retryable, but note nothing clears a mount on its own — it lasts as long as the sandbox does. Built-in skills cannot be purged: their soft-deleted row is what stops the seeder recreating them on the next restart. Restore wins a race.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * Restricted to the built-in Admin and Platform Admin roles. No permission grants it: `skill:delete` and even `skill:admin` reach the trash, not past it, and a service account never qualifies. A caller without it gets 404, not 403, so the endpoint never confirms the skill exists.
 */
export const permanentlyDeleteSkill = <ThrowOnError extends boolean = false>(options: Options<PermanentlyDeleteSkillData, ThrowOnError>) => (options.client ?? client).delete<PermanentlyDeleteSkillResponses, PermanentlyDeleteSkillErrors, ThrowOnError>({ url: '/api/skills/{id}/permanent', ...options });

/**
 * Reset a built-in skill to its shipped default SKILL.md and resource files. Only applies to skills with sourceType `built_in`.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `skill:update`: Modify agent skills and their team assignments
 */
export const resetSkill = <ThrowOnError extends boolean = false>(options: Options<ResetSkillData, ThrowOnError>) => (options.client ?? client).post<ResetSkillResponses, ResetSkillErrors, ThrowOnError>({ url: '/api/skills/{id}/reset', ...options });

/**
 * Manage a GitHub-synced skill: change its pull frequency, trigger an immediate pull, or disconnect it from the GitHub source (interval null) — a disconnected skill keeps its content and provenance and becomes editable in the app.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `skill:update`: Modify agent skills and their team assignments
 */
export const updateSkillGithubSync = <ThrowOnError extends boolean = false>(options: Options<UpdateSkillGithubSyncData, ThrowOnError>) => (options.client ?? client).patch<UpdateSkillGithubSyncResponses, UpdateSkillGithubSyncErrors, ThrowOnError>({
    url: '/api/skills/{id}/github-sync',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Enable the Agent Skill tools (`list_skills`, `load_skill`) for this organization. Sets the org-level flag and backfills the tools onto every existing agent. Idempotent.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `skill:admin`: Full administrative control over all agent skills, bypassing team restrictions
 */
export const enableSkillToolDefaults = <ThrowOnError extends boolean = false>(options?: Options<EnableSkillToolDefaultsData, ThrowOnError>) => (options?.client ?? client).post<EnableSkillToolDefaultsResponses, EnableSkillToolDefaultsErrors, ThrowOnError>({ url: '/api/skills/enable-defaults', ...options });

/**
 * Search the crawled public-GitHub skill catalog by name, repo, path, or description. Backed by an in-memory token index; returns ranked candidates to import via the GitHub import endpoints.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `skill:read`: View and use agent skills within your scope (org, your teams, your own)
 */
export const searchSkillCatalog = <ThrowOnError extends boolean = false>(options?: Options<SearchSkillCatalogData, ThrowOnError>) => (options?.client ?? client).get<SearchSkillCatalogResponses, SearchSkillCatalogErrors, ThrowOnError>({ url: '/api/skills/catalog/search', ...options });

/**
 * Discover skills in a GitHub repository
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `skill:read`: View and use agent skills within your scope (org, your teams, your own)
 */
export const discoverGithubSkills = <ThrowOnError extends boolean = false>(options: Options<DiscoverGithubSkillsData, ThrowOnError>) => (options.client ?? client).post<DiscoverGithubSkillsResponses, DiscoverGithubSkillsErrors, ThrowOnError>({
    url: '/api/skills/github/discover',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Fetch a single skill's manifest and files from GitHub without persisting it.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `skill:read`: View and use agent skills within your scope (org, your teams, your own)
 */
export const previewGithubSkill = <ThrowOnError extends boolean = false>(options: Options<PreviewGithubSkillData, ThrowOnError>) => (options.client ?? client).post<PreviewGithubSkillResponses, PreviewGithubSkillErrors, ThrowOnError>({
    url: '/api/skills/github/preview',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Import selected skills from a GitHub repository
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `skill:create`: Create new agent skills
 */
export const importGithubSkills = <ThrowOnError extends boolean = false>(options: Options<ImportGithubSkillsData, ThrowOnError>) => (options.client ?? client).post<ImportGithubSkillsResponses, ImportGithubSkillsErrors, ThrowOnError>({
    url: '/api/skills/github/import',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete a persistent file. Allowed for the file's author, or anyone with access to the project owning the file.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `sandbox:execute`: Run commands and upload/download files in code execution sandboxes
 */
export const deleteSkillSandboxArtifact = <ThrowOnError extends boolean = false>(options: Options<DeleteSkillSandboxArtifactData, ThrowOnError>) => (options.client ?? client).delete<DeleteSkillSandboxArtifactResponses, DeleteSkillSandboxArtifactErrors, ThrowOnError>({ url: '/api/skill-sandbox/artifacts/{artifactId}', ...options });

/**
 * Stream the raw bytes of a skill sandbox artifact. Inline for known-safe raster images and real PDFs, so the browser can preview them; download for everything else.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `sandbox:execute`: Run commands and upload/download files in code execution sandboxes
 */
export const getSkillSandboxArtifact = <ThrowOnError extends boolean = false>(options: Options<GetSkillSandboxArtifactData, ThrowOnError>) => (options.client ?? client).get<GetSkillSandboxArtifactResponses, unknown, ThrowOnError>({ url: '/api/skill-sandbox/artifacts/{artifactId}', ...options });

/**
 * Overwrite the full text content of a row-backed Markdown/plain-text file. Allowed for the file's author, or anyone with access to the project owning the file.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `sandbox:execute`: Run commands and upload/download files in code execution sandboxes
 */
export const updateSkillSandboxArtifactContent = <ThrowOnError extends boolean = false>(options: Options<UpdateSkillSandboxArtifactContentData, ThrowOnError>) => (options.client ?? client).put<UpdateSkillSandboxArtifactContentResponses, UpdateSkillSandboxArtifactContentErrors, ThrowOnError>({
    url: '/api/skill-sandbox/artifacts/{artifactId}/content',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List the artifact files produced in a conversation's sandbox.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `sandbox:execute`: Run commands and upload/download files in code execution sandboxes
 */
export const getSkillSandboxConversationArtifacts = <ThrowOnError extends boolean = false>(options: Options<GetSkillSandboxConversationArtifactsData, ThrowOnError>) => (options.client ?? client).get<GetSkillSandboxConversationArtifactsResponses, GetSkillSandboxConversationArtifactsErrors, ThrowOnError>({ url: '/api/skill-sandbox/conversations/{conversationId}/artifacts', ...options });

/**
 * List skill share links for the organization, optionally filtered by skill
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `skill:admin`: Full administrative control over all agent skills, bypassing team restrictions
 */
export const getSkillShareLinks = <ThrowOnError extends boolean = false>(options?: Options<GetSkillShareLinksData, ThrowOnError>) => (options?.client ?? client).get<GetSkillShareLinksResponses, GetSkillShareLinksErrors, ThrowOnError>({ url: '/api/skill-share-links', ...options });

/**
 * Create a share link exposing one or more skills via the public marketplace endpoint. The raw token is returned exactly once.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `skill:admin`: Full administrative control over all agent skills, bypassing team restrictions
 */
export const createSkillShareLink = <ThrowOnError extends boolean = false>(options: Options<CreateSkillShareLinkData, ThrowOnError>) => (options.client ?? client).post<CreateSkillShareLinkResponses, CreateSkillShareLinkErrors, ThrowOnError>({
    url: '/api/skill-share-links',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Rotate a share link: revoke it and create its replacement in one transaction, so no failure mode leaves both tokens live. The new raw token is returned exactly once.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `skill:admin`: Full administrative control over all agent skills, bypassing team restrictions
 */
export const rotateSkillShareLink = <ThrowOnError extends boolean = false>(options: Options<RotateSkillShareLinkData, ThrowOnError>) => (options.client ?? client).post<RotateSkillShareLinkResponses, RotateSkillShareLinkErrors, ThrowOnError>({
    url: '/api/skill-share-links/{id}/rotate',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Revoke a skill share link. Idempotent: revoking an already-revoked link is a no-op.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `skill:admin`: Full administrative control over all agent skills, bypassing team restrictions
 */
export const revokeSkillShareLink = <ThrowOnError extends boolean = false>(options: Options<RevokeSkillShareLinkData, ThrowOnError>) => (options.client ?? client).delete<RevokeSkillShareLinkResponses, RevokeSkillShareLinkErrors, ThrowOnError>({ url: '/api/skill-share-links/{id}', ...options });

/**
 * Get team statistics
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `llmCost:read`: View LLM usage cost statistics and analytics
 */
export const getTeamStatistics = <ThrowOnError extends boolean = false>(options?: Options<GetTeamStatisticsData, ThrowOnError>) => (options?.client ?? client).get<GetTeamStatisticsResponses, GetTeamStatisticsErrors, ThrowOnError>({ url: '/api/statistics/teams', ...options });

/**
 * Get agent statistics
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `llmCost:read`: View LLM usage cost statistics and analytics
 */
export const getAgentStatistics = <ThrowOnError extends boolean = false>(options?: Options<GetAgentStatisticsData, ThrowOnError>) => (options?.client ?? client).get<GetAgentStatisticsResponses, GetAgentStatisticsErrors, ThrowOnError>({ url: '/api/statistics/agents', ...options });

/**
 * Get model statistics
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `llmCost:read`: View LLM usage cost statistics and analytics
 */
export const getModelStatistics = <ThrowOnError extends boolean = false>(options?: Options<GetModelStatisticsData, ThrowOnError>) => (options?.client ?? client).get<GetModelStatisticsResponses, GetModelStatisticsErrors, ThrowOnError>({ url: '/api/statistics/models', ...options });

/**
 * Get per-user usage statistics (requests, tokens, cost and model mix), for AI-adoption reporting. Paginated because user cardinality is unbounded. Only traffic that carries a resolved user identity is included; requests authenticated with a shared credential and no user context are not attributed to anyone.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `llmCost:read`: View LLM usage cost statistics and analytics
 */
export const getUserStatistics = <ThrowOnError extends boolean = false>(options?: Options<GetUserStatisticsData, ThrowOnError>) => (options?.client ?? client).get<GetUserStatisticsResponses, GetUserStatisticsErrors, ThrowOnError>({ url: '/api/statistics/users', ...options });

/**
 * Get the calling user's own cost and usage for a timeframe: requests, tokens, billed and subscription-covered spend, active days and model mix. Reports only the caller's own activity, so unlike the other statistics endpoints it requires no permission over organization-wide cost data.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * None (no additional RBAC permission required)
 */
export const getMyStatistics = <ThrowOnError extends boolean = false>(options?: Options<GetMyStatisticsData, ThrowOnError>) => (options?.client ?? client).get<GetMyStatisticsResponses, GetMyStatisticsErrors, ThrowOnError>({ url: '/api/statistics/me', ...options });

/**
 * Explain the calling user's own usage: the client applications that generated it, the price band their tokens fell into (fresh input, cache read, cache write, output, plus what caching cost and saved), how their requests were distributed across context sizes, and which of their sessions concentrated the spend. Cost here is list price across both billing modes, because this is a consumption view — a caller on a flat-rate plan has no billed spend to apportion. Reports only the caller's own activity, so like `/api/statistics/me` it requires no permission over organization-wide cost data.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * None (no additional RBAC permission required)
 */
export const getMyUsageBreakdown = <ThrowOnError extends boolean = false>(options?: Options<GetMyUsageBreakdownData, ThrowOnError>) => (options?.client ?? client).get<GetMyUsageBreakdownResponses, GetMyUsageBreakdownErrors, ThrowOnError>({ url: '/api/statistics/me/breakdown', ...options });

/**
 * Get per-MCP-App cost: what each app cost to build (the LLM spend of the session that authored it) and what it costs to run (its own archestra.llm.complete() calls, plus how often it is opened). Includes an estimated chat-equivalent cost per app, derived from the organization's measured average cost per chat session over the same timeframe — the baseline is returned alongside so the estimate is auditable. Paginated. Apps outside the caller's visibility are excluded.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `app:read`: View and run MCP Apps within your scope (org, your teams, your own)
 * `llmCost:read`: View LLM usage cost statistics and analytics
 */
export const getAppStatistics = <ThrowOnError extends boolean = false>(options?: Options<GetAppStatisticsData, ThrowOnError>) => (options?.client ?? client).get<GetAppStatisticsResponses, GetAppStatisticsErrors, ThrowOnError>({ url: '/api/statistics/apps', ...options });

/**
 * Get per-skill cost. `contextTokens` is the skill's own footprint — the tokens its activation blocks added to the model's context, measured when they were injected. The `attributed*` figures are the spend of the turns that then ran with the skill in context, which is shared with everything else in those turns rather than being the skill's bill alone — they are computed for the returned page, so they are not a sortable column. Paginated; skills outside the caller's scope are excluded.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `llmCost:read`: View LLM usage cost statistics and analytics
 * `skill:read`: View and use agent skills within your scope (org, your teams, your own)
 */
export const getSkillStatistics = <ThrowOnError extends boolean = false>(options?: Options<GetSkillStatisticsData, ThrowOnError>) => (options?.client ?? client).get<GetSkillStatisticsResponses, GetSkillStatisticsErrors, ThrowOnError>({ url: '/api/statistics/skills', ...options });

/**
 * Get overview statistics
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `llmCost:read`: View LLM usage cost statistics and analytics
 */
export const getOverviewStatistics = <ThrowOnError extends boolean = false>(options?: Options<GetOverviewStatisticsData, ThrowOnError>) => (options?.client ?? client).get<GetOverviewStatisticsResponses, GetOverviewStatisticsErrors, ThrowOnError>({ url: '/api/statistics/overview', ...options });

/**
 * Get cost savings statistics
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `llmCost:read`: View LLM usage cost statistics and analytics
 */
export const getCostSavingsStatistics = <ThrowOnError extends boolean = false>(options?: Options<GetCostSavingsStatisticsData, ThrowOnError>) => (options?.client ?? client).get<GetCostSavingsStatisticsResponses, GetCostSavingsStatisticsErrors, ThrowOnError>({ url: '/api/statistics/cost-savings', ...options });

/**
 * Get all teams in the organization
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `team:read`: View teams and their members
 */
export const getTeams = <ThrowOnError extends boolean = false>(options?: Options<GetTeamsData, ThrowOnError>) => (options?.client ?? client).get<GetTeamsResponses, GetTeamsErrors, ThrowOnError>({ url: '/api/teams', ...options });

/**
 * Create a new team
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `team:create`: Create new teams
 */
export const createTeam = <ThrowOnError extends boolean = false>(options: Options<CreateTeamData, ThrowOnError>) => (options.client ?? client).post<CreateTeamResponses, CreateTeamErrors, ThrowOnError>({
    url: '/api/teams',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete a team
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `team:delete`: Delete teams
 */
export const deleteTeam = <ThrowOnError extends boolean = false>(options: Options<DeleteTeamData, ThrowOnError>) => (options.client ?? client).delete<DeleteTeamResponses, DeleteTeamErrors, ThrowOnError>({ url: '/api/teams/{id}', ...options });

/**
 * Get a team by ID
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `team:read`: View teams and their members
 */
export const getTeam = <ThrowOnError extends boolean = false>(options: Options<GetTeamData, ThrowOnError>) => (options.client ?? client).get<GetTeamResponses, GetTeamErrors, ThrowOnError>({ url: '/api/teams/{id}', ...options });

/**
 * Update a team
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `team:read`: View teams and their members
 */
export const updateTeam = <ThrowOnError extends boolean = false>(options: Options<UpdateTeamData, ThrowOnError>) => (options.client ?? client).put<UpdateTeamResponses, UpdateTeamErrors, ThrowOnError>({
    url: '/api/teams/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete several teams in one request. Ids outside the caller's organization are reported in `failed` as not found and leave the rest of the batch applied. Resources scoped to a deleted team lose that assignment, exactly as they do on the single-team delete.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `team:delete`: Delete teams
 */
export const bulkDeleteTeams = <ThrowOnError extends boolean = false>(options: Options<BulkDeleteTeamsData, ThrowOnError>) => (options.client ?? client).delete<BulkDeleteTeamsResponses, BulkDeleteTeamsErrors, ThrowOnError>({
    url: '/api/teams/bulk',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get all members of a team
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `team:read`: View teams and their members
 */
export const getTeamMembers = <ThrowOnError extends boolean = false>(options: Options<GetTeamMembersData, ThrowOnError>) => (options.client ?? client).get<GetTeamMembersResponses, GetTeamMembersErrors, ThrowOnError>({ url: '/api/teams/{id}/members', ...options });

/**
 * Add a member to a team
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `team:read`: View teams and their members
 */
export const addTeamMember = <ThrowOnError extends boolean = false>(options: Options<AddTeamMemberData, ThrowOnError>) => (options.client ?? client).post<AddTeamMemberResponses, AddTeamMemberErrors, ThrowOnError>({
    url: '/api/teams/{id}/members',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Remove a member from a team
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `team:read`: View teams and their members
 */
export const removeTeamMember = <ThrowOnError extends boolean = false>(options: Options<RemoveTeamMemberData, ThrowOnError>) => (options.client ?? client).delete<RemoveTeamMemberResponses, RemoveTeamMemberErrors, ThrowOnError>({ url: '/api/teams/{id}/members/{userId}', ...options });

/**
 * Update a team member role
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `team:read`: View teams and their members
 */
export const updateTeamMember = <ThrowOnError extends boolean = false>(options: Options<UpdateTeamMemberData, ThrowOnError>) => (options.client ?? client).put<UpdateTeamMemberResponses, UpdateTeamMemberErrors, ThrowOnError>({
    url: '/api/teams/{id}/members/{userId}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get all label keys used by teams
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `team:read`: View teams and their members
 */
export const getTeamLabelKeys = <ThrowOnError extends boolean = false>(options?: Options<GetTeamLabelKeysData, ThrowOnError>) => (options?.client ?? client).get<GetTeamLabelKeysResponses, GetTeamLabelKeysErrors, ThrowOnError>({ url: '/api/teams/labels/keys', ...options });

/**
 * Get all label values used by teams
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `team:read`: View teams and their members
 */
export const getTeamLabelValues = <ThrowOnError extends boolean = false>(options?: Options<GetTeamLabelValuesData, ThrowOnError>) => (options?.client ?? client).get<GetTeamLabelValuesResponses, GetTeamLabelValuesErrors, ThrowOnError>({ url: '/api/teams/labels/values', ...options });

/**
 * Get all external groups mapped to a team for SSO team sync
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `team:read`: View teams and their members
 */
export const getTeamExternalGroups = <ThrowOnError extends boolean = false>(options: Options<GetTeamExternalGroupsData, ThrowOnError>) => (options.client ?? client).get<GetTeamExternalGroupsResponses, GetTeamExternalGroupsErrors, ThrowOnError>({ url: '/api/teams/{id}/external-groups', ...options });

/**
 * Add an external group mapping to a team for SSO team sync
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `team:read`: View teams and their members
 */
export const addTeamExternalGroup = <ThrowOnError extends boolean = false>(options: Options<AddTeamExternalGroupData, ThrowOnError>) => (options.client ?? client).post<AddTeamExternalGroupResponses, AddTeamExternalGroupErrors, ThrowOnError>({
    url: '/api/teams/{id}/external-groups',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Remove an external group mapping from a team for SSO team sync
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `team:read`: View teams and their members
 */
export const removeTeamExternalGroup = <ThrowOnError extends boolean = false>(options: Options<RemoveTeamExternalGroupData, ThrowOnError>) => (options.client ?? client).delete<RemoveTeamExternalGroupResponses, RemoveTeamExternalGroupErrors, ThrowOnError>({ url: '/api/teams/{id}/external-groups/{groupId}', ...options });

/**
 * Get tokens visible to the user based on their permissions
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `team:read`: View teams and their members
 */
export const getTokens = <ThrowOnError extends boolean = false>(options?: Options<GetTokensData, ThrowOnError>) => (options?.client ?? client).get<GetTokensResponses, GetTokensErrors, ThrowOnError>({ url: '/api/tokens', ...options });

/**
 * Get the full token value (for copying to clipboard)
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `team:read`: View teams and their members
 */
export const getTokenValue = <ThrowOnError extends boolean = false>(options: Options<GetTokenValueData, ThrowOnError>) => (options.client ?? client).get<GetTokenValueResponses, GetTokenValueErrors, ThrowOnError>({ url: '/api/tokens/{tokenId}/value', ...options });

/**
 * Rotate a token (generate new value)
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `team:read`: View teams and their members
 */
export const rotateToken = <ThrowOnError extends boolean = false>(options: Options<RotateTokenData, ThrowOnError>) => (options.client ?? client).post<RotateTokenResponses, RotateTokenErrors, ThrowOnError>({ url: '/api/tokens/{tokenId}/rotate', ...options });

/**
 * Get all tools
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `toolPolicy:read`: View tools, tool invocation policies, and trusted data policies
 */
export const getTools = <ThrowOnError extends boolean = false>(options?: Options<GetToolsData, ThrowOnError>) => (options?.client ?? client).get<GetToolsResponses, GetToolsErrors, ThrowOnError>({ url: '/api/tools', ...options });

/**
 * Get all tools with their profile assignments (one entry per tool)
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `toolPolicy:read`: View tools, tool invocation policies, and trusted data policies
 */
export const getToolsWithAssignments = <ThrowOnError extends boolean = false>(options?: Options<GetToolsWithAssignmentsData, ThrowOnError>) => (options?.client ?? client).get<GetToolsWithAssignmentsResponses, GetToolsWithAssignmentsErrors, ThrowOnError>({ url: '/api/tools/with-assignments', ...options });

/**
 * Get filter options for observed tools: the users who have observed tools in LLM proxy traffic, and the client app families their observations came from
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `toolPolicy:read`: View tools, tool invocation policies, and trusted data policies
 */
export const getToolObservers = <ThrowOnError extends boolean = false>(options?: Options<GetToolObserversData, ThrowOnError>) => (options?.client ?? client).get<GetToolObserversResponses, GetToolObserversErrors, ThrowOnError>({ url: '/api/tools/observers', ...options });

/**
 * Delete an auto-discovered tool (tools without an MCP server)
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `toolPolicy:delete`: Remove tools and security policies
 */
export const deleteTool = <ThrowOnError extends boolean = false>(options: Options<DeleteToolData, ThrowOnError>) => (options.client ?? client).delete<DeleteToolResponses, DeleteToolErrors, ThrowOnError>({ url: '/api/tools/{id}', ...options });

/**
 * Get a single tool's policy-editor fields (id, name, parameters) by id, scoped to what the caller can access
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `toolPolicy:read`: View tools, tool invocation policies, and trusted data policies
 */
export const getTool = <ThrowOnError extends boolean = false>(options: Options<GetToolData, ThrowOnError>) => (options.client ?? client).get<GetToolResponses, GetToolErrors, ThrowOnError>({ url: '/api/tools/{id}', ...options });

/**
 * Get current user's permissions
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * None (no additional RBAC permission required)
 */
export const getUserPermissions = <ThrowOnError extends boolean = false>(options?: Options<GetUserPermissionsData, ThrowOnError>) => (options?.client ?? client).get<GetUserPermissionsResponses, GetUserPermissionsErrors, ThrowOnError>({ url: '/api/user/permissions', ...options });

/**
 * List users in the caller's organization that admins can impersonate (role debugger)
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `member:impersonate`: Temporarily sign in as another member to see the app with their access (role debugging)
 */
export const getImpersonableUsers = <ThrowOnError extends boolean = false>(options?: Options<GetImpersonableUsersData, ThrowOnError>) => (options?.client ?? client).get<GetImpersonableUsersResponses, GetImpersonableUsersErrors, ThrowOnError>({ url: '/api/user/impersonable', ...options });

/**
 * Get current user's personal token
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * None (no additional RBAC permission required)
 */
export const getUserToken = <ThrowOnError extends boolean = false>(options?: Options<GetUserTokenData, ThrowOnError>) => (options?.client ?? client).get<GetUserTokenResponses, GetUserTokenErrors, ThrowOnError>({ url: '/api/user-tokens/me', ...options });

/**
 * Get the full personal token value
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * None (no additional RBAC permission required)
 */
export const getUserTokenValue = <ThrowOnError extends boolean = false>(options?: Options<GetUserTokenValueData, ThrowOnError>) => (options?.client ?? client).get<GetUserTokenValueResponses, GetUserTokenValueErrors, ThrowOnError>({ url: '/api/user-tokens/me/value', ...options });

/**
 * Rotate personal token (generate new value)
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * None (no additional RBAC permission required)
 */
export const rotateUserToken = <ThrowOnError extends boolean = false>(options?: Options<RotateUserTokenData, ThrowOnError>) => (options?.client ?? client).post<RotateUserTokenResponses, RotateUserTokenErrors, ThrowOnError>({ url: '/api/user-tokens/me/rotate', ...options });

/**
 * Get virtual API keys visible to the current user, with provider key mappings
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `llmVirtualKey:read`: View LLM virtual keys
 */
export const getAllVirtualApiKeys = <ThrowOnError extends boolean = false>(options?: Options<GetAllVirtualApiKeysData, ThrowOnError>) => (options?.client ?? client).get<GetAllVirtualApiKeysResponses, GetAllVirtualApiKeysErrors, ThrowOnError>({ url: '/api/llm-virtual-keys', ...options });

/**
 * Create a new virtual API key. Returns the full token value once.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `llmVirtualKey:create`: Create LLM virtual keys
 */
export const createVirtualApiKey = <ThrowOnError extends boolean = false>(options: Options<CreateVirtualApiKeyData, ThrowOnError>) => (options.client ?? client).post<CreateVirtualApiKeyResponses, CreateVirtualApiKeyErrors, ThrowOnError>({
    url: '/api/llm-virtual-keys',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete a virtual API key
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `llmVirtualKey:delete`: Delete LLM virtual keys
 */
export const deleteVirtualApiKey = <ThrowOnError extends boolean = false>(options: Options<DeleteVirtualApiKeyData, ThrowOnError>) => (options.client ?? client).delete<DeleteVirtualApiKeyResponses, DeleteVirtualApiKeyErrors, ThrowOnError>({ url: '/api/llm-virtual-keys/{id}', ...options });

/**
 * Get a virtual API key visible to the current user, with provider key mappings
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `llmVirtualKey:read`: View LLM virtual keys
 */
export const getVirtualApiKey = <ThrowOnError extends boolean = false>(options: Options<GetVirtualApiKeyData, ThrowOnError>) => (options.client ?? client).get<GetVirtualApiKeyResponses, GetVirtualApiKeyErrors, ThrowOnError>({ url: '/api/llm-virtual-keys/{id}', ...options });

/**
 * Update a virtual API key
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `llmVirtualKey:update`: Modify LLM virtual keys and their visibility
 */
export const updateVirtualApiKey = <ThrowOnError extends boolean = false>(options: Options<UpdateVirtualApiKeyData, ThrowOnError>) => (options.client ?? client).patch<UpdateVirtualApiKeyResponses, UpdateVirtualApiKeyErrors, ThrowOnError>({
    url: '/api/llm-virtual-keys/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get the raw value of a virtual API key. Only the key's author can reveal it.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `llmVirtualKey:read`: View LLM virtual keys
 */
export const getVirtualApiKeyValue = <ThrowOnError extends boolean = false>(options: Options<GetVirtualApiKeyValueData, ThrowOnError>) => (options.client ?? client).get<GetVirtualApiKeyValueResponses, GetVirtualApiKeyValueErrors, ThrowOnError>({ url: '/api/llm-virtual-keys/{id}/value', ...options });

/**
 * Delete several virtual API keys in one request. Each id is authorized exactly as the single-key delete authorizes its own, so a key the caller cannot see or manage is reported in `failed` while the rest of the batch still applies.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `llmVirtualKey:delete`: Delete LLM virtual keys
 */
export const bulkDeleteVirtualApiKeys = <ThrowOnError extends boolean = false>(options: Options<BulkDeleteVirtualApiKeysData, ThrowOnError>) => (options.client ?? client).delete<BulkDeleteVirtualApiKeysResponses, BulkDeleteVirtualApiKeysErrors, ThrowOnError>({
    url: '/api/llm-virtual-keys/bulk',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Create embeddings with vLLM (uses default agent)
 *
 * Authentication:
 *
 * This route accepts either an LLM provider API key or a Virtual API Key. See [LLM Proxy Authentication](/docs/platform-llm-proxy-authentication).
 */
export const vllmEmbeddingsWithDefaultAgent = <ThrowOnError extends boolean = false>(options: Options<VllmEmbeddingsWithDefaultAgentData, ThrowOnError>) => (options.client ?? client).post<VllmEmbeddingsWithDefaultAgentResponses, VllmEmbeddingsWithDefaultAgentErrors, ThrowOnError>({
    url: '/v1/vllm/embeddings',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Create embeddings with vLLM for a specific agent
 *
 * Authentication:
 *
 * This route accepts either an LLM provider API key or a Virtual API Key. See [LLM Proxy Authentication](/docs/platform-llm-proxy-authentication).
 */
export const vllmEmbeddingsWithAgent = <ThrowOnError extends boolean = false>(options: Options<VllmEmbeddingsWithAgentData, ThrowOnError>) => (options.client ?? client).post<VllmEmbeddingsWithAgentResponses, VllmEmbeddingsWithAgentErrors, ThrowOnError>({
    url: '/v1/vllm/{agentId}/embeddings',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Create a chat completion with vLLM (uses default agent)
 *
 * Authentication:
 *
 * This route accepts either an LLM provider API key or a Virtual API Key. See [LLM Proxy Authentication](/docs/platform-llm-proxy-authentication).
 */
export const vllmChatCompletionsWithDefaultAgent = <ThrowOnError extends boolean = false>(options: Options<VllmChatCompletionsWithDefaultAgentData, ThrowOnError>) => (options.client ?? client).post<VllmChatCompletionsWithDefaultAgentResponses, VllmChatCompletionsWithDefaultAgentErrors, ThrowOnError>({
    url: '/v1/vllm/chat/completions',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Create a chat completion with vLLM for a specific agent
 *
 * Authentication:
 *
 * This route accepts either an LLM provider API key or a Virtual API Key. See [LLM Proxy Authentication](/docs/platform-llm-proxy-authentication).
 */
export const vllmChatCompletionsWithAgent = <ThrowOnError extends boolean = false>(options: Options<VllmChatCompletionsWithAgentData, ThrowOnError>) => (options.client ?? client).post<VllmChatCompletionsWithAgentResponses, VllmChatCompletionsWithAgentErrors, ThrowOnError>({
    url: '/v1/vllm/{agentId}/chat/completions',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Create a chat completion with xAI (uses default agent)
 *
 * Authentication:
 *
 * This route accepts either an LLM provider API key or a Virtual API Key. See [LLM Proxy Authentication](/docs/platform-llm-proxy-authentication).
 */
export const xaiChatCompletionsWithDefaultAgent = <ThrowOnError extends boolean = false>(options: Options<XaiChatCompletionsWithDefaultAgentData, ThrowOnError>) => (options.client ?? client).post<XaiChatCompletionsWithDefaultAgentResponses, XaiChatCompletionsWithDefaultAgentErrors, ThrowOnError>({
    url: '/v1/xai/chat/completions',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Create a chat completion with xAI for a specific agent
 *
 * Authentication:
 *
 * This route accepts either an LLM provider API key or a Virtual API Key. See [LLM Proxy Authentication](/docs/platform-llm-proxy-authentication).
 */
export const xaiChatCompletionsWithAgent = <ThrowOnError extends boolean = false>(options: Options<XaiChatCompletionsWithAgentData, ThrowOnError>) => (options.client ?? client).post<XaiChatCompletionsWithAgentResponses, XaiChatCompletionsWithAgentErrors, ThrowOnError>({
    url: '/v1/xai/{agentId}/chat/completions',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Start the xAI OAuth device flow used to connect an X Premium (SuperGrok) subscription as an xAI provider credential
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * None (no additional RBAC permission required)
 */
export const xaiSubscriptionDeviceAuthStart = <ThrowOnError extends boolean = false>(options?: Options<XaiSubscriptionDeviceAuthStartData, ThrowOnError>) => (options?.client ?? client).post<XaiSubscriptionDeviceAuthStartResponses, XaiSubscriptionDeviceAuthStartErrors, ThrowOnError>({ url: '/api/xai-subscription-auth/device/start', ...options });

/**
 * Poll the xAI OAuth device flow once; returns the encoded provider credential when the user has authorized
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * None (no additional RBAC permission required)
 */
export const xaiSubscriptionDeviceAuthPoll = <ThrowOnError extends boolean = false>(options: Options<XaiSubscriptionDeviceAuthPollData, ThrowOnError>) => (options.client ?? client).post<XaiSubscriptionDeviceAuthPollResponses, XaiSubscriptionDeviceAuthPollErrors, ThrowOnError>({
    url: '/api/xai-subscription-auth/device/poll',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Create embeddings with Zhipu AI (uses default agent)
 *
 * Authentication:
 *
 * This route accepts either an LLM provider API key or a Virtual API Key. See [LLM Proxy Authentication](/docs/platform-llm-proxy-authentication).
 */
export const zhipuaiEmbeddingsWithDefaultAgent = <ThrowOnError extends boolean = false>(options: Options<ZhipuaiEmbeddingsWithDefaultAgentData, ThrowOnError>) => (options.client ?? client).post<ZhipuaiEmbeddingsWithDefaultAgentResponses, ZhipuaiEmbeddingsWithDefaultAgentErrors, ThrowOnError>({
    url: '/v1/zhipuai/embeddings',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Create embeddings with Zhipu AI for a specific agent
 *
 * Authentication:
 *
 * This route accepts either an LLM provider API key or a Virtual API Key. See [LLM Proxy Authentication](/docs/platform-llm-proxy-authentication).
 */
export const zhipuaiEmbeddingsWithAgent = <ThrowOnError extends boolean = false>(options: Options<ZhipuaiEmbeddingsWithAgentData, ThrowOnError>) => (options.client ?? client).post<ZhipuaiEmbeddingsWithAgentResponses, ZhipuaiEmbeddingsWithAgentErrors, ThrowOnError>({
    url: '/v1/zhipuai/{agentId}/embeddings',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Create a chat completion with Zhipu AI (uses default agent)
 *
 * Authentication:
 *
 * This route accepts either an LLM provider API key or a Virtual API Key. See [LLM Proxy Authentication](/docs/platform-llm-proxy-authentication).
 */
export const zhipuaiChatCompletionsWithDefaultAgent = <ThrowOnError extends boolean = false>(options: Options<ZhipuaiChatCompletionsWithDefaultAgentData, ThrowOnError>) => (options.client ?? client).post<ZhipuaiChatCompletionsWithDefaultAgentResponses, ZhipuaiChatCompletionsWithDefaultAgentErrors, ThrowOnError>({
    url: '/v1/zhipuai/chat/completions',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Create a chat completion with Zhipu AI for a specific agent
 *
 * Authentication:
 *
 * This route accepts either an LLM provider API key or a Virtual API Key. See [LLM Proxy Authentication](/docs/platform-llm-proxy-authentication).
 */
export const zhipuaiChatCompletionsWithAgent = <ThrowOnError extends boolean = false>(options: Options<ZhipuaiChatCompletionsWithAgentData, ThrowOnError>) => (options.client ?? client).post<ZhipuaiChatCompletionsWithAgentResponses, ZhipuaiChatCompletionsWithAgentErrors, ThrowOnError>({
    url: '/v1/zhipuai/{agentId}/chat/completions',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete several custom roles in one request. A role that cannot be deleted — a predefined one, or one still held by a member — is reported in `failed` with that reason, and the rest of the batch still applies.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `ac:delete`: Delete custom roles
 */
export const bulkDeleteRoles = <ThrowOnError extends boolean = false>(options: Options<BulkDeleteRolesData, ThrowOnError>) => (options.client ?? client).delete<BulkDeleteRolesResponses, BulkDeleteRolesErrors, ThrowOnError>({
    url: '/api/roles/bulk',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get public identity provider list for login page (no secrets exposed)
 *
 * Authentication:
 *
 * Not required.
 *
 * Authorization:
 *
 * None (no additional RBAC permission required)
 */
export const getPublicIdentityProviders = <ThrowOnError extends boolean = false>(options?: Options<GetPublicIdentityProvidersData, ThrowOnError>) => (options?.client ?? client).get<GetPublicIdentityProvidersResponses, GetPublicIdentityProvidersErrors, ThrowOnError>({ url: '/api/identity-providers/public', ...options });

/**
 * Get all identity providers with configuration (admin only, secrets redacted)
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `identityProvider:read`: View identity provider configurations (SSO)
 */
export const getIdentityProviders = <ThrowOnError extends boolean = false>(options?: Options<GetIdentityProvidersData, ThrowOnError>) => (options?.client ?? client).get<GetIdentityProvidersResponses, GetIdentityProvidersErrors, ThrowOnError>({ url: '/api/identity-providers', ...options });

/**
 * Create a new identity provider (secrets redacted)
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `identityProvider:create`: Set up new identity providers
 */
export const createIdentityProvider = <ThrowOnError extends boolean = false>(options: Options<CreateIdentityProviderData, ThrowOnError>) => (options.client ?? client).post<CreateIdentityProviderResponses, CreateIdentityProviderErrors, ThrowOnError>({
    url: '/api/identity-providers',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Get identity providers as minimal team-sync options (id, name, and groups extraction template) for linking external groups to teams
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `team:read`: View teams and their members
 */
export const getIdentityProviderTeamSyncOptions = <ThrowOnError extends boolean = false>(options?: Options<GetIdentityProviderTeamSyncOptionsData, ThrowOnError>) => (options?.client ?? client).get<GetIdentityProviderTeamSyncOptionsResponses, GetIdentityProviderTeamSyncOptionsErrors, ThrowOnError>({ url: '/api/identity-providers/team-sync-options', ...options });

/**
 * Get the IdP logout URL for the current user's identity provider
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * None (no additional RBAC permission required)
 */
export const getIdentityProviderIdpLogoutUrl = <ThrowOnError extends boolean = false>(options?: Options<GetIdentityProviderIdpLogoutUrlData, ThrowOnError>) => (options?.client ?? client).get<GetIdentityProviderIdpLogoutUrlResponses, GetIdentityProviderIdpLogoutUrlErrors, ThrowOnError>({ url: '/api/identity-providers/idp-logout-url', ...options });

/**
 * Get whether the current user has a usable token for this identity provider
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `mcpServerInstallation:create`: Install MCP servers from the registry
 */
export const getIdentityProviderLinkStatus = <ThrowOnError extends boolean = false>(options: Options<GetIdentityProviderLinkStatusData, ThrowOnError>) => (options.client ?? client).get<GetIdentityProviderLinkStatusResponses, GetIdentityProviderLinkStatusErrors, ThrowOnError>({ url: '/api/identity-providers/{id}/link-status', ...options });

/**
 * Get decoded claims from the current user's latest identity-provider tokens
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `team:read`: View teams and their members
 */
export const getIdentityProviderLatestIdTokenClaims = <ThrowOnError extends boolean = false>(options: Options<GetIdentityProviderLatestIdTokenClaimsData, ThrowOnError>) => (options.client ?? client).get<GetIdentityProviderLatestIdTokenClaimsResponses, GetIdentityProviderLatestIdTokenClaimsErrors, ThrowOnError>({ url: '/api/identity-providers/{id}/latest-id-token-claims', ...options });

/**
 * Delete identity provider
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `identityProvider:delete`: Remove identity providers
 */
export const deleteIdentityProvider = <ThrowOnError extends boolean = false>(options: Options<DeleteIdentityProviderData, ThrowOnError>) => (options.client ?? client).delete<DeleteIdentityProviderResponses, DeleteIdentityProviderErrors, ThrowOnError>({ url: '/api/identity-providers/{id}', ...options });

/**
 * Get identity provider by ID (secrets redacted)
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `identityProvider:read`: View identity provider configurations (SSO)
 */
export const getIdentityProvider = <ThrowOnError extends boolean = false>(options: Options<GetIdentityProviderData, ThrowOnError>) => (options.client ?? client).get<GetIdentityProviderResponses, GetIdentityProviderErrors, ThrowOnError>({ url: '/api/identity-providers/{id}', ...options });

/**
 * Update identity provider (secrets redacted)
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `identityProvider:update`: Modify identity provider settings
 */
export const updateIdentityProvider = <ThrowOnError extends boolean = false>(options: Options<UpdateIdentityProviderData, ThrowOnError>) => (options.client ?? client).put<UpdateIdentityProviderResponses, UpdateIdentityProviderErrors, ThrowOnError>({
    url: '/api/identity-providers/{id}',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Ingest a batch of product-usage (RUM) events from the web client. Events are forwarded as OTLP log records to the deployment-configured collector; when no RUM export endpoint is configured the batch is acknowledged and dropped.
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * None (no additional RBAC permission required)
 */
export const ingestRumEvents = <ThrowOnError extends boolean = false>(options: Options<IngestRumEventsData, ThrowOnError>) => (options.client ?? client).post<IngestRumEventsResponses, IngestRumEventsErrors, ThrowOnError>({
    url: '/api/rum/events',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Delete a team's Vault folder mapping
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `team:update`: Modify team settings
 */
export const deleteTeamVaultFolder = <ThrowOnError extends boolean = false>(options: Options<DeleteTeamVaultFolderData, ThrowOnError>) => (options.client ?? client).delete<DeleteTeamVaultFolderResponses, DeleteTeamVaultFolderErrors, ThrowOnError>({ url: '/api/teams/{teamId}/vault-folder', ...options });

/**
 * Get a team's Vault folder configuration
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `team:read`: View teams and their members
 */
export const getTeamVaultFolder = <ThrowOnError extends boolean = false>(options: Options<GetTeamVaultFolderData, ThrowOnError>) => (options.client ?? client).get<GetTeamVaultFolderResponses, GetTeamVaultFolderErrors, ThrowOnError>({ url: '/api/teams/{teamId}/vault-folder', ...options });

/**
 * Set or update a team's Vault folder path
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `team:update`: Modify team settings
 */
export const setTeamVaultFolder = <ThrowOnError extends boolean = false>(options: Options<SetTeamVaultFolderData, ThrowOnError>) => (options.client ?? client).post<SetTeamVaultFolderResponses, SetTeamVaultFolderErrors, ThrowOnError>({
    url: '/api/teams/{teamId}/vault-folder',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * Check connectivity to a team's Vault folder
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `team:update`: Modify team settings
 */
export const checkTeamVaultFolderConnectivity = <ThrowOnError extends boolean = false>(options: Options<CheckTeamVaultFolderConnectivityData, ThrowOnError>) => (options.client ?? client).post<CheckTeamVaultFolderConnectivityResponses, CheckTeamVaultFolderConnectivityErrors, ThrowOnError>({
    url: '/api/teams/{teamId}/vault-folder/check-connectivity',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});

/**
 * List secrets available in a team's Vault folder
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `team:read`: View teams and their members
 */
export const listTeamVaultFolderSecrets = <ThrowOnError extends boolean = false>(options: Options<ListTeamVaultFolderSecretsData, ThrowOnError>) => (options.client ?? client).get<ListTeamVaultFolderSecretsResponses, ListTeamVaultFolderSecretsErrors, ThrowOnError>({ url: '/api/teams/{teamId}/vault-folder/secrets', ...options });

/**
 * Get the keys of a specific secret in a team's Vault folder
 *
 * Authentication:
 *
 * Required. Use an authenticated browser session or send your Archestra API key in the `Authorization` header.
 *
 * Authorization:
 *
 * `team:read`: View teams and their members
 */
export const getTeamVaultSecretKeys = <ThrowOnError extends boolean = false>(options: Options<GetTeamVaultSecretKeysData, ThrowOnError>) => (options.client ?? client).post<GetTeamVaultSecretKeysResponses, GetTeamVaultSecretKeysErrors, ThrowOnError>({
    url: '/api/teams/{teamId}/vault-folder/secrets/keys',
    ...options,
    headers: {
        'Content-Type': 'application/json',
        ...options.headers
    }
});
